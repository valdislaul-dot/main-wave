# 多端可视化部署指南 (2026-08-16)

## 一、架构

```
┌───────────── 本地 Win/Mac ─────────────┐        ┌───── 云端/多端 ─────┐
│  9:25 竞价采集 (morning_check)          │        │  Streamlit Cloud    │
│  盘后流水线 (run_pipeline) ──┐           │  git   │  gui_cloud.py       │
│  本地GUI (gui_dashboard)     │           │ push  │  (只读快照)         │
│                              ▼           │ ────▶ │                     │
│  关键快照 → sync_cloud.py → GitHub 仓库  │        │  手机/平板/iOS      │
└──────────────────────────────────────────┘        └─────────────────────┘
```

**核心原则**：采集留在本地（9:25 精确时点 + A股免费API对海外IP不友好），展示可以多端（只读快照）。

## 二、本地端（Win/Mac）

### 完整 GUI
```bash
streamlit run scripts/daily/gui_dashboard.py
```
- 实时评分（读 data/kline_data）
- 一键刷新（跑 run_pipeline --fast）
- 多持仓 + 竞价决策 + 涨停池 + 交易记录

### 数据上云
盘后流水线已自动集成（Step 9），也可手动：
```bash
python scripts/daily/sync_cloud.py          # add+commit+push
python scripts/daily/sync_cloud.py --status # 查看将同步哪些文件
```

## 三、局域网访问（零成本，白送）

Streamlit 默认监听所有网卡，手机/平板连同一 WiFi，浏览器打开：
```
http://<主机局域网IP>:8501
```
查 IP：Win `ipconfig` / Mac `ifconfig en0`（形如 192.168.x.x）。

## 四、公网访问（出门在外）

### 方案 A：Streamlit Cloud（免费，推荐先试）
1. 确保关键快照已 push 到 GitHub（`sync_cloud.py` 或手动）；
2. 打开 [share.streamlit.io](https://share.streamlit.io) → New app；
3. 选仓库 → **Main file: `scripts/daily/gui_cloud.py`** → Deploy；
4. 获得 `https://xxx.streamlit.app`，iPhone/Safari 打开，添加到主屏幕。

> 注意：Cloud 服务器在海外，`gui_cloud.py` **不实时拉行情**，只读已同步的快照。
> 实时竞价/盘中行情必须靠方案 B 连回本地。

### 方案 B：Tailscale/ZeroTier 组网（数据实时）
1. 家里电脑装 [Tailscale](https://tailscale.com) 并登录；
2. 手机装 Tailscale App 登录同一账号；
3. 手机浏览器开 `http://<家里电脑的Tailscale IP>:8501`（本地版 gui_dashboard，数据实时）。

## 五、数据流说明

| 数据 | 本地采集 | 上云(只读快照) |
|------|---------|---------------|
| 涨停池 zt_pool_state.json | 盘后流水线 | ✅ |
| 竞价快照 auction/*.json + auction_state.json | 9:25 morning_check | ✅ |
| 候选评分 candidates_*.json | 盘后流水线 | ✅ |
| 持仓/交易 portfolio/trading_journal | 手动记账 | ✅ |
| K线 kline_data/ (3048只) | 腾讯fqkline+新浪 | ❌ 过大不上云 |
| 实时行情(竞价/盘中) | 本地拉腾讯/东财 | ❌ 仅本地/Tailscale |

**结论**：云端看的是「最近一次同步的快照」；要看实时竞价/盘中，用局域网或 Tailscale 连回本地。
