---
phase: 03-trigger-runner-job-registry-locks-auth-enforcement
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
files_modified:
  - .planning/phases/03-trigger-runner-job-registry-locks-auth-enforcement/03-04-SUMMARY.md
autonomous: true
requirements:
  - ACT-01
  - ACT-02
  - ACT-03
  - SEC-01
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The resident API service is running the new code: after restart it answers GET /health 200 without a key, protected POST/GET paths enforce the pinned 401 (missing API key + WWW-Authenticate: ApiKey) / 403 (invalid API key) contract against the live service, and rejected requests spawn nothing (registry dir count unchanged) (SC4 live, SEC-01)"
    - "A valid-key live trigger of the health-check kind returns 202 with a job_id, GET /v1/jobs/{job_id} polls to a terminal succeeded state with an exit_code and an existing UTF-8-decodable log file, and the key byte string appears in neither the job log nor the tail of logs/api/console.log (SC1/SC4 live, ACT-01/ACT-02)"
    - "A real pipeline run stays single-flight on the live machine: a second POST /v1/actions/pipeline while the first job is running returns 409 whose detail object carries the running job_id of the first run (SC2 live, ACT-03)"
    - "GET /health keeps answering during the real pipeline run with a measured p95 wall time under 50 ms (SC3 live — the in-suite p95 pin from 03-01 confirmed against live uvicorn)"
    - "Killing the API process tree mid-run (taskkill /F /T on the port-8000 owner pid), restarting the service, and polling GET /v1/jobs/{job_id} yields the deterministic interrupted terminal state with finished_at set and the partial job log still present — no job silently lost (SC5 live, ACT-02 reload sweep through the real boot path)"
    - "After the kill/restart the pipeline lock is free again: job_lock.acquire('pipeline', 'data/locks') returns an open handle immediately (OS auto-release on holder death — probe V2 confirmed through the real crash) and a fresh health-check trigger succeeds, proving the service returned to normal operation"
    - "The stale 15:30 scheduled task stays absent — the scheduled-task query by name (流水线/主升浪/pipeline) returns nothing but the gogo-api task; no elevated unregister is needed (D-02 verify-only, RESEARCH V5 machine evidence re-confirmed) and the dead auto_start.bat / install_scheduled_task.ps1 files remain untouched in the repo"
    - "python -m pytest -q stays green at the phase gate, git status --porcelain -- data/ logs/ is empty (locks and registry never enter git/sync_cloud scope), the API is left running at the end, and .planning/phases/03-trigger-runner-job-registry-locks-auth-enforcement/03-04-SUMMARY.md records every live result"
  artifacts:
    - path: ".planning/phases/03-trigger-runner-job-registry-locks-auth-enforcement/03-04-SUMMARY.md"
      provides: "The live-verification record: restart method used, auth smoke results, SC1-SC5 evidence (job ids, timings, p95 figure), D-02 re-confirmation output, GUI human-gate cross-reference, and the final machine state"
      contains: "live smoke evidence, p95 number, SC5 interrupted job evidence"
    - path: "logs/api/jobs/{job_id}.json (app-produced)"
      provides: "The interrupted job record created by the real kill/restart — the live proof of the reload sweep (03-01) wired through main() (03-02); consumed read-only, never edited by the executor"
  key_links:
    - from: "service restart"
      to: "gogo-api scheduled task / run_api.bat"
      via: "stop + start via the existing Task Scheduler task (research V5: task Ready, state 0xC000013A is the historical Ctrl+C exit); Start-ScheduledTask -TaskName gogo-api primary, run_api.bat manual fallback — the registry reload runs inside main() on this boot (Pattern 4 placement from 03-02)"
    - from: "taskkill /F /T"
      to: "port 8000 owner pid"
      via: "Get-NetTCPConnection -LocalPort 8000 → OwningProcess → taskkill /F /T /PID — the tree flag kills the child pipeline too (Pitfall 3), keeping the crash scenario clean; never a broad python.exe kill"
    - from: "GET /v1/jobs/{job_id} after restart"
      to: "jobs.reload_registry() boot call"
      via: "the interrupted status comes from the sweep running during main() boot — the live proof that boot-time reload (03-02 Task 1) executes before uvicorn accepts requests"
    - from: "key byte grep"
      to: "api.boot.read_token chain"
      via: "the smoke script resolves the token exactly like the API does (env GOGO_API_TOKEN first, data/api_token.txt second) inside python — the token never appears in argv, shell history, or process listings"
  prohibitions:
    - statement: "The API key MUST be read and used inside python only (api.boot.read_token with env-first semantics) — it must never appear in a shell command, argv, curl -H, a committed file, or console output; smoke scripts live under the OS temp dir, never inside the repo"
      category: safety
      requirement_id: SEC-01
      status: unverified
    - statement: "A real pipeline trigger MUST only run outside the weekday 09:15-15:00 window or with the user's explicit approval (the CLAUDE.md time rule applies: date +'%H:%M' first) — a live run inside the morning window would collide with the user's auction flow and data writes"
      category: safety
      requirement_id: ACT-01
      status: unverified
    - statement: "Killing MUST be tree-scoped to the API owner pid of port 8000 (taskkill /F /T) — never a blanket taskkill on python.exe, and never a kill while the user's GUI or another consumer is mid-run without coordination"
      category: safety
      requirement_id: ACT-02
      status: unverified
    - statement: "logs/api/console.log and the job registry files MUST be read-only to this plan (tail/read; never truncated, deleted, or edited) and git status --porcelain -- data/ logs/ must stay empty — lock files and registry never enter git or sync_cloud scope"
      category: safety
      requirement_id: SEC-01
      status: unverified
    - statement: "If the scheduled-task query finds the stale 主升浪 task (contradicting RESEARCH V5's machine finding), work MUST halt and report — D-02 would change from verify-only to an elevated-unregister action needing a new plan delta"
      category: safety
      requirement_id: ACT-03
      status: unverified
    - statement: "The API MUST be left running at the end of the gate (restore the operational state found at research time was 'down' — the phase leaves the resident service up so the user can consume it next morning)"
      category: safety
      requirement_id: ACT-01
      status: unverified
---

# Plan 03-04 — Real-machine live gate: service restart, SC1-SC5 smoke, SC5 kill/restart, D-02 verify-only (ops, no code changes)

## User-story framing

**As a** user who will consume the API from the cloud panel and scheduled integrator before market open, **I want to** know the resident service on this box actually enforces the whole phase contract on live hardware — real 202/409/401/403 responses, /health under 50 ms while a real pipeline runs, a killed-and-restarted API that turns my in-flight job into a queryable interrupted record, and a machine free of the stale 15:30 task — **so that** tomorrow's first trigger is a routine call, not a discovery.

<objective>
Run the phase's real-machine gate (03-VALIDATION Manual-Only Verifications) against the resident service: restart the API onto the wave 1-2 code (registry reload runs at boot), smoke the live contract with a throwaway temp-dir script (auth 401/403 + challenge header, no-spawn on rejection, valid-key health-check trigger → 202 → polled succeeded, key-bytes absent from job log and console.log tail), prove SC2/SC3/SC5 with one real pipeline run (409-with-running-job-id while running, /health p95 < 50 ms during the run, taskkill /F /T mid-run → service restart → the job queryable as interrupted → pipeline lock verifiably auto-released → service back to normal), re-confirm D-02's machine finding (stale scheduled task absent — verify-only, no elevated command), and close the phase with the full pytest suite, hygiene checks, and the SUMMARY record.

Purpose: TestClient coverage cannot exercise real process kill/restart, real uvicorn timing, or the real Task Scheduler boot — these are the phase's five success criteria as the user will actually experience them (03-VALIDATION Manual-Only Verifications table). No production code changes are in scope; this plan is an evidence gate.

Output: a live-verification record in 03-04-SUMMARY.md; the service left running on the new code.
</objective>

## Flagged Assumptions (spec-less probe fallback — continuation)

- [ASSUMED A5] The fake-script suite fully covers the spawn/409/auth logic; the live smoke closes the residual integration gap (real scripts, real uvicorn, real Task Scheduler boot). If a live step contradicts the suite, the suite finding wins for logic and the live discrepancy is recorded with evidence — never papered over.
- D-02 evidence supersession: 03-CONTEXT D-02 assumed the 15:30 task exists and needs an elevated unregister; RESEARCH V5 machine-verified it is ABSENT (201-task query; only gogo-api matches). This plan therefore treats D-02 as verify-only, with a hard halt if the re-query contradicts V5 (prohibition).
- The health-check kind is read-mostly and safe to run at any time; the pipeline kind performs real network data collection and writes data/ (its designed function) — hence the market-hours precondition on pipeline triggers.
- Research recorded the API as currently down with the gogo-api task Ready; if the machine state differs when this plan runs, the executor adapts the restart to the observed state and records it (last task result 0xC000013A = the historical Ctrl+C console-exit pattern, already in STATE.md).

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-trigger-runner-job-registry-locks-auth-enforcement/03-01-SUMMARY.md
@.planning/phases/03-trigger-runner-job-registry-locks-auth-enforcement/03-02-SUMMARY.md
@.planning/phases/03-trigger-runner-job-registry-locks-auth-enforcement/03-03-SUMMARY.md
@.planning/phases/03-trigger-runner-job-registry-locks-auth-enforcement/03-VALIDATION.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Restart the resident service onto the wave 1-2 code + live auth/trigger smoke (SC1/SC4 live) with a temp-dir script</name>
  <files>.planning/phases/03-trigger-runner-job-registry-locks-auth-enforcement/03-04-SUMMARY.md</files>
  <read_first>
    - 03-VALIDATION.md Manual-Only Verifications table (SC1-SC4 smoke rows) — C:/Users/Davis/Desktop/项目/gogo/.planning/phases/03-trigger-runner-job-registry-locks-auth-enforcement/03-VALIDATION.md
    - 03-RESEARCH.md probe V5 machine-state section (gogo-api task Ready, console.log path, API currently down) and Pattern 5 key-hygiene paragraph — C:/Users/Davis/Desktop/项目/gogo/.planning/phases/03-trigger-runner-job-registry-locks-auth-enforcement/03-RESEARCH.md
    - C:/Users/Davis/Desktop/项目/gogo/api/boot.py (read_token — the smoke script reuses it), C:/Users/Davis/Desktop/项目/gogo/run_api.bat (restart fallback entry point)
  </read_first>
  <action>
    Restart the resident API onto the new code and run the live auth + trigger smoke. First run `date +"%H:%M"` (the CLAUDE.md time rule); this task's kinds (health-check) are safe at any time, but the rule still precedes every operation block.

    1. Machine state: probe 127.0.0.1:8000/health (curl, 2 s timeout). If the service is already answering, identify the code generation by checking whether GET /v1/jobs/{any-32-hex-id} answers 404 job not found (new code) vs 404 Not Found (old routerless code) — if old code is running, restart as below; if new code is running (a prior partial run), skip the restart and proceed to the smoke, recording the finding.
    2. Restart onto new code: find the owner pid of port 8000 (`powershell -Command "Get-NetTCPConnection -LocalPort 8000 -State Listen | Select-Object -ExpandProperty OwningProcess"`), stop it via `taskkill /F /T /PID <pid>` only if a listener exists (a stale console from research time may hold the port); then start the service via `powershell -Command "Start-ScheduledTask -TaskName 'gogo-api'"`; if the task start fails, fall back to launching run_api.bat in a new console window and record which method worked. Wait for /health 200 (poll up to 60 s).
    3. Throwaway smoke script (never inside the repo): write /tmp/gsd_p3_live_smoke.py (OS temp dir, deleted after the run) — a python script that inserts the repo root into sys.path, reuses api.boot.read_token (env GOGO_API_TOKEN first, data/api_token.txt second — the token exists only inside the process), and prints one PASS/FAIL line per check, exiting non-zero on any FAIL:
       - no-key GET /health → 200 (public, unchanged);
       - no-key POST /v1/actions/health-check → 401 with body detail missing API key and WWW-Authenticate header value ApiKey;
       - wrong-key POST /v1/actions/health-check → 403 with detail invalid API key;
       - registry stillness: count logs/api/jobs/*.json before and ~1.5 s after the two rejected requests — counts equal (rejections spawn nothing);
       - valid-key POST /v1/actions/health-check → 202 with job_id/kind/status pending; immediate second valid-key POST → either 409 (job still live — assert detail object message health-check already running with a matching running_job_id) or 202 (job finished too fast — record which, tolerate the race, the definitive live 409 proof is the pipeline run in the next task);
       - poll GET /v1/jobs/{job_id} to terminal → succeeded, exit_code 0, log_path present and existing; log file bytes decode as UTF-8;
       - key hygiene: assert the token byte string is absent from the job log file bytes AND from the tail (last 256 KB) of logs/api/console.log;
       - print a final line with the polled job's status and the health-check wall duration.
       All HTTP via urllib with the X-API-Key header set in-process. The token must never appear in argv, curl flags, output, or any file inside the repo.
    4. Record results (job id, p95 if measured, PASS lines) into the 03-04 SUMMARY as SC1/SC4-live evidence, and leave the service running.
  </action>
  <verify>
    <automated>python /tmp/gsd_p3_live_smoke.py</automated>
    <fails_when>non-zero exit or any FAIL line printed (auth contract bytes, registry stillness after 401/403, or key-bytes-in-log/console detection failed against the live service)</fails_when>
    <automated>curl -s -o /dev/null -w "%{http_code}" --max-time 5 http://127.0.0.1:8000/health</automated>
    <fails_when>output differs from 200 (service not answering after restart)</fails_when>
    <automated>git status --porcelain -- data/ logs/</automated>
    <fails_when>non-empty output (the smoke or restart dirtied the real trees)</fails_when>
  </verify>
  <acceptance_criteria>
    - The service runs the wave 1-2 code (404-job-not-found probe or documented fresh restart) and answers /health 200
    - The smoke script printed PASS for every check: 401 + challenge header, 403, registry stillness after rejections, valid-key 202 → polled succeeded with existing UTF-8 log, token bytes absent from job log and console.log tail
    - The smoke script exited 0; git status --porcelain -- data/ logs/ is empty; the service is left running; results recorded in the SUMMARY
  </acceptance_criteria>
  <done>The live service enforces the auth contract and runs a real health-check job to a succeeded terminal state with the key provably absent from every log — SC1 and SC4 proven on live uvicorn.</done>
</task>

<task type="auto">
  <name>SC2/SC3/SC5 live: real pipeline run — 409 while running, /health p95 < 50 ms, taskkill /F /T mid-run, restart, interrupted, lock released</name>
  <files>.planning/phases/03-trigger-runner-job-registry-locks-auth-enforcement/03-04-SUMMARY.md</files>
  <read_first>
    - 03-RESEARCH.md probe V3 (tasklist raw-byte pid matching) + Pitfall 3 (tree-kill discipline) + Pattern 4 (interrupted semantics) — C:/Users/Davis/Desktop/项目/gogo/.planning/phases/03-trigger-runner-job-registry-locks-auth-enforcement/03-RESEARCH.md
    - C:/Users/Davis/Desktop/项目/gogo/.planning/phases/03-trigger-runner-job-registry-locks-auth-enforcement/03-VALIDATION.md (SC5 manual row)
  </read_first>
  <precondition>Current time is outside the weekday 09:15-15:00 window (run `date +"%H:%M"` first), or the user has explicitly approved a live pipeline run inside the window — a real pipeline collects market data and writes data/, which must not collide with the user's auction flow.</precondition>
  <action>
    Prove SC2/SC3/SC5 with one real pipeline run on the live service. Every token-handling rule from Task 1 applies (token only inside python processes). Sequence:

    1. Preconditions and setup: confirm the service answers /health; check no other consumer is mid-run (a GUI session with the refresh spinning, or an existing job file in a non-terminal state from the smoke — GET each non-terminal file's job via the API is not possible without ids, so instead: list logs/api/jobs/*.json; if any shows a pending/running status, wait for its terminal or stop and report before proceeding). Record the pre-kill registry count.
    2. Trigger and 409 (SC2): via a small python snippet, POST /v1/actions/pipeline with the valid key → 202 (job_id P). Poll GET /v1/jobs/P until status running (pid present — the real run_pipeline --fast step 1 network phase gives ample window). POST /v1/actions/pipeline again → assert 409 with detail object {"message": "pipeline already running", "running_job_id": <P>}.
    3. /health p95 (SC3): while the pipeline keeps running, loop ~120 GET /health requests via urllib recording wall times; compute and print the p95 — must be below 0.05 s.
    4. Kill and restart (SC5): identify the API owner pid of port 8000 (Get-NetTCPConnection). Record the child pipeline's pid from the job record (GET /v1/jobs/P → pid). `taskkill /F /T /PID <api-pid>` — the tree flag kills the pipeline child too (Pitfall 3). Verify the child pid is gone via the probe-V3 raw-byte method (`tasklist /FI "PID eq <child>" /FO CSV /NH` output must NOT contain the quoted pid bytes). Restart the service exactly as in Task 1 (Start-ScheduledTask gogo-api, run_api.bat fallback), wait for /health 200.
    5. Interrupted and recovery: GET /v1/jobs/P (valid key) → status interrupted with a non-null finished_at and the log_path still pointing at an existing (partial) log file — the reload sweep ran during main() boot (03-02 wiring of 03-01). Then prove the OS auto-release through the real crash: run a python snippet that imports scripts/daily/job_lock from the repo and acquires pipeline — the acquire must return an open fd immediately (assert not None), then close it. Finally POST /v1/actions/health-check with the valid key → 202 → poll to succeeded (the service is fully operational again).
    6. Record every evidence line (job ids, the 409 body, the p95 number, taskkill/restart timestamps, the interrupted GET body, the lock-acquire result) into the 03-04 SUMMARY as SC2/SC3/SC5-live evidence. Leave the service running.
  </action>
  <verify>
    <automated>python /tmp/gsd_p3_live_sc5.py</automated>
    <fails_when>non-zero exit or any FAIL line printed (the 409 did not carry the running job id, p95 >= 0.05 s, the killed job did not come back interrupted with finished_at, the post-crash pipeline acquire returned None, or the post-restart health-check did not reach succeeded)</fails_when>
    <automated>git status --porcelain -- data/ logs/</automated>
    <fails_when>non-empty output (registry/lock files stayed out of git scope — logs/ and data/locks/ are ignored by design; any tracked-file change means a discipline break)</fails_when>
  </verify>
  <acceptance_criteria>
    - SC2: second POST during the real pipeline run returned 409 with detail {"message": "pipeline already running", "running_job_id": <P>}
    - SC3: measured /health p95 across ~120 requests during the run is below 0.05 s and recorded
    - SC5: after taskkill /F /T of the port-8000 owner and a clean restart, GET /v1/jobs/P shows interrupted with finished_at set, the child pid no longer exists (tasklist raw-byte check), the partial job log still exists
    - Recovery: job_lock.acquire('pipeline', 'data/locks') returns an open fd immediately after restart; a fresh health-check trigger polls to succeeded; the service is left running; everything recorded in the SUMMARY; git hygiene clean
  </acceptance_criteria>
  <done>The phase's three machine-only success criteria are proven on real hardware with a real pipeline: 409 single-flight while running, /health p95 under 50 ms during the run, and a deterministic interrupted recovery after a tree-kill and restart with the lock verifiably auto-released.</done>
</task>

<task type="auto">
  <name>D-02 verify-only re-confirmation + full-suite/hygiene close-out + SUMMARY + user SC walkthrough (human gate)</name>
  <files>.planning/phases/03-trigger-runner-job-registry-locks-auth-enforcement/03-04-SUMMARY.md</files>
  <read_first>
    - 03-RESEARCH.md 定稿机制 sign-off gate table (D-02 row: task machine-verified absent, auto_start.bat/install_scheduled_task.ps1 are dead files) — C:/Users/Davis/Desktop/项目/gogo/.planning/phases/03-trigger-runner-job-registry-locks-auth-enforcement/03-RESEARCH.md
    - C:/Users/Davis/Desktop/项目/gogo/.planning/phases/03-trigger-runner-job-registry-locks-auth-enforcement/03-VALIDATION.md (Validation Sign-Off list)
  </read_first>
  <action>
    Close the phase gate:

    1. D-02 re-confirmation (verify-only): query the Task Scheduler exactly like research V5 — `powershell -Command "Get-ScheduledTask | Where-Object { $_.TaskName -match '主升浪|选股|流水线|pipeline' } | Format-Table -AutoSize TaskName,State"` (if the Chinese literals mangle in the git-bash → powershell handoff, rerun with the English-only pattern 'pipeline|gogo' and eyeball the full task-name list for 主升浪 entries). Expected: no rows (the gogo-api task does not match these patterns). If any row appears, STOP — D-02 escalates from verify-only to an elevated unregister and this plan cannot proceed without a delta. Also confirm auto_start.bat and install_scheduled_task.ps1 are byte-untouched (git status shows no modification).
    2. Full-suite and hygiene: python -m pytest -q from the repo root (green), git status --porcelain -- data/ logs/ empty, git status --porcelain shows ONLY the intended wave 1-2 tracked changes (scripts/daily/job_lock.py, api/jobs.py, api/auth.py, api/actions.py, api/main.py, scripts/daily/gui_dashboard.py, .gitignore, tests/test_jobs.py, tests/test_auth.py, tests/test_actions.py, .planning/* files) — anything else gets investigated before close.
    3. Write .planning/phases/03-trigger-runner-job-registry-locks-auth-enforcement/03-04-SUMMARY.md: the restart method used and observed pre-state, every PASS line from the Task 1 smoke, the SC2/SC3/SC5 evidence block from Task 2 (job ids, 409 body bytes, p95 figure, kill/restart timestamps, the interrupted GET body, the lock-acquire result), the D-02 query output, the full-suite count, the final machine state (service running, port 8000), and a cross-reference to the 03-03 manual click-through scenario as the remaining human gate item.
    4. Close with the user: present the five success criteria with the live evidence for each and let the user walk the GUI click-through from 03-03 (held-lock warning / free-lock refresh) against the now-running service if they want the live demo — the API-side 409 for a GUI-held lock is the mirror of the suite's cross-process test.
  </action>
  <verify>
    <automated>python -m pytest -q</automated>
    <fails_when>non-zero exit, or any failed/error above 0, or no tests ran (phase-gate full suite)</fails_when>
    <automated>powershell -Command "Get-ScheduledTask | Where-Object { $_.TaskName -match 'pipeline|流水线|主升浪' } | Select-Object -ExpandProperty TaskName"</automated>
    <fails_when>non-empty output (a stale scheduled task exists — D-02 is no longer verify-only; halt and report per the prohibition)</fails_when>
    <automated>git status --porcelain</automated>
    <fails_when>output lists tracked files beyond the intended wave 1-2 set (unexpected modifications must be investigated, not closed over)</fails_when>
    <human-check>The user reviews the five success criteria against the live evidence in 03-04-SUMMARY.md and (optionally) runs the 03-03 click-through against the running service — held-lock click warns without running, free-lock click refreshes. This is the end-of-phase human gate; the executor records the outcome in the SUMMARY.</human-check>
  </verify>
  <acceptance_criteria>
    - The scheduled-task query returns nothing (stale 15:30 task confirmed absent); auto_start.bat / install_scheduled_task.ps1 untouched
    - python -m pytest -q exits 0; git status --porcelain -- data/ logs/ is empty; tracked-file status matches the intended wave 1-2 set exactly
    - 03-04-SUMMARY.md exists with the restart record, smoke PASS lines, SC2/SC3/SC5 evidence, D-02 output, suite count, final machine state, and the 03-03 human-gate cross-reference; the service is left running; the user walkthrough outcome (if run) is recorded
    - The Phase 3 write-side deferral is verifiable at close-out: git diff --name-only across this phase's commits (and the intended wave 1-2 tracked-file review in step 2) lists no scripts/daily/trading_journal.py path — save_portfolio/save_journal stay untouched, deferred per D-04/D-05 to Phase 4 and bound there per D-06 as the mandatory prerequisite before the Phase 4 STA-02 portfolio-read go-live
  </acceptance_criteria>
  <done>The phase closes with machine evidence: D-02 re-confirmed verify-only, the full suite green, the repo clean, the SUMMARY complete, and the user's five success criteria demonstrated on the live service.</done>
</task>

</tasks>

## Artifacts this phase produces (symbols)

- `.planning/phases/03-trigger-runner-job-registry-locks-auth-enforcement/03-04-SUMMARY.md` (new) — live evidence record (content per Task 3)
- No production code files are created or modified by this plan — it is the phase's real-machine evidence gate
- App-produced (read-only to this plan): the interrupted `logs/api/jobs/{job_id}.json` + partial `{job_id}.log` from the SC5 kill — live proof artifacts; a fresh health-check succeeded record from the recovery step

## Source Audit (multi-source coverage)

```
SOURCE    | ID      | Feature/Requirement                              | Plan  | Status
--------- | ------- | ------------------------------------------------ | ----- | ------
GOAL      | SC1     | Live 202 + pollable job + log path on real uvicorn | 03-04 | COVERED (task 1)
GOAL      | SC2     | Live 409 + running job_id (real pipeline)         | 03-04 | COVERED (task 2)
GOAL      | SC3     | Live /health p95 < 50 ms during a real run        | 03-04 | COVERED (task 2)
GOAL      | SC4     | Live 401/403 no-spawn; key absent from real logs  | 03-04 | COVERED (task 1)
GOAL      | SC5     | Kill/restart → interrupted, queryable, no loss    | 03-04 | COVERED (task 2)
REQ       | ACT-01  | Real health-check + pipeline kinds spawn and run   | 03-04 | COVERED (tasks 1-2)
REQ       | ACT-02  | Durable registry through real crash (reload at boot) | 03-04 | COVERED (task 2)
REQ       | ACT-03  | Single-flight + OS auto-release after real crash   | 03-04 | COVERED (task 2)
REQ       | SEC-01  | Live auth enforcement + key hygiene on real logs   | 03-04 | COVERED (task 1)
CONTEXT   | D-02    | 15:30 scheduled task verify-only (V5 evidence)     | 03-04 | COVERED (task 3)
CONTEXT   | discretion | Service restart onto new code; final machine state (left running) | 03-04 | COVERED (tasks 1-3)
```

Explicit exclusions (not gaps): GUI click-through automation (human gate, 03-03 Task 2 records the scenario; this plan's task 3 offers the live walkthrough); ACT-04/ACT-05, D-04/D-05/D-06, D-12, OPS-03 as deferred per CONTEXT.

## Assumption-delta decision

The assumption-delta scan was pre-run by the orchestrator: `{"detected": false}` → the checkpoint does not fire. **Decision**: `no-change` (this plan adds no new nouns; it executes evidence collection over the phase's existing ones).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executor shell → live service | Verification commands hit the real resident API on 127.0.0.1:8000; the API key exists only inside python processes |
| Executor shell → process tree | taskkill /F /T targets the port-8000 owner pid — the API and its pipeline child |
| Live service → git scope | Registry/lock/log files produced by the live runs must never enter tracked trees |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-03-17 | Information Disclosure | API key in shell/argv/output | high | mitigate | The key is read and used only inside python via api.boot.read_token (env-first semantics); it never appears in a shell command, curl -H, a repo file, or printed output; smoke scripts live in the OS temp dir and are deleted after the run |
| T-03-18 | Denial of Service | Live run colliding with the user's market flow | high | mitigate | The time rule runs before every block (date +"%H:%M"); real pipeline triggers are gated to outside weekday 09:15-15:00 or explicit user approval (precondition on task 2); the smoke's health-check kind is read-mostly and safe anytime |
| T-03-19 | Denial of Service | Non-tree kill leaving an orphan that double-runs after restart | high | mitigate | taskkill /F /T on the port-8000 owner pid (Pitfall 3); the child's death is verified via the probe-V3 tasklist raw-byte check before restart; the post-restart GET must show interrupted and the post-crash pipeline acquire must succeed — both asserted in the SC5 script |
| T-03-20 | Tampering | Registry/lock/log files entering git or sync_cloud scope | medium | mitigate | git status --porcelain -- data/ logs/ asserted empty after every task; logs/ and data/locks/ are gitignored (03-01 added the locks line); tracked-file status reviewed at close-out; nothing from the live runs is ever staged |
| T-03-21 | Tampering | D-02 evidence drift (task reappears) | low | mitigate | Verify-only re-query at close-out with a hard halt if the stale task exists — escalation to an elevated unregister would need a plan delta (prohibition) |

ASVS level 1, blocking threshold high: high rows map to task-level asserts (token-in-python-only, time-window precondition, tree-kill + verification, hygiene gates). Zero package installs — package-legitimacy gate not triggered.
</threat_model>

<verification>
- Task 1: python /tmp/gsd_p3_live_smoke.py exits 0 with all PASS lines; /health returns 200; git hygiene clean.
- Task 2: python /tmp/gsd_p3_live_sc5.py exits 0 with all PASS lines (409 body, p95 < 50 ms, interrupted after restart, lock re-acquirable, recovery trigger succeeded); git hygiene clean.
- Task 3: python -m pytest -q green; scheduled-task query empty; tracked-file status matches the intended set; SUMMARY written; service left running.
- Full phase: all five success criteria demonstrated live; D-02 verify-only re-confirmed; user walkthrough (optional) recorded.
</verification>

<success_criteria>
- SC1-SC5 each carry live-machine evidence in the SUMMARY: real 202 → polled succeeded with log path; real 409 with running job_id; /health p95 under 50 ms during a real run; live 401/403 with no spawn and key bytes absent from real logs; tree-kill → restart → interrupted queryable with the lock auto-released.
- D-02 verify-only holds (task absent; dead installer files untouched); no elevated command was needed.
- The repo is clean (only intended wave 1-2 tracked changes), the full suite is green, and the resident service is left running on the new code for the user's next consumption.
</success_criteria>

<output>
This plan's own deliverable is .planning/phases/03-trigger-runner-job-registry-locks-auth-enforcement/03-04-SUMMARY.md (written in Task 3) — the live-evidence close-out record; no code SUMMARY is produced since no code changed here.
</output>
