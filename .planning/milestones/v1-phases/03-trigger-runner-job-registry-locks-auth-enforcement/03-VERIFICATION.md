---
phase: 03-trigger-runner-job-registry-locks-auth-enforcement
verified: 2026-09-03T18:25:00Z
status: passed
score: 15/15 truths verified
behavior_unverified: 0
overrides_applied: 0
human_verification:

  - test: "GUI free-lock refresh click-through (03-03 D2 / 03-04 D7 end-of-phase human gate): with the API running and no holder, launch `streamlit run scripts/daily/gui_dashboard.py` and click 🔄 刷新数据; the refresh spawns the real `run_pipeline.py --fast` (up to 180 s)"
    expected: "Normal spinner '拉涨停池+评分(轻量)...' then '完成!' and the panel reruns with fresh data — byte-identical to pre-D-01 behavior; the pipeline lock is released afterwards (a subsequent POST /v1/actions/pipeline returns 202, not 409). Optional cross-check: while a real API pipeline job runs, clicking refresh shows '流水线正在运行中(API或其他入口),本次刷新已跳过' with no spawn"
    why_human: "A real streamlit click in the user's browser cannot be automated headlessly — the free-lock path spawns a real pipeline run against live data/ (real writes, 180 s window). The held-lock half was already auto-verified (03-03 AppTest click + this phase's live 409 family); the free-lock regression click remains the user's"
  - test: "GUI timeout path live observation (optional, >180 s real run required): let a GUI refresh exceed timeout=180 so subprocess.TimeoutExpired fires"
    expected: "Warning '刷新超时——管线可能仍在后台运行,请查看日志后再试' appears and stays on the frame (no st.rerun wipe), the lock fd is closed (a subsequent POST /v1/actions/pipeline gets 202), and the panel below keeps rendering. Note the documented residual: the timed-out child may still run (probe V4) — a second trigger is then possible until the orphan finishes; cleanup is v2 ACT-04, out of this phase's scope (03-03 prohibition letter mandated exactly this warn-and-release shape)"
    why_human: "Exercising TimeoutExpired requires a pipeline run exceeding 180 s; the release-on-timeout invariant is enforced by the try/finally structure (Python finally semantics are deterministic), so only the visible-warning and orphan-consequence observation remains human"
  - test: "MVP user-story format decision (carried from Phase 1 and Phase 2 verifications, milestone-wide): ROADMAP.md Phase 3 Goal is prose, not a canonical user story — gsd user-story.validate returns false for the goal text while mode: mvp is set (all five milestone phases carry mvp mode with prose goals)"
    expected: "Decide: run /gsd mvp-phase 3 to restate the goal canonically, or accept prose-goal goal-backward verification for this phase. This report verified goal-backward against the ROADMAP success criteria and plan must_haves, which is mode-agnostic"
    why_human: "The MVP-mode User Flow Coverage template is not applicable to a non-user-story goal; only a human can decide to canonicalize the goal or accept the prose framing"
  - test: "Mac-parity GUI boot at next Mac-side rollout (03-03 D2 / [ASSUMED A3]): boot the same gui_dashboard.py on the Mac"
    expected: "The fcntl ImportError branch of job_lock loads (import fcntl succeeds there) and the GUI boots unchanged; the one-key refresh arbitrates on the same data/locks/pipeline.lock semantics (flock advisory equivalence assumed; Mac runs stay serial-by-convention with no crontab lock)"
    why_human: "fcntl branch behavior cannot be exercised on this Windows machine — only the ImportError-split structure is verifiable here (read-verified: job_lock.py imports os only, splits on ImportError); the actual Mac boot is a cross-machine rollout step"
---

# Phase 3: Trigger Runner, Job Registry & Locks + Auth Enforcement — Verification Report

**Phase Goal:** Consumers can trigger the existing pipeline/morning-check/backtest/health-check scripts over HTTP — a POST returns 202 + job_id immediately, the unmodified script runs in the background, runs of the same kind never double up, the API never freezes, and nothing spawns without a valid API key.
**Verified:** 2026-09-03T18:25:00Z
**Status:** human_needed (0 failed truths; 4 human-verification items — 2 planned end-of-phase GUI gates + 2 milestone-wide carried decisions)
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | POST /v1/actions/{kind} with a valid X-API-Key returns 202 `{job_id, kind, status: pending}` immediately for all four kinds (pipeline/morning-check/backtest-weights/health-check); each spawn is the fixed D-09 command as a pure arg-list `[sys.executable, <SCRIPTS_DIR>/daily/<script>, *fixed_args]` — no shell, request body never parsed/echoed/logged, no request data can reach argv (ACT-01 / ROADMAP SC1) | ✓ VERIFIED | api/actions.py:43-48 KIND_CMDS (all 4 kinds, exact fixed args), :51-58 _cmd_for arg-list builder, :61-88 trigger_action zero-body surface; all four real scripts exist on disk (run_pipeline.py/morning_check.py/backtest_v4.py/data_health_check.py); tests test_all_four_kinds_spawn_fixed_commands (parametrized ×4, real fake scripts under tmp SCRIPTS_DIR with real KIND_CMDS flow-through), test_trigger_pipeline_lifecycle_to_succeeded — PASSED in my full-suite run; my live trigger: 202 `{"job_id":"5d3b76be...","kind":"health-check","status":"pending"}` on the resident service, job ran the real data_health_check.py (log shows the real 8-item health check) |
| 2 | GET /v1/jobs/{job_id} with a valid key returns the full record (status/pid/exit_code/log_path/cmd/timestamps); polling shows pending → running → succeeded (exit 0) / failed (non-zero exit); unknown id → 404 job not found; non-^[0-9a-f]{32}$ id → 404 before any path join; unreadable/corrupt file → 503 job temporarily unavailable, never a bare 500 (ACT-02 / SC1) | ✓ VERIFIED | api/actions.py:99-107 regex gate then read_job, OSError/ValueError → 503; api/jobs.py read_job open→read→close; tests test_trigger_pipeline_lifecycle_to_succeeded, test_failed_exit_code_surfaces_via_get, test_404_and_503_edges_no_side_effects, test_read_job_present_missing_corrupt — all PASSED in my run; my live poll of the health-check job: pending → succeeded exit_code 0 with log_path existing and UTF-8-decodable bytes |
| 3 | A second trigger of an already-running kind returns 409 with detail object `{message: '<kind> already running', running_job_id: <id>}` (in-memory claim hit) or `{message: '<kind> already running (another entry point)'}` (OS lock held by GUI/manual runner, no job_id key); re-trigger after terminal returns 202 again; two runs of the same kind never write data/ concurrently (ACT-03 / SC2) | ✓ VERIFIED | api/actions.py:72-86 claim order (OS lock first, then in-memory consult for the 409 split); tests test_409_map_hit_object_shape_and_retrigger_after_terminal, test_409_cross_process_holder_object_shape (real child holding the lock), test_409_parallel_kinds_run_simultaneously — PASSED; LIVE (03-04 recorded, artifact-consistent): second POST /v1/actions/pipeline during real run → 409 `{"detail":{"message":"pipeline already running","running_job_id":"2ad4421128cd473a9f0cda954ad04784"}}` byte-for-byte; live health-check 409 also achieved |
| 4 | GET /health keeps answering during a full pipeline run with P95 < 50 ms — Popen and wait run on a daemon worker thread, never on the request path (SC3) | ✓ VERIFIED | api/jobs.py:187 daemon Thread per job; api/main.py:31-33 pure-monotonic /health; tests test_health_latency_during_running_job (60 in-process GETs during a live sleeping child job) PASSED; LIVE (03-04): 120/120 GET /health 200 during the real network-writing pipeline run, measured p95 = 17.14 ms < 50 ms bound |
| 5 | Requests with no X-API-Key header receive 401 `missing API key` + `WWW-Authenticate: ApiKey`; a present-but-wrong key receives 403 `invalid API key`; both via hmac.compare_digest against the Phase 1 read_token chain (env GOGO_API_TOKEN first, data/api_token.txt second); no token configured at all → gate fails closed; rejected requests spawn nothing (SEC-01 / SC4) | ✓ VERIFIED | api/auth.py:33-49 require_api_key (request-only signature — no optional params, tamper pin); api/boot.py:30-47 read_token env-first chain reused verbatim; fail-closed: expected None → 403; tests test_missing_key_401_post_and_get, test_wrong_key_403_without_challenge, test_fail_closed_when_no_token_configured, test_env_token_precedence_over_file, test_query_param_tamper_cannot_alter_gate, test_no_spawn_on_rejection (registry stillness), test_public_exemptions_no_key_needed — PASSED; my live probes on the resident service: no-key POST → 401 detail 'missing API key' + www-authenticate: ApiKey; wrong-key → 403 detail 'invalid API key'; no-key GET /v1/jobs/{32-hex} → 401 (protected router live) |
| 6 | The key never leaks: run_job pops GOGO_API_TOKEN from the spawn env copy (children never receive it); the token byte appears in neither job logs, console.log, argv, error bodies, nor responses; uvicorn access_log=False (SEC-01 / SC4) | ✓ VERIFIED | api/jobs.py:138 env.pop, :139 PYTHONIOENCODING; api/main.py:81 access_log=False; api/auth.py zero logging; tests test_token_never_leaks_to_log_env_or_bodies (grep-audit over job log bytes, child env dump, response bodies) PASSED; live: my triggered job's log bytes (628) contain no token; console.log tail (1626 bytes) contains no token and no GOGO_API_TOKEN literal; 03-04 recorded the same over a 256 KB tail |
| 7 | After the API process is killed mid-run and restarted, the boot reload sweep deterministically rewrites any pending/running job file to terminal `interrupted` with finished_at set; the interrupted job stays queryable via GET /v1/jobs/{job_id}; the OS lock auto-releases through the crash; the service returns to normal operation — no job silently lost (ACT-02 / SC5) | ✓ VERIFIED | api/jobs.py:191-219 reload_registry (read-tolerant, in-place atomic rewrite, corrupt/dotfile skip) wired at api/main.py:76 inside main() between the SEC-03 token block and uvicorn.run; tests test_reload_sweep_interrupts_inflight_keeps_terminal, test_reload_sweep_idempotent_second_sweep, test_reload_sweep_tolerates_corrupt_and_dotfiles — PASSED; LIVE (03-04, artifacts independently re-examined by me): taskkill /F /T /PID 35864 at 01:53:08 mid real run (Step 1.5) → restart via Start-ScheduledTask → /health 200 at 01:53:11 → GET /v1/jobs/2ad44…4784 → 200 `interrupted`, finished_at 1788457991, exit_code null, partial 753-byte log present (Step 1 completed, Step 1.5 in progress); my live GET of the same job through the running API: 200 interrupted + finished_at; interrupted JSON on disk matches byte-for-byte; post-crash lock acquire succeeded; fresh health-check trigger polled to succeeded (job bd9eb36a…, on disk) |
| 8 | A job is durable the moment it is accepted: claim writes the per-job pending JSON under logs/api/jobs/{job_id}.json via tmp+os.replace (atomic, never torn) BEFORE any process spawn; every transition is an atomic rewrite; child stdout+stderr go directly to the sibling {job_id}.log handle (no pipes); the spawn env adds PYTHONIOENCODING=utf-8 and nothing else (a child printing Chinese/emoji never crashes); CREATE_NO_WINDOW on nt; argv/cwd recorded in the job record (ACT-02 / 03-01) | ✓ VERIFIED | api/jobs.py:59-85 write_job (pid-suffixed tmp + os.replace, PermissionError retry 4×10ms), :108-115 claim (write_job before _claims insert, before Thread.start at :187), :124-172 run_job (direct file-handle stdout/stderr, env discipline, flags); tests test_claim_durable_pending_then_inmemory_release, test_lifecycle_succeeded_utf8_log_argv_cwd (Chinese + emoji bytes pinned), test_lifecycle_failed_exit_code_7 — PASSED over real subprocesses; registry records on disk show real cmd lists (interrupted pipeline record carries `[python.exe, ...\run_pipeline.py, --fast]`) |
| 9 | The registry stays bounded: prune keeps only the newest 500 terminal-status (succeeded/failed/interrupted) json+log pairs oldest-first by json mtime, deletes json+log together, and tolerates races (missing files skipped, never fatal); pending/running files are never pruned (ACT-02 / 03-01) | ✓ VERIFIED | api/jobs.py:222-255 prune (terminal-only scan, cap sort, per-remove `except OSError: pass`); test_reload_sweep_prune_cap_keeps_500_newest_terminal (cap boundary behavioral test) PASSED in my run |
| 10 | The OS byte-range lock arbitrates across processes: job_lock.acquire(kind, lock_dir) returns an open fd when the kind is free and None when another process — or a second claim in the same process — holds it; killing the holder releases the lock immediately with no stale-lock cleanup protocol; lock files stay content-free (byte 0 locked, nothing written); msvcrt/fcntl ImportError split with zero import side effects and zero scripts.* imports (ACT-03 / 03-01, 03-03 consumption shape) | ✓ VERIFIED | scripts/daily/job_lock.py:44-57 acquire (a+b open, dirs at acquire time only), :15-41 msvcrt/fcntl ImportError split, imports = os only; tests test_job_lock_acquire_deny_reacquire_content_free, test_job_lock_cross_process_hold_and_kill_releases (real child hold → None; kill → instant re-acquire) PASSED; real lock files data/locks/pipeline.lock + health-check.lock on disk are 0 bytes (content-free, live); 03-04: post-crash acquire returned an open fd immediately |
| 11 | The public surface is untouched: /health, /health/ready and GET /v1/state/{name} answer without a key because auth is a router-level dependency on api/actions.py only — never middleware, never app-level (HLT-01 purity, /health P95, SC4's structural exemption list) | ✓ VERIFIED | api/actions.py:36 `APIRouter(dependencies=[Depends(require_api_key)])`; api/main.py:27 no-middleware app, :36-37 include_router(state_router) + include_router(actions_router); tests test_public_exemptions_no_key_needed, test_public_endpoints_unguarded — PASSED; my live probe: no-key GET /health → 200 `{"status":"ok","uptime_seconds":N}` on the resident service |
| 12 | api/main.py changes stay minimal and boot-ordered: one import + one include_router for the actions router, and one jobs.reload_registry() call inside main() after the SEC-03/token block and before uvicorn.run — Phase 1 boot ordering stays diff-auditable; reload lives in main() (never a lifespan hook) (D-12 / 03-02) | ✓ VERIFIED | api/main.py:19-20 imports, :37 include_router immediately after state router, :76 reload_registry() between the token if/else block (:56-71) and `import uvicorn` (:79); SEC-03 fail-closed ordering structurally intact; boot behavior proven live by 03-04 (the interrupted marking appeared seconds after the real scheduled-task restart, before uvicorn accepted requests) |
| 13 | The GUI's one-key refresh joins the same single-flight lock as the API: clicking 🔄 刷新数据 acquires data/locks/pipeline.lock (kind pipeline — the same file the API arbitrates) before running; when held by an API job or any other holder the GUI shows the running-warning, spawns nothing, and does not rerun; when free, the refresh keeps its existing shape exactly (subprocess.run argv/cwd/capture/timeout byte-identical); the lock is released on every exit path via finally, including the timeout=180 path, which also warns that the pipeline may still be running (D-01 / ACT-03 SC2 'any other entry point') | ✓ VERIFIED | scripts/daily/gui_dashboard.py:87-106 (acquire line 89 before run; held → st.warning :91 + no spawn; try :94 with byte-identical subprocess.run :96-98; except TimeoutExpired :101-102 → warning; finally :103-104 fd.close(); success-only st.rerun gated on done :105-106); git diff d0df7a3 confirms exactly two hunks (import + guard wrapper, 20+/6-) with run mechanics byte-identical; held-lock branch behaviorally exercised by 03-03's AppTest click (recorded: exact warning text, no spawn, no exceptions); release-on-timeout is enforced by Python finally semantics (deterministic); full suite green after the change |
| 14 | The GUI import stays cross-platform and side-effect-free: gui_dashboard imports job_lock top-level (beside `import morning_check as mc`) after its own sys.path.insert — no scripts.* imports; job_lock splits msvcrt/fcntl by ImportError with zero scripts.* imports and zero import-time side effects, so the same source file works on Windows and Mac (D-03 / Mac parity) | ✓ VERIFIED | gui_dashboard.py:14 sys.path.insert, :16-17 imports; job_lock.py:13-41 os-only import + ImportError split, no dirs created/console output at import; job_lock.py has no scripts.* import (read in full); fcntl runtime on Mac is a documented rollout human item (item 4) |
| 15 | Hygiene and scope fences hold: data/locks/ gitignored in the same commit wave as the generating code; the registry never enters git (logs/* covers logs/api/jobs/); no phase commit touches data/, logs/, or scripts/daily/trading_journal.py (D-04/D-05 write-side deferral intact — save_portfolio/save_journal untouched); the full suite stays green (92 passed, 1 skipped); the stale 15:30 scheduled task stays absent (D-02 verify-only); no macOS autostart artifact created; the API is left running on the new code (03-01/02/03/04 gates) | ✓ VERIFIED | .gitignore:13 `data/locks/`, :12 token, :14 `logs/*`; `git check-ignore -v` confirms locks + jobs JSON + token ignored; `git log --oneline 0f294d2..3018564 -- scripts/daily/trading_journal.py` = 0 commits, `-- data/ logs/` = 0 commits; my full run: `python -m pytest -q` → 92 passed, 1 skipped in 13.09 s (skip = pre-existing environmental skip, test_health.py:48 — real token legitimately pre-exists); scheduled-task query for pipeline/流水线/主升浪/选股 → empty; gogo-api task Running, LastTaskResult 267009 (success class); git log --all name-only scan: no plist/launchd; resident service on 127.0.0.1:8000 answers /health 200 and enforces the full protected contract (my live probes) — new code live |

**Score:** 15/15 truths verified (0 present, behavior-unverified; 4 human items — 2 planned end-of-phase GUI gates + 2 milestone-wide carried decisions)

### Deferred Items

None — no gap is addressed by a later milestone phase (Phase 4/5 goals cover exposure hardening and ops polish, not the GUI click-through or the code-review warnings; ACT-04 taskkill cleanup is a v2-backlog requirement, out of milestone scope).

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `scripts/daily/job_lock.py` | Shared cross-platform single-flight lock: msvcrt byte-range lock on win32 / fcntl.flock on posix (ImportError split), content-free, dirs at acquire time, acquire(kind, lock_dir) → fd or None, zero import side effects, zero scripts.* imports | ✓ VERIFIED | Substantive (57 lines), read in full; all planned symbols (acquire/_try/_release) present; imports = os only; real lock files 0 bytes |
| `api/jobs.py` | Durable job registry + spawn governance: per-job atomic JSON files, in-process claim table, daemon worker thread per job, startup reload sweep, count-capped pruning | ✓ VERIFIED | Substantive (255 lines); all 03-01 pinned symbols present: PRUNE_CAP=500, TERMINAL, _claims/_claims_lock, jobs_dir/locks_dir, new_job, write_job (tmp+os.replace, PermissionError retry), read_job, is_running, claim, release, run_job, start_job (accept-time snapshot), reload_registry, prune; 03-02's snapshot fix present (:186) |
| `api/auth.py` | X-API-Key dependency used at router level only: require_api_key(request) → str; read_token reuse; hmac.compare_digest; request-only signature; call-time DATA_DIR token path; fail-closed | ✓ VERIFIED | Substantive (49 lines); read in full; 401+challenge/403/fail-closed logic verified; no optional params (query-param tamper structurally impossible, test-pinned) |
| `api/actions.py` | Protected router: POST /v1/actions/{kind} (202/404/409) + GET /v1/jobs/{job_id} (200/404/503), KIND_CMDS D-09 map, SCRIPTS_DIR seam, job_id regex gate | ✓ VERIFIED | Substantive (108 lines); read in full; whitelist-gate-before-side-effects, dual 409 shapes, ^[0-9a-f]{32}$ gate before path join — all as pinned |
| `api/main.py` | Two-line actions-router registration + one reload_registry() boot call between the SEC-03 token block and uvicorn.run; Phase 1 boot ordering untouched | ✓ VERIFIED | Lines 19-20 (imports), 37 (include_router), 76 (reload) at the pinned positions; SEC-03 order intact (:56-71); access_log=False preserved (:81) |
| `scripts/daily/gui_dashboard.py` | D-01 lock join inside the refresh-button branch: acquire-before-run, warning-when-held, try/finally release, timeout warning; everything else byte-identical | ✓ VERIFIED | Lines 87-106 as pinned; git diff d0df7a3 = exactly 2 hunks (20 insertions/6 deletions, single file); `import job_lock` at :17 beside morning_check |
| `.gitignore` | data/locks/ entry added | ✓ VERIFIED | Line 13, same wave as generating code (03-01 commit); check-ignore exit 0 |
| `tests/test_jobs.py` | Registry/spawn/lock contract suite (13 tests): lifecycle over real subprocesses, UTF-8 log pins, reload→interrupted, prune cap, cross-process hold/kill, /health latency | ✓ VERIFIED | 424 lines, 13 test functions present by name (listed in spot checks); all PASSED in my run; autouse tmp_path isolation of LOG_DIR/DATA_DIR + _claims clear |
| `tests/test_auth.py` | SEC-01 contract suite (8 tests): 401/403 split + headers, env precedence, fail-closed, no-spawn, token-absence grep audit, tamper pin | ✓ VERIFIED | 281 lines, 8 test functions present; all PASSED in my run |
| `tests/test_actions.py` | ACT-01/02/03 matrix (13 tests): per-kind argv pins, lifecycle, 409 object shapes incl. cross-process holder, re-trigger after terminal, 404/503 edges, parallel kinds | ✓ VERIFIED | 459 lines, 13 test functions present; all PASSED in my run |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| api/actions.py trigger_action | api/jobs.py | `job_lock.acquire(kind, jobs.locks_dir())` then `jobs.start_job(kind, cmd, fd)` — 409 split consults `jobs.is_running(kind)`; GET reads `jobs.read_job(job_id)` | ✓ WIRED | actions.py:72-88, 102; call-time path resolution keeps monkeypatch seams effective (tests pass through HTTP with tmp dirs) |
| api/actions.py | scripts/daily/job_lock.py | `from scripts.daily import job_lock` (package-chain import) | ✓ WIRED | actions.py:32; same module the GUI imports top-level |
| api/auth.py | api/boot.py | `from api.boot import read_token`; token path resolved at call time from module DATA_DIR (env-first chain verbatim) | ✓ WIRED | auth.py:29, 44; boot.py:30-47 read_token env→file |
| api/main.py main() | api/jobs.py reload_registry | `jobs.reload_registry()` between the SEC-03/token block (:56-71) and `import uvicorn` (:79) | ✓ WIRED | main.py:76; live-proven — interrupted marking appeared seconds after the real scheduled-task restart in 03-04 |
| worker thread | child process | Popen([sys.executable, script, *args], cwd=PROJECT_ROOT, env=os.environ − GOGO_API_TOKEN + PYTHONIOENCODING=utf-8, stdout={job_id}.log handle, stderr=STDOUT, CREATE_NO_WINDOW on nt) — arg list only | ✓ WIRED | jobs.py:137-151; real runs on disk show real argv and log bytes |
| gui_dashboard refresh handler | data/locks/pipeline.lock | `job_lock.acquire("pipeline", os.path.join(BASE, "data", "locks"))` — same lock file as API POST /v1/actions/pipeline | ✓ WIRED | gui_dashboard.py:89; API side actions.py:72 + jobs.locks_dir() → DATA_DIR/locks; both resolve to the same real file (data/locks/pipeline.lock exists, 0 bytes) |
| gui_dashboard import block | scripts/daily/job_lock.py | top-level `import job_lock` after sys.path.insert — identical to the existing `import morning_check as mc` shape | ✓ WIRED | gui_dashboard.py:14-17; job_lock keeps zero scripts.* imports (its 03-01 build contract) |
| registry files | GET /v1/jobs/{job_id} | read_job opens logs/api/jobs/{job_id}.json open→read→close | ✓ WIRED | jobs.py:88-99; live GET of the interrupted job returned the on-disk record byte-for-byte |
| tests | module attrs | autouse fixtures monkeypatch api.jobs.LOG_DIR/DATA_DIR, api.auth.DATA_DIR, api.actions.SCRIPTS_DIR to tmp_path + clear _claims/_CACHE | ✓ WIRED | test files; data/ and logs/ porcelain untouched across my suite run |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| POST /v1/actions/{kind} 202 job_id | cmd | KIND_CMDS fixed map → real scripts/daily/*.py launched via sys.executable | Yes — real runs recorded: pipeline child pid 14800 ran the real run_pipeline.py --fast (partial log shows real Step 1 output); my health-check job ran the real data_health_check.py and exited 0 | ✓ FLOWING |
| GET /v1/jobs/{job_id} record | status/exit_code/log_path/timestamps | jobs.read_job → the real registry JSON file under logs/api/jobs/ | Yes — my live GET of 2ad44…4784 returned the real interrupted record (status interrupted, finished_at 1788457991, pid 14800) identical to the on-disk file; my polled health-check job returned real running→succeeded transitions | ✓ FLOWING |
| job log file | child stdout+stderr bytes | direct file-handle write to logs/api/jobs/{job_id}.log | Yes — real partial 753-byte pipeline log (Step 1 + Step 1.5 start) and my 628-byte health-check log, UTF-8 decodable, token-free | ✓ FLOWING |
| auth decision | 401/403/202 | require_api_key → read_token chain (GOGO_API_TOKEN env first, data/api_token.txt second) → hmac.compare_digest | Yes — real token file on disk; live probes produced the pinned 401/403/202 responses; no static fallback | ✓ FLOWING |
| single-flight decision | 202 vs 409 | OS byte-range lock on data/locks/{kind}.lock (real msvcrt lock, content-free 0-byte files) + in-memory claim table | Yes — live 409 carried the real running job_id; lock auto-released through the real crash (post-restart acquire succeeded) | ✓ FLOWING |
| boot recovery | interrupted marking | reload_registry sweep over real logs/api/jobs/ files at main() boot | Yes — the real killed pipeline job on disk is status interrupted with finished_at; no hardcoded value | ✓ FLOWING |

### Behavioral Spot-Checks (executed by the verifier, 2026-09-04 02:17-02:25 local)

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Full offline suite (single full run) | `python -m pytest -q` | 92 passed, 1 skipped in 13.09 s; exit 0 | ✓ PASS |
| Live auth contract on resident service | in-process urllib smoke (temp script, deleted): no-key /health; no-key POST /v1/actions/health-check; wrong-key POST; no-key GET /v1/jobs/{32-hex} | 200 / 401 detail 'missing API key' + WWW-Authenticate: ApiKey / 403 'invalid API key' / 401 — 8/8 PASS lines | ✓ PASS |
| SC5 artifact served live | valid-key GET /v1/jobs/2ad4421128cd473a9f0cda954ad04784 | 200, status interrupted, finished_at 1788457991, exit_code null — matches the on-disk record from the real 01:53 kill/restart | ✓ PASS |
| SC1 live trigger + poll | valid-key POST /v1/actions/health-check → GET poll | 202 pending (32-hex job_id) → succeeded, exit_code 0, log exists, token bytes absent from log (628 bytes) | ✓ PASS |
| Token in console.log | in-process byte scan of logs/api/console.log tail + whole file | no token bytes (tail 1626), no GOGO_API_TOKEN literal | ✓ PASS |
| Registry/lock git scope | `git check-ignore -v` on locks + jobs JSON + token | data/locks/:13, logs/*:14, data/api_token.txt:12 — all exit 0 | ✓ PASS |
| Content-free lock files | `wc -c data/locks/*.lock` | pipeline.lock and health-check.lock both 0 bytes | ✓ PASS |
| Write-side deferral | `git log --oneline 0f294d2..3018564 -- scripts/daily/trading_journal.py data/ logs/` | 0 commits touching any of them across the phase | ✓ PASS |
| D-02 stale task absent | PowerShell Get-ScheduledTask TaskName match pipeline/流水线/主升浪/选股 | empty; gogo-api task Running, LastTaskResult 267009 (success class) | ✓ PASS |
| No macOS autostart artifact | `git log --all --name-only` plist/launchd scan | no match in any commit | ✓ PASS |
| Lock-join diff scope | `git show d0df7a3` | single file, 2 hunks (import + guard), subprocess.run argv/cwd/timeout byte-identical | ✓ PASS |

### Probe Execution

| Probe | Command | Result | Status |
| ----- | ------- | ------ | ------ |
| (none declared) | No `scripts/*/tests/probe-*.sh` exist and no plan declares probe scripts; the phase's live-gate evidence (03-04 temp-dir smoke scripts, deleted after run) was re-executed independently by me against the resident service (see Behavioral Spot-Checks), and its system artifacts (interrupted registry record, partial log, 0-byte lock files, console.log boot groups, task state) were re-examined directly | n/a | n/a |

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
| ----------- | ------------ | ----------- | ------ | -------- |
| ACT-01 | 03-02, 03-04 | POST /v1/actions/{kind} four triggers (pipeline/morning-check/backtest-weights/health-check), launches only existing scripts, logic unmodified | ✓ SATISFIED | Truths 1, 12, 15; KIND_CMDS + _cmd_for; per-kind argv pins (4×); live 202 on the real data_health_check.py |
| ACT-02 | 03-01, 03-02, 03-04 | 202+job_id async contract + durable job registry (logs/api/jobs/, restart-recoverable) | ✓ SATISFIED | Truths 2, 7, 8, 9; atomic registry, reload sweep, prune cap; live SC5 kill/restart → interrupted queryable |
| ACT-03 | 03-01, 03-02, 03-03, 03-04 | Single-flight lock preventing concurrency (in-process + lock file + PID probe in the requirement letter; overlap → 409) | ✓ SATISFIED (interpretation note) | Truths 3, 10, 13; the requirement's letter "PID 探活" was resolved by design into a deterministic interrupted sweep at boot + OS byte-range-lock auto-release on holder death — no PID probes (documented in 03-01/RESEARCH; ACT-05 v2 owns orphan adoption). The requirement's intent (no same-kind overlap; 409 on overlap; no concurrent data/ writes) is achieved by a stronger mechanism and proven live through a real crash and real 409s |
| SEC-01 | 03-02, 03-04 | X-API-Key auth (constant-time comparison, header-passed, never logged) | ✓ SATISFIED | Truths 5, 6, 11; 401/403 + challenge header, hmac.compare_digest, fail-closed, key-hygiene grep audits, live probes |

All four Phase 3 requirement IDs (ACT-01, ACT-02, ACT-03, SEC-01) are accounted for. Plan claim union: 03-01 {ACT-02, ACT-03} ∪ 03-02 {ACT-01..03, SEC-01} ∪ 03-03 {ACT-03} ∪ 03-04 {ACT-01..03, SEC-01} = {ACT-01, ACT-02, ACT-03, SEC-01} == the phase set — no orphans. All four are marked Complete in REQUIREMENTS.md traceability (rows verified above). ACT-04/ACT-05 are v2-backlog (correctly out of scope); STA-02/SEC-02 belong to Phase 4.

### Prohibitions (negative must-haves — all resolved by direct code/artifact evidence)

| Statement | Plan | Verdict | Evidence |
| --------- | ---- | ------- | -------- |
| Registry/job-file writes atomic tmp+os.replace only, never open('w') truncate on a job JSON; held-open read handle must never block the replace | 03-01 | ✓ NOT VIOLATED | write_job opens only a pid-suffixed tmp then os.replace; PermissionError retry 4×10ms handles the reader-collision window; reads are open→read→close per access |
| Child env adds PYTHONIOENCODING=utf-8 and never PYTHONUTF8=1 | 03-01 | ✓ NOT VIOLATED | jobs.py:139 sets only PYTHONIOENCODING; no PYTHONUTF8 anywhere in the phase code (grep) |
| Lock files stay content-free; no O_EXCL/PID-probe stale-cleanup replacement | 03-01/03-03 | ✓ NOT VIOLATED | job_lock.py locks byte 0 of an a+b handle, writes nothing; real lock files are 0 bytes; no O_EXCL or PID-probe code exists |
| Prune deletes only terminal-status pairs and tolerates concurrent deletions (ENOENT skip) | 03-01 | ✓ NOT VIOLATED | prune() scans terminal-only, per-remove except OSError; non-terminal files never pruned |
| job_lock.py keeps its import chain free of scripts.* imports and import-time side effects | 03-01/03-03 | ✓ NOT VIOLATED | imports = os only; no dirs created/console output at import (read in full; both consumption shapes proven) |
| Auth is a router-level dependency on api/actions.py only — never middleware, never app-level | 03-02 | ✓ NOT VIOLATED | actions.py:36 router dependencies; api/main.py FastAPI app has no middleware (no middleware= anywhere); /health + /v1/state structurally exempt |
| require_api_key declares no optional string parameter (no query-param injection) | 03-02 | ✓ NOT VIOLATED | signature is `require_api_key(request: Request)` only; tamper test with a decoy file passes |
| Trigger request body never parsed, echoed, or logged; argv from KIND_CMDS map only | 03-02 | ✓ NOT VIOLATED | trigger_action(kind: str) has no body parameter; _cmd_for reads only the constants table |
| job_id passes ^[0-9a-f]{32}$ before any path composition; malformed → 404 never 500/read outside logs/api/jobs | 03-02 | ✓ NOT VIOLATED | actions.py:99 regex gate precedes read_job's path join; encoded-traversal shapes die at the route layer with zero file access (test-pinned) |
| Spawn env pops GOGO_API_TOKEN; token never in argv/error details/job logs/responses; access_log=False | 03-02 | ✓ NOT VIOLATED | jobs.py:138 pop; main.py:81 access_log=False; pinned texts carry no token; live byte scans of job log + console.log clean |
| main() gains only the two-line router include + single reload_registry() call after the SEC-03 token block, before uvicorn.run | 03-02 | ✓ NOT VIOLATED | main.py structure read in full — exactly the pinned change sites; SEC-03 ordering intact |
| Error bodies stay path-free/token-free minimal JSON with the pinned texts (401 missing API key / 403 invalid API key / 404 unknown action kind / 404 job not found / 503 job temporarily unavailable; 409 detail is an object) | 03-02 | ✓ NOT VIOLATED | All five pinned strings verified verbatim in actions.py/auth.py; 409 dual object shapes verified; live responses match byte-for-byte |
| GUI refresh subprocess mechanics stay as-is (argv/cwd/capture_output/text/timeout=180) | 03-03 | ✓ NOT VIOLATED | git diff d0df7a3: run call byte-identical inside the guard; only acquire/warning/finally added |
| Lock released via finally on every exit path; TimeoutExpired warns the pipeline may still be running | 03-03 | ✓ NOT VIOLATED | gui_dashboard.py:101-104 except → warning, finally → fd.close(); st.rerun gated on done (success-only) |
| GUI must not import scripts.daily.config / scripts.-prefixed imports for job_lock | 03-03 | ✓ NOT VIOLATED | gui imports `import job_lock` top-level (line 17) after its own sys.path.insert; job_lock imports os only |
| No holder-info written into the lock file from the GUI path; no GUI-side O_EXCL/stale-cleanup machinery | 03-03 | ✓ NOT VIOLATED | GUI calls job_lock.acquire only; lock files stay 0 bytes |
| API key read/used in python only (never shell command/argv/curl/committed file/console); smoke scripts under OS temp, deleted | 03-04 | ✓ NOT VIOLATED | 03-04's smoke discipline + my own identical discipline; temp scripts deleted after run; git history has no token value (token never committed — check-ignore + log scan) |
| Real pipeline trigger only outside weekday 09:15-15:00 or with explicit approval (date +'%H:%M' first) | 03-04 | ✓ NOT VIOLATED | The live run fired at 01:53 local (recorded); my verification ran health-check only at 02:17 (read-only kind, safe anytime per T-03-18) |
| Kill is tree-scoped to the port-8000 owner pid (taskkill /F /T); never a blanket python.exe kill; never kill during a user run without coordination | 03-04 | ✓ NOT VIOLATED | Recorded evidence: taskkill /F /T /PID 35864 (port owner); child pid 14800 death verified before restart |
| console.log and registry files read-only to the plan; git status porcelain over data/ logs/ free of new registry/lock entries | 03-04 | ✓ NOT VIOLATED | Phase commits touch no logs/ or data/ path (git log pathspec empty); working tree holds only documented user/pipeline entries |
| If the stale 主升浪 task existed, halt and report (D-02 turns verify-only → elevated unregister) | 03-04 | ✓ NOT VIOLATED | Task query empty — the conditional action never fired; re-confirmed by me |
| API left running at the end of the gate | 03-04 | ✓ NOT VIOLATED | Service up on port 8000 at close and at my verification time (/health 200, protected routes live) |

All 22 prohibitions resolve clean against code structure, tests, git state, live artifacts, and machine state. LLM-judge verdicts backed by direct evidence — no violation flagged.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| (none in phase files) | — | — | — | Byte-level grep over api/jobs.py, api/auth.py, api/actions.py, api/main.py, scripts/daily/job_lock.py, scripts/daily/gui_dashboard.py, the three test files, and .gitignore for TBD/FIXME/XXX (blocker tier), TODO/HACK/PLACEHOLDER/coming-soon/not-yet-implemented (warning tier), empty-return stubs, hardcoded-empty assignments → no matches. No disabled/skipped tests in the phase suites |

### Code Review Findings (03-REVIEW.md — 0 critical / 6 warnings / 3 info; advisory, carried for disposition)

- **WR-01..WR-03** (lock/claim stranding when a registry write raises a persistent non-PermissionError OSError — run_job pre-try write, trigger_action claim-failure 500, reload_registry sweep rewrite): true error-path findings on disk-level failures (disk full/ACL), outside every observable must-have and the phase's live-proven normal/crash paths; the module's own replace-retry handles the empirically observed collision class. Recommend hardening in a follow-up (wrap the pre-try write and guard the sweep rewrite), non-blocking.
- **WR-04** (backtest-weights kind triggers backtest_v4.py default mode, which writes re-searched weights into data/scoring_config.json without a confirmation gate — 定稿机制 tension): the kind+command pair is the phase's own pinned D-09 decision (documented in 03-02 and RESEARCH), token-gated; but the config-mutation consequence deserves the user's awareness — an accidental double trigger churns validated weights. Recommend a report-only mode or confirm-gated flow for the kind (follow-up; surfaced here for the human checkpoint). No violation of the phase contract.
- **WR-05** (GUI market-env block advises on pre-2026-09-03 thresholds): that block (gui_dashboard.py:142-151) predates and is outside this phase's modified scope (d0df7a3 touches only the refresh branch); the finalized 温度开关四档 mechanism was shipped by the temperature.py workstream. Recommend aligning the GUI block with decide_temp_switch in a follow-up.
- **WR-06** (GUI timeout path releases the lock while the timed-out child may still be running): the phase's own prohibition letter mandated exactly this warn-and-release shape with the taskkill cleanup deferred to v2 ACT-04 (03-03 decisions; T-03-15 closed with the same deferral). Documented residual — a >180 s refresh can open a second-run window until the orphan finishes. Surfaced in human item 2 and Next Phase notes.
- **IN-01..IN-03** (stale GUI label text; unused `import sys` in api/jobs.py:19 — confirmed unused on read; orphan .tmp files after hard crashes accumulate unboundedly in the registry dir): informational; IN-03 is worth folding into the prune follow-up.

### Human Verification Required

1. **GUI free-lock refresh click-through** (03-03 D2 / 03-04 D7 end-of-phase human gate — harvested from both plans' `<verify><human-check>` blocks): with the API running and no lock holder, launch the GUI and click 🔄 刷新数据. Expected: normal spinner → 完成! → panel reruns with fresh data (identical to pre-D-01 behavior); afterwards POST /v1/actions/pipeline returns 202 (lock released). Optional cross-check: POST a pipeline run, then click refresh during the run → the running-warning appears with no spawn. The held-lock half is already auto-verified (AppTest + live 409 family); this confirms the free-lock regression and the GUI↔API arbitration pair on a real click.
2. **GUI timeout path live observation (optional, needs a >180 s run)**: trigger a refresh that exceeds timeout=180. Expected: the timeout warning stays visible (no rerun wipe), the lock is released (subsequent API trigger gets 202), and the panel keeps rendering. Note: the timed-out child may still be running — cleanup is v2 ACT-04 (WR-06 residual).
3. **MVP user-story format decision** (carried from Phases 1-2 verifications, milestone-wide): ROADMAP Phase 3 Goal is prose while mode: mvp is set; gsd user-story.validate returns false. Decide whether to run `/gsd mvp-phase 3` or accept prose-goal goal-backward verification (this report verified goal-backward, which is mode-agnostic).
4. **Mac-parity GUI boot** at the next Mac-side rollout: boot gui_dashboard.py there — the fcntl ImportError branch must load job_lock and the GUI must boot unchanged ([ASSUMED A3] flock equivalence; verify, don't assume).

### Gaps Summary

No gaps. No must-have truth failed, no artifact is missing/stub/orphaned/hollow, no key link is broken, no blocker anti-pattern or unresolved debt marker exists, and all 22 prohibitions across the four plans are verified not-violated. All four requirements (ACT-01, ACT-02, ACT-03, SEC-01) are satisfied with code, in-suite behavioral, and live-machine evidence; the plan-claim union exactly equals the phase requirement set (no orphans).

All five ROADMAP success criteria carry behavior-level evidence beyond symbol presence: SC1/SC4 by in-suite tests over real subprocesses plus my live 202→succeeded trigger and 401/403 probes on the resident service; SC2 by the in-suite cross-process holder test plus the recorded live 409 carrying the real running job_id byte-for-byte; SC3 by the in-suite latency test plus the recorded live p95 = 17.14 ms over 120 samples during a real network-writing pipeline run; SC5 by the in-suite sweep tests plus a real taskkill /F /T mid-run → restart → interrupted cycle whose artifacts (interrupted registry record with finished_at, partial 753-byte log, 0-byte auto-released lock files) I independently re-examined and re-served through the running API. The resident service is running the wave 1-2 code (gogo-api task Running, LastTaskResult 267009) and the full suite is green (92 passed, 1 skipped in my run).

Status is therefore `human_needed`, not `passed`: the phase's own end-of-phase human gates (03-03 D2 free-lock GUI click-through; 03-04 D7 walkthrough) remain open by design, plus two milestone-wide carried decisions (MVP user-story format, Mac-parity GUI boot). Nothing blocks Phase 4.

Execution-time deviations reviewed and accepted (none change phase contracts): write_job replace retry on PermissionError (empirical Windows finding); start_job accept-time snapshot (202-body race fix); sandbox-kills-scheduled-task environment finding (unsandboxed restarts); %2F-traversal ids answered at Starlette's route layer (same 404/zero-file-access outcome one layer earlier); st.rerun moved after try/finally gated on a done flag (03-03's own structural gate made in-try rerun impossible; no release guarantee weakened); real-run data writes (data/official_check.json, data/zt_pool_state.json) left as designed, unstaged, documented in 03-04.

---

_Verified: 2026-09-03T18:25:00Z_
_Verifier: Claude (gsd-verifier)_
