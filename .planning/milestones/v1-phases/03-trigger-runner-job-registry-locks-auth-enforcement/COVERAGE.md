# API Coverage — Phase 3

No external API integration: exposes a local FastAPI surface and spawns existing in-repo pipeline scripts as local subprocesses — no third-party API/SDK/service; stdlib-only, zero installs.
