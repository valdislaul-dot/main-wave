---
phase: 05-recovery-observability-ops-polish
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - README.md
  - .planning/PROJECT.md
  - .planning/STATE.md
autonomous: true
requirements:
  - OPS-03
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "README.md (gitignored, local-only — NEVER committed per the 2026-08-31 upload-scope rule) carries a new known-limits deployment-note segment: the P2 interactive-session lifecycle phenomenon — an interactive console session's Ctrl+C stops the service but the AtStartup autostart survives reboot and relaunches it — recorded as a known deployment characteristic with NO code change (D-37); the segment also documents the /health/details endpoint (机密级, X-API-Key required, 401/403 behavior) and the bounded-log behavior (5MB console rotation, 20-job registry cap)"
    - "README.md carries the Mac verification checklist (D-35): git pull → python -m pytest -q (full suite) → confirm the conftest network-blocking fixture proves no test touches the network (any network-touching test fails) → expected result 157+ passed with 1 env-conditional skip (exact Win-side final counts recorded in the 05-04 SUMMARY; Mac counts expected comparable — recorded by the user when they run it as the cross-machine rollout step)"
    - ".planning/PROJECT.md's 定稿 data-classification table gains the /health/details row: 机密级, route source api/health.py, X-API-Key router-level gate (require_api_key, same dependency object as the other 机密级 routes), matching the D-29/D-31 contract and the Phase 4 table wording conventions — the table stays the single classification authority"
    - ".planning/STATE.md's [P3→P5] tracker row (15:30 scheduled task) closes with a re-confirmed audit result: Get-ScheduledTask enumeration matching pipeline/流水线/主升浪 names is EMPTY at execution time → the task is confirmed absent, D-36 records the confirmation, no disable command is needed; if the enumeration is NOT empty (drift), the finding is recorded and a disable command is handed to the user for execution — never executed by the agent without the user"
    - "No code file changes in this plan (docs/records only); the docs reflect the 05-01 as-built route contract (path/name read from 05-01-SUMMARY.md, never invented)"
  artifacts:
    - path: "README.md"
      provides: "Known-limits deployment note (D-37 Ctrl+C lifecycle + bounded-log behavior) + Mac verification checklist (D-35) + endpoint quick-reference including the auth-gated /health/details"
      contains: "deployment-note headers, checklist steps, /health/details row"
    - path: ".planning/PROJECT.md"
      provides: "The classification-table row for /health/details (机密级 — mirrors the Phase 4 table layout: endpoint, tier, route source, gate)"
      contains: "/health/details row"
    - path: ".planning/STATE.md"
      provides: "The closed [P3→P5] 15:30-task row: confirmed-absent audit result with date + enumeration evidence, D-36 record (or, on drift, the recorded finding + handed-off disable command)"
      contains: "15:30 row close-out"
  key_links:
    - from: "README.md deployment note"
      to: "D-37 + 05-01/05-02 as-built behavior"
      via: "the note must state the ACTUAL built semantics (read from 05-01-SUMMARY.md / 05-02-SUMMARY.md): /health/details path + 401/403 bytes, 5MB rotation with one .1 history, 20-job cap — a doc that drifts from the build re-introduces the P2-class confusion this phase exists to close"
    - from: ".planning/PROJECT.md row"
      to: "Phase 4 定稿 table conventions"
      via: "the row follows the existing 机密级 rows' exact column semantics (tier, source module, gate) so the table stays one consistent authority (04-06 wording)"
  prohibitions:
    - statement: "README.md MUST NOT be committed or staged — it is gitignored local-only under the 2026-08-31 upload-scope rule; the commit for this plan covers .planning/PROJECT.md and .planning/STATE.md only"
      category: safety
      requirement_id: OPS-03
      status: unverified
    - statement: "D-36's disable action MUST NOT be executed by the agent: if the 15:30 task is found in the enumeration (drift from the Phase 3 verify-only finding), the plan records the finding and hands the user a single elevation-required disable command to run themselves (D-02 discipline)"
      category: safety
      requirement_id: OPS-03
      status: unverified
    - statement: "No code changes in this plan — the docs describe as-built facts read from 05-01/05-02 SUMMARYs; documenting a route shape, threshold, or cap that differs from the build is a defect"
      category: safety
      requirement_id: OPS-03
      status: unverified
    - statement: "The D-37 Ctrl+C lifecycle note records the phenomenon as a known deployment characteristic with NO code change — no watchdog, no relaunch logic, no behavior alteration (05-CONTEXT D-37 wording)"
      category: safety
      requirement_id: OPS-03
      status: unverified
---

# Plan 05-03 — Ops documentation + legacy close-out: README known-limits/Mac checklist (D-37/D-35) + PROJECT.md classification row + D-36 15:30 scheduled-task audit record

## User-story framing

**As an** operator moving between the Windows and Mac machines, **I want to** find the deployment lifecycle facts (Ctrl+C behavior, bounded logs, the auth-gated details endpoint) and a step-for-step Mac verification checklist in README, see /health/details classified in the project's single authority table, and have the 15:30 legacy task question closed with evidence — **so that** documentation matches the built system and no stale scheduled task silently survives.

<objective>
Close the phase's doc and legacy records without touching code: D-35's Mac verification checklist and D-37's known-limits deployment note (Ctrl+C lifecycle + /health/details + bounded-log facts) land in README.md (gitignored local-only — never committed); the /health/details row lands in .planning/PROJECT.md's 定稿 classification table (route source api/health.py, as-built facts from 05-01-SUMMARY.md); and D-36's 15:30 scheduled-task audit runs live with the confirmed-absent (or drifted-with-handoff) result recorded in .planning/STATE.md's [P3→P5] row.

Purpose: SC3's cross-machine verification story and the phase's known-limits documentation (D-35/D-37) ship here, and the Phase 3 → Phase 5 legacy tracker (D-36) closes with live evidence instead of assumption.

Output: README.md segments, PROJECT.md classification row, STATE.md close-out row, 05-03-SUMMARY.md.
Decisions: D-35, D-36, D-37 (discretion exercised: checklist format mirrors the Phase 3 Mac-verification pattern already recorded in README; audit command is the same verify-only enumeration used since Phase 3)
</objective>

## Flagged Assumptions (spec-less probe fallback — OPS-03 row unresolved, surfaced explicitly)

- [ASSUMED] The 15:30 scheduled task remains absent at execution time (it was absent at planner time — 2026-09-04 full enumeration matched only gogo-api). The plan's task handles BOTH outcomes: empty enumeration → record confirmed-absent; non-empty → record finding + hand the user the disable command (never self-executed). No outcome is a dead end.
- [ASSUMED] README.md still exists with the Phase 4 known-limits segment (04-06 wrote it) — this plan appends/extends that segment; if the file is missing (local deletion), recreate the local README from the 04-06 SUMMARY's description of its structure before appending.
- [ASSUMED] The expected-suite-count wording "157+ passed, 1 env-conditional skip" (05-CONTEXT specifics) describes the phase-end Windows result — the checklist states the floor and points to the 05-04-SUMMARY.md exact count for parity comparison, since the checklist is written before the final Win count exists.

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-recovery-observability-ops-polish/05-CONTEXT.md
@.planning/phases/05-recovery-observability-ops-polish/05-PATTERNS.md
@.planning/phases/05-recovery-observability-ops-polish/05-01-SUMMARY.md
@.planning/phases/05-recovery-observability-ops-polish/05-02-SUMMARY.md
@.planning/PROJECT.md
@.planning/STATE.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>README.md: known-limits deployment note (D-37 Ctrl+C lifecycle + bounded-log facts + /health/details endpoint) + Mac verification checklist (D-35)</name>
  <files>README.md</files>
  <read_first>
    - C:/Users/Davis/Desktop/gogo/README.md (the existing known-limits segment from 04-06 — this plan extends it; content is local-only, never committed)
    - C:/Users/Davis/Desktop/gogo/.planning/phases/05-recovery-observability-ops-polish/05-01-SUMMARY.md (as-built /health/details contract: path, tier, 401/403 bytes, body shape)
    - C:/Users/Davis/Desktop/gogo/.planning/phases/05-recovery-observability-ops-polish/05-02-SUMMARY.md (as-built rotation/cap facts: 5MB → console.log.1 one generation, 20-job registry cap)
    - C:/Users/Davis/Desktop/gogo/tests/conftest.py lines 17-55 (network-blocking fixture facts the Mac checklist cites)
  </read_first>
  <action>
    Extend the README's known-limits segment with three as-built sections (facts read from the SUMMARYs, never invented):

    1. 部署生命周期注意 (D-37): the P2 phenomenon — an interactive console session's Ctrl+C stops the resident service, while the AtStartup scheduled task relaunches it after reboot; recorded as a known characteristic with NO code change (no watchdog, no relaunch logic). State it in one or two sentences plus the consequence (check service state via the scheduled task / /health after interactive sessions end).
    2. 端点参考: add the /health/details row to the endpoint quick-reference — 机密级, GET /health/details, X-API-Key required, no key → 401 missing API key + challenge, wrong key → 403 invalid API key, 200 body = {versions, uptime_seconds, last_check}; note /health and /health/ready stay public (no key).
    3. 日志有界行为: 5MB boot rotation into console.log.1 (one history) + 20-job registry cap — one short paragraph so operators never hand-clean logs.
    4. Mac 验证清单 (D-35): numbered steps — git pull the repo; python -m pytest -q (full suite, expect 157+ passed with 1 env-conditional skip — Win-side exact counts in the 05-04-SUMMARY.md, Mac counts expected comparable, record them when run); confirm the conftest network-blocking autouse fixture is active (any test touching the network fails — that IS the SC3 proof); run the endpoint smoke (GET /health; GET /health/details with the token from data/api_token.txt). Mark the checklist as the cross-machine rollout step the user executes and back-fills (Phase 3 Mac-verification pattern).
    5. Verify the file's own git state: README.md must stay untracked/ignored (git check-ignore README.md confirms) — never staged, never committed.
  </action>
  <verify>
    <automated>git check-ignore README.md</automated>
    <fails_when>non-zero exit (README.md must be gitignored — if it is tracked, STOP and report: the 2026-08-31 upload-scope rule forbids committing it and its tracked state is a repo-level problem outside this plan)</fails_when>
    <automated>grep -c "console.log.1" README.md && grep -c "Mac" README.md</automated>
    <fails_when>either count is 0 (rotation fact and Mac checklist must exist in the file — the D-37 note header must also be present, checked by the same pass)</fails_when>
  </verify>
  <acceptance_criteria>
    - README.md holds: D-37 lifecycle note (no-code-change wording), /health/details endpoint row with 401/403 bytes, bounded-log paragraph (5MB/.1/20-cap), numbered Mac checklist with suite floor + network-blocking explanation + token smoke
    - git check-ignore README.md exits 0; the plan's commit excludes README.md
  </acceptance_criteria>
  <done>The README's local-known-limits now matches the as-built system — lifecycle, endpoint, log bounds and the Mac verification path are all documented for the operator and the cross-machine rollout step.</done>
</task>

<task type="auto">
  <name>PROJECT.md 定稿 classification table: /health/details row (机密级, api/health.py) mirroring Phase 4 conventions</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - C:/Users/Davis/Desktop/gogo/.planning/PROJECT.md (the 定稿 data-classification table — locate the 机密级 rows region)
    - C:/Users/Davis/Desktop/gogo/.planning/phases/05-recovery-observability-ops-polish/05-01-SUMMARY.md (as-built route facts: api/health.py, router-level gate, dependency identity)
  </read_first>
  <action>
    Add the /health/details row to the 定稿 classification table with the same column semantics as the existing 机密级 rows (read the row layout from PROJECT.md first — match tier column, route source column, gate column exactly):

    1. Row content: GET /health/details — 机密级 — route source api/health.py — gate X-API-Key router-level (Depends(require_api_key), same dependency object as /v1/private/*). A one-line annotation cites the contract source (05-CONTEXT D-29/D-31) so the classification rationale stays traceable.
    2. Do NOT restructure the table, reword existing rows, or add any other row — the Phase 4 定稿 wording is the authority; this is an additive change matching it.
    3. Verify the row exists and the table diff is additive-only, then commit THIS task's change together with the STATE.md close-out in the final task's commit (or as its own scoped commit if the STATE.md audit lands on a different day — keep commits scoped and separate from README).
  </action>
  <verify>
    <automated>grep -n "/health/details" .planning/PROJECT.md</automated>
    <fails_when>no match (the row must exist in the table)</fails_when>
    <automated>git diff --stat .planning/PROJECT.md</automated>
    <fails_when>stat shows deletion-line counts > 0 (additive-only change discipline — rewording existing 定稿 rows is out of scope)</fails_when>
  </verify>
  <acceptance_criteria>
    - The table carries the /health/details 机密级 row with source + gate matching the as-built 05-01 contract
    - git diff on PROJECT.md is additive-only (no existing-row rewording)
  </acceptance_criteria>
  <done>The single classification authority now covers /health/details at 机密级 with the same conventions as every other secret-tier route.</done>
</task>

<task type="auto">
  <name>D-36 live audit: Get-ScheduledTask enumeration + [P3→P5] STATE.md row close-out with evidence (or drift finding + user-handoff)</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - C:/Users/Davis/Desktop/gogo/.planning/STATE.md (the [P3→P5] 15:30-task tracker row — locate its current wording)
    - C:/Users/Davis/Desktop/gogo/.planning/phases/05-recovery-observability-ops-polish/05-CONTEXT.md lines 30-38 (D-36 wording: confirm → if still registered deliver disable command for the user → else record the confirmation)
  </read_first>
  <action>
    Run the D-36 live audit and record the outcome:

    1. Run the full scheduled-task enumeration scoped to the project's task names: powershell -Command 'Get-ScheduledTask | Where-Object { $_.TaskName -match "pipeline|流水线|主升浪" } | Select-Object -ExpandProperty TaskName' — record the raw output. (The bash invocation single-quotes the WHOLE -Command argument so bash passes $_ through verbatim to PowerShell — with double quotes bash expands $_ to the previous command's last argument before PowerShell ever parses it, mangling the filter; the PS-internal string quotes around the pattern are the double ones.)
    2. Expected (confirmed at planner time 2026-09-04): EMPTY output — the 15:30 task was disabled/removed in Phase 3 and the enumeration matches only gogo-api (which does not match the filter). Record the confirmed-absent result in the [P3→P5] row of STATE.md: date of audit, the enumeration command + empty result, and the D-36 close-out line — no disable command is needed. Mark the row closed.
    3. Drift branch (non-empty output — a matching task IS registered): do NOT disable anything. Update the STATE.md row with the finding (task name, state, trigger), and record for the user a single ready-to-run elevation-required disable command (Unregister-ScheduledTask with the exact task name, or the Phase 3 disable script's pattern if one exists in the repo) — the user executes it themselves under D-02 discipline; note the handoff in the SUMMARY.
    4. Close the plan: write 05-03-SUMMARY.md (README segment inventory + PROJECT.md row + D-36 evidence verbatim + drift-branch note if taken), then the scoped commit: `docs(05): classification row + D-36 audit record (OPS-03 docs)` covering .planning/PROJECT.md and .planning/STATE.md ONLY (README.md excluded by rule).
  </action>
  <verify>
    <automated>powershell -Command 'Get-ScheduledTask | Where-Object { $_.TaskName -match "pipeline|流水线|主升浪" } | Select-Object -ExpandProperty TaskName'</automated>
    <fails_when>command errors (the audit itself must run cleanly — its OUTPUT is the evidence recorded either way)</fails_when>
    <automated>git diff -- .planning/STATE.md</automated>
    <fails_when>diff lacks the audit date + result line in the [P3→P5] row (the close-out record must be in the row, not just the SUMMARY)</fails_when>
  </verify>
  <acceptance_criteria>
    - The enumeration ran and its output is recorded verbatim in STATE.md's [P3→P5] row + the SUMMARY
    - Confirmed-absent → row closed with the date; drift → finding recorded + disable command handed to the user (never executed by the agent)
    - Scoped commit contains .planning/PROJECT.md + .planning/STATE.md only; README.md untracked; hygiene clean
  </acceptance_criteria>
  <done>The Phase 3 → Phase 5 legacy tracker closes with live evidence — the 15:30 task question is answered by enumeration output, not by assumption, and the answer (or the user handoff) is recorded where the tracker lives.</done>
</task>

</tasks>

## Artifacts this phase produces (symbols)

- `README.md` (modified, local-only/ignored) — deployment-lifecycle note (D-37 Ctrl+C), /health/details endpoint row, bounded-log paragraph, Mac verification checklist (D-35)
- `.planning/PROJECT.md` (modified) — /health/details 机密级 classification-table row (source api/health.py, router-level gate, D-29/D-31 citation)
- `.planning/STATE.md` (modified) — [P3→P5] 15:30-task row closed with the D-36 audit date + verbatim enumeration evidence (or the drift finding + user handoff)
- `.planning/phases/05-recovery-observability-ops-polish/05-03-SUMMARY.md` (new path, committed) — segment inventory + D-36 evidence + drift note

## Source Audit (multi-source coverage)

```
SOURCE    | ID      | Feature/Requirement                    | Plan  | Status
--------- | ------- | -------------------------------------- | ----- | ------
CONTEXT   | D-35    | Mac 验证步骤清单 (拉取→pytest→网络封锁证明), 实跑用户执行后回填 | 05-03 | COVERED (task 1; 回填经 README 清单 + 05-04 gate 交接)
CONTEXT   | D-36    | 15:30 定时任务存废确认 → 在册交付停用命令 / 不在册记录 | 05-03 | COVERED (task 3, live audit + both branches)
CONTEXT   | D-37    | Ctrl+C 已知生命周期特性 → README known-limits 部署注意, 零代码改动 | 05-03 | COVERED (task 1)
CONTEXT   | discretion | 清单存放位置/格式 | 05-03 | COVERED (README segment, Phase 3 pattern)
REQ       | OPS-03  | classification-table mirror of the new 机密级 route | 05-03 | COVERED (task 2 — PROJECT.md single authority)
```

Other source rows covered by sibling plans: /health/details contract + SC2 audit → 05-01; rotation + registry cap → 05-02; real-machine live gate (SC1 live matrix + rotation proof + SC3 final suite + human review) → 05-04. Exclusions (not gaps): v2 OPS-04/05/06, ACT-04/05, 进程守护, GUI items (CLI-only), Mac 实跑本身 (跨机 rollout, 用户执行 — the checklist IS this plan's deliverable for it).

## Assumption-delta decision

No assumption-delta payload was delivered with this plan-phase invocation. The [ASSUMED] rows above (task still absent / README exists with the 04-06 segment / checklist wording floor vs exact count) all have explicit both-branch handling inside the tasks — no outcome dead-ends.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs ↔ as-built system | README/PROJECT/STATE claims must match the 05-01/05-02 build — a doc claiming a different path/tier/threshold re-creates the operator confusion this phase closes |
| Agent ↔ Task Scheduler | The audit READS the scheduler; any disable action crosses into system mutation and is user-only (D-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-09 | Spoofing | README claiming wrong endpoint/tier facts | medium | mitigate | All facts read from the 05-01/05-02 SUMMARYs at execution time (paths, 401/403 bytes, 5MB/.1/20 values); Task 1 verify asserts the rotation fact text exists |
| T-05-10 | Elevation of Privilege | Agent disabling a registered scheduled task | high | mitigate | The drift branch NEVER executes the disable — it records the finding and hands the user a ready-to-run elevation-required command (D-02 discipline, 05-CONTEXT D-36 wording); verify checks the STATE.md record, not the scheduler state |
| T-05-11 | Information Disclosure | README pushed to the private repo exposing local ops notes | medium | mitigate | Upload-scope rule (2026-08-31): README.md stays gitignored local-only; Task 1 verify runs git check-ignore and the commit scope excludes it |
| T-05-SC | Tampering | package installs | high | mitigate | Zero package installs — package-legitimacy gate not triggered |

ASVS level 1, blocking threshold high: the high row (T-05-10) is structural — the plan contains no scheduler-mutation path at all. Package legitimacy: no install commands exist in this plan.
</threat_model>

<verification>
- Task 1: git check-ignore README.md exits 0; grep confirms the D-37/bounded-log/Mac content landed; README excluded from commits.
- Task 2: grep finds the /health/details row in PROJECT.md; git diff --stat shows additive-only change.
- Task 3: the enumeration command runs cleanly and its verbatim output is recorded in STATE.md's [P3→P5] row + SUMMARY; the scoped commit holds PROJECT.md + STATE.md only.
</verification>

<success_criteria>
- README locally documents the D-37 lifecycle characteristic (no code change), the auth-gated /health/details endpoint, the bounded-log behavior, and the D-35 Mac checklist — all as-built, all local-only.
- PROJECT.md's 定稿 table classifies /health/details 机密级 with the Phase 4 conventions; zero existing-row rewording.
- STATE.md's [P3→P5] row closes with the D-36 audit date + evidence (or the drift finding + user-handoff command).
</success_criteria>

<output>
Create .planning/phases/05-recovery-observability-ops-polish/05-03-SUMMARY.md when done — inventory the README segments added (with the exact section headers), the PROJECT.md row, and the D-36 audit output verbatim with the STATE.md close-out wording; note any drift-branch handoff for the end-of-phase human review.
</output>
