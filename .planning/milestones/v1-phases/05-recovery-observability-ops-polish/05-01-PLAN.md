---
phase: 05-recovery-observability-ops-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/health.py
  - api/main.py
  - tests/test_health_details.py
  - tests/test_auth.py
autonomous: true
requirements:
  - OPS-03
estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "GET /health/details with a valid X-API-Key returns 200 application/json with EXACTLY the D-29 top-level key set {\"versions\", \"uptime_seconds\", \"last_check\"} and last_check subkeys {\"health_job\", \"market_state_mtime\"}; versions carries {\"python\", \"uvicorn\", \"fastapi\"} read lazily from sys.version + importlib.metadata (never hardcoded — self-consistency-asserted against an in-test importlib.metadata computation so both machines pass); uptime_seconds comes from the same module-import monotonic anchor as /health (uptime_seconds() helper beside main.py _START); health_job = ISO-8601 UTC of the newest succeeded health-check job's finished_at from the jobs registry, or null; market_state_mtime = ISO of data/market_state.json os.stat mtime, or null (OPS-03 SC1, D-29/D-30)"
    - "No key → 401 {\"detail\": \"missing API key\", \"code\": \"missing_api_key\"} + WWW-Authenticate: ApiKey; wrong key → 403 {\"detail\": \"invalid API key\", \"code\": \"invalid_api_key\"} with no challenge header; a key when no token is configured anywhere → 403 fail-closed — all from the same require_api_key dependency object reused as a router-level dependency (api/auth.py), zero new raise texts and zero new CODE_BY_DETAIL rows (D-31, frozen 04-01 envelope)"
    - "The null legs are 200s, never 5xx: empty jobs dir / only failed health-check jobs / only succeeded jobs of other kinds / corrupt or unreadable registry files / listdir OSError → health_job null; market_state.json missing or unstat-able → market_state_mtime null (PATTERNS discretion (a): symmetric null keeps the ops-inspection endpoint answering while a state file is absent)"
    - "The SC2 route-by-route audit passes with \"/health/details\" added to expected_secret (tests/test_auth.py:349) and the audit proves the require_api_key dependency-object identity on the live route (route.dependencies[].dependency is require_api_key) — the suite goes red the moment a route lands without an explicit tier (PATTERNS constraint 3: same-task mandatory pin)"
    - "The public /health surface is untouched beside the new route: bare GET /health still 200 with EXACT body keys {\"status\", \"uptime_seconds\"} and non-decreasing uptime; the module docstring of test_auth.py notes /health/details is 机密级 and NOT part of the D-11 per-path exemption list"
    - "python -m pytest -q stays green with tests/test_health_details.py added; git status --porcelain -- data/ logs/ stays empty; api/errors.py has zero diff (frozen table untouched)"
  artifacts:
    - path: "api/health.py"
      provides: "The OPS-03 SC1 route module (04-PATTERNS: independent module over main.py inlining — matches the state/private/actions module convention and keeps the test seam off main.py): router = APIRouter(dependencies=[Depends(require_api_key)]) (same dependency object as api/private.py:55 — SEC-02 机密级), GET /health/details handler assembling the D-29 dict, private helpers _versions() (importlib.metadata with PackageNotFoundError → \"unknown\") and _latest_succeeded_health_check() (registry scan: kind == \"health-check\" AND status == \"succeeded\", max finished_at, OSError/ValueError-tolerant per api/jobs.py prune scan shape)"
      contains: "router, health_details endpoint, _versions, _latest_succeeded_health_check"
    - path: "api/main.py"
      provides: "def uptime_seconds() public helper immediately after the _START anchor (L32) returning int(time.monotonic() - _START); /health (L49) body switched to call it (behavior byte-unchanged — test_health.py pins survive untouched); from api.health import router as health_router import beside L20-23 and app.include_router(health_router) in the include region L52-54"
      contains: "uptime_seconds, health_router include"
    - path: "tests/test_health_details.py"
      provides: "The OPS-03 SC1 contract suite: autouse isolation fixture (api.health.DATA_DIR/LOG_DIR + api.jobs.LOG_DIR + api.auth.DATA_DIR → tmp_path subtree, write_token — test_private.py:68-101 shape), _seed_job_file registry helper extended to carry kind/finished_at (test_jobs.py:239-261 shape), frozen os.utime market_state fixture, gate matrix, exact 200 key-set pins, ISO equality pins against in-test computations, null-leg matrix, fail-closed no-token leg, public /health untouched legs"
      contains: "TestClient(app), _isolated, write_token, _seed_health_check_job, _write_market_state"
    - path: "tests/test_auth.py"
      provides: "The SC2 classification audit extended: expected_secret (L349) gains \"/health/details\" (expected_public untouched); module docstring L8-14 wording updated — /health/details is 机密级 and joins the audit set, the D-11 exemption list stays per-path enumeration with /health purity wording intact"
      contains: "expected_secret set membership"
  key_links:
    - from: "api/health.py"
      to: "api/auth.py"
      via: "require_api_key imported once and attached at router level (APIRouter(dependencies=[Depends(require_api_key)) — dependency-OBJECT identity is what the SC2 audit asserts; never middleware, never app-level (auth.py design constraint)"
    - from: "api/health.py"
      to: "api/main.py uptime anchor"
      via: "handler-level lazy import of api.main.uptime_seconds at request time (module-level import would circular-fail: main.py imports api.health at L23 before _START/uptime_seconds are defined at L32; lazy import hits sys.modules post-load — main.py:105-108 precedent)"
    - from: "api/health.py"
      to: "api/jobs.py"
      via: "jobs_dir() + read_job() reused for the last_check.health_job scan — registry files are the single source of truth; scan mirrors api/prune's OSError/ValueError tolerance (jobs.py:227-245); kind literal \"health-check\" matches api/actions.py KIND_CMDS"
    - from: "api/health.py"
      to: "scripts/daily/config.py"
      via: "DATA_DIR/LOG_DIR module attrs imported once, os.path.join at call time (monkeypatch seam, state.py:66 convention); import chain stays stdlib + fastapi + api.* + scripts.daily.config — no network capability (SC4)"
    - from: "tests/test_health_details.py"
      to: "tests/test_private.py + tests/test_jobs.py scaffolding"
      via: "module-level TestClient(app) without context manager, autouse tmp_path path-seam fixture + write_token, _seed_job_file-style direct-open registry seeding with os.utime mtime freezing — real data/ and logs/ zero-touch (test_private.py:25-27 pin)"
  prohibitions:
    - statement: "GET /health/details MUST reuse the require_api_key dependency object as a router-level dependency — never middleware/app-level, never a second token loader; the 401/403 texts and challenge-header split come from api/auth.py unchanged (D-10/D-31)"
      category: safety
      requirement_id: OPS-03
      status: unverified
    - statement: "The frozen 04-01 error envelope MUST NOT grow: /health/details introduces zero new raise texts and api/errors.py gets zero diff (its only error paths are the auth dependency's existing raises)"
      category: safety
      requirement_id: OPS-03
      status: unverified
    - statement: "The version strings MUST NOT be hardcoded: python from sys.version, uvicorn/fastapi from importlib.metadata.version (PackageNotFoundError → \"unknown\") — tests assert self-consistency against an in-test computation so machine version drift cannot false-red"
      category: safety
      requirement_id: OPS-03
      status: unverified
    - statement: "GET /health/details MUST be a pure read handler — no writes, no spawn, no network, no job registry mutation (jobs registry stays read-only through read_job); the scan tolerates missing/corrupt registry files with null, never 5xx"
      category: safety
      requirement_id: OPS-03
      status: unverified
    - statement: "The D-11 public exemption list stays a per-path enumeration: /health/details does NOT join it and /health stays pure — the SC2 audit set-equality is the drift guard that fails any route without an explicit tier"
      category: safety
      requirement_id: OPS-03
      status: unverified
---

# Plan 05-01 — Auth-gated GET /health/details (OPS-03 SC1): route module + uptime anchor refactor + contract suite + SC2 audit pin

## Execution prerequisite (orchestrator action — not a task change)

> Tracked `data/auction_state.json` carries pre-existing dirt (09-04 09:26 auction snapshot; last [auto] data commit 2026-09-03 20:58). The orchestrator commits that snapshot per the repo [auto] 数据快照同步 convention BEFORE this plan executes, so Task 1's `git status --porcelain -- data/ logs/` hygiene gate starts from a clean tree. If the gate still shows pre-existing dirt at execution time, stop and report — never fix forward.

## User-story framing

**As an** operator holding the API token, **I want to** GET /health/details and see live versions, the uptime anchored to the same process start as /health, and the last health-check + market-state freshness in ISO form — while unauthenticated callers get the pinned 401/403 — **so that** I can answer "is the service healthy and how fresh is its view" in one authenticated call, with the secret tier classification enforced by the same guard as every other 机密级 route.

<objective>
Deliver the phase's signature operator endpoint on the frozen auth/envelope foundation: `api/health.py` (router-level require_api_key gate, D-29 shape `{versions, uptime_seconds, last_check}` with lazy importlib.metadata reads, registry scan for the newest succeeded health-check job, os.stat of market_state.json, ISO-8601 output), the `uptime_seconds()` public-helper refactor in `api/main.py` (single monotonic anchor shared by /health and /health/details), the `tests/test_health_details.py` contract suite, and the mandatory SC2 audit extension in `tests/test_auth.py` (same task as the route — the audit fails any unclassified route, PATTERNS constraint 3).

Purpose: OPS-03 SC1 lands here — versions/uptime/last-check behind auth with zero new error texts (frozen 04-01 table untouched) and a provable classification pin. The plan opens with the phase tracer: one authenticated GET /health/details slice wired through every layer the endpoint touches (gate → lazy versions → registry scan → stat → ISO compose → envelope-consistent errors) before any expansion.

Output: `api/health.py`, `api/main.py` (uptime_seconds + two-line include), `tests/test_health_details.py`, `tests/test_auth.py` (SC2 set + docstring), green suite.
Decisions: D-29, D-30, D-31 (discretion exercised: independent module over inline; uptime wiring = public helper + handler-level lazy import; market_state missing → null; versions missing-dist → "unknown"; .json scan = jobs_dir + read_job)
</objective>

## Flagged Assumptions (spec-less probe fallback — OPS-03 row unresolved, surfaced explicitly)

- OPS-03 (probe `unclassified` — no scenario clause to honor): the endpoint's content contract, data sources and auth split are fully pinned by D-29..D-31 + 05-PATTERNS (field shape, lazy sys.version/importlib.metadata, /health anchor reuse, registry-scan source, require_api_key reuse, frozen-envelope constraint) — no spec gap remains; the three planner-裁定 micro-decisions are flagged below as [ASSUMED] with their fallbacks so the executor never guesses silently.
- [ASSUMED] market_state.json missing/unreadable → `market_state_mtime: null` (symmetric with health_job's null; the endpoint stays 200 because its job is ops introspection — a details probe that 503s on a missing state file would hide the very condition it should report). Fallback if rejected at review: switch to the frozen 503 `state file unavailable` row (errors.py:46) — one-line change in the handler plus one test leg.
- [ASSUMED] uptime wiring: public `uptime_seconds()` in api/main.py adjacent to `_START`, /health refactored to call it (behavior byte-unchanged, test_health.py pins survive untouched); api/health.py reaches it via handler-level lazy import (module-level import is circular by construction — main.py imports api.health before _START exists).
- [ASSUMED] "newest succeeded health-check job" = max `finished_at` among registry jsons with kind == "health-check" and status == "succeeded" (registry files are the single source of truth; scan is OSError/ValueError-tolerant per the api/jobs.py prune shape).
- [ASSUMED] ISO output is `datetime.fromtimestamp(value, timezone.utc).isoformat()`; the suite freezes mtimes/finished_at via os.utime and asserts equality against an in-test computation of the same expression (no timezone-drift-sensitive literals).

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-recovery-observability-ops-polish/05-CONTEXT.md
@.planning/phases/05-recovery-observability-ops-polish/05-PATTERNS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@api/main.py
@api/auth.py
@api/errors.py
@api/private.py
@api/jobs.py
@tests/test_private.py
@tests/test_auth.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>api/health.py + main.py uptime anchor + SC2 audit pin + tests/test_health_details.py: one authenticated GET /health/details slice end-to-end (gate → lazy versions → registry scan → market stat → ISO body)</name>
  <files>api/health.py, api/main.py, tests/test_health_details.py, tests/test_auth.py</files>
  <read_first>
    - 05-PATTERNS.md api/health.py section (module skeleton, router gate, uptime 接法 A/B, scan shape, ISO/versions notes, SC2 连带) — C:/Users/Davis/Desktop/gogo/.planning/phases/05-recovery-observability-ops-polish/05-PATTERNS.md
    - C:/Users/Davis/Desktop/gogo/api/private.py lines 32-55 (the 机密级 router skeleton to copy verbatim: imports, router = APIRouter(dependencies=[Depends(require_api_key)]), zero-side-effect discipline)
    - C:/Users/Davis/Desktop/gogo/api/main.py (whole file — _START anchor L30-32, /health route L46-49, import region L18-28, include region L52-54)
    - C:/Users/Davis/Desktop/gogo/api/auth.py (require_api_key — the dependency object to attach), C:/Users/Davis/Desktop/gogo/api/errors.py (frozen table — this plan adds no rows)
    - C:/Users/Davis/Desktop/gogo/api/jobs.py lines 33-41 + 88-99 + 227-245 (jobs_dir/read_job + the OSError/ValueError-tolerant scan shape to mirror)
    - C:/Users/Davis/Desktop/gogo/tests/test_private.py lines 68-116 (autouse isolation fixture + write_token + frozen-mtime helpers to clone), C:/Users/Davis/Desktop/gogo/tests/test_jobs.py lines 239-261 (_seed_job_file to extend with kind/finished_at)
    - C:/Users/Davis/Desktop/gogo/tests/test_auth.py lines 330-373 (the SC2 audit whose expected_secret must gain /health/details in this same task) and lines 1-25 (docstring wording to update)
  </read_first>
  <behavior>
    - Test 1 (gate): with api.health.DATA_DIR/LOG_DIR + api.jobs.LOG_DIR + api.auth.DATA_DIR pointed at a tmp subtree holding a written token file, GET /health/details with no header → 401 {"detail": "missing API key", "code": "missing_api_key"} + WWW-Authenticate: ApiKey; with a wrong key → 403 {"detail": "invalid API key", "code": "invalid_api_key"} and no challenge header; with the valid key → 200.
    - Test 2 (exact D-29 shape): the 200 body's top-level key set is exactly {"versions", "uptime_seconds", "last_check"}; versions' key set is exactly {"python", "uvicorn", "fastapi"}; last_check's key set is exactly {"health_job", "market_state_mtime"}; content-type starts with application/json.
    - Test 3 (registry + market stat wired): with a seeded succeeded health-check job whose finished_at is os.utime-frozen (e.g. 1_789_459_479) and a market_state.json frozen the same way, the 200 body carries health_job == datetime.fromtimestamp(finished_at, timezone.utc).isoformat() computed in-test, and market_state_mtime == the same computation for the frozen mtime.
    - Test 4 (uptime anchored to /health): GET /health then GET /health/details with the valid key → details.uptime_seconds is an int >= 0 and >= the /health uptime_seconds read moments earlier (same monotonic anchor).
    - Test 5 (public face untouched): bare GET /health (no key) → 200 with exact key set {"status", "uptime_seconds"}.
  </read_first>
  <action>
    Write the five behaviors in tests/test_health_details.py FIRST (module docstring in Chinese naming OPS-03 SC1 / D-29..D-31 + the data-isolation pin; scaffolding cloned from tests/test_private.py: module-level client = TestClient(app), autouse _isolated fixture patching api.health.DATA_DIR/LOG_DIR, api.jobs.LOG_DIR, api.auth.DATA_DIR into tmp_path subtrees + write_token; _seed_job_file-style helper that writes a registry json with kind/finished_at fields directly under tmp/logs/api/jobs/; market_state.json + os.utime frozen-mtime helper) — the tests fail red on ImportError (no api/health.py yet), then implement:

    1. api/health.py — module docstring in Chinese; import chain: os, sys, time, datetime/timezone, importlib.metadata.version, fastapi APIRouter/Depends, api.auth.require_api_key, api.jobs (module attr, call-time reference), scripts.daily.config DATA_DIR/LOG_DIR. Zero import side effects; ASCII-only console text (none expected). router = APIRouter(dependencies=[Depends(require_api_key)]) — the same dependency object identity the SC2 audit asserts. NO module-level import of api.main (circular by construction — main.py imports api.health before _START exists).
    2. Private helpers: _versions() returning {"python": sys.version, "uvicorn": version("uvicorn"), "fastapi": version("fastapi")} with PackageNotFoundError → "unknown" for each dist; _latest_succeeded_health_check() scanning jobs.jobs_dir() for *.json (skip dotfiles), read via api.jobs.read_job, keep jobs with kind == "health-check" (the api/actions.py KIND_CMDS literal) and status == "succeeded", track max finished_at (skip None), listdir/read OSError or ValueError → skip (never fatal), return the winning job dict or None; _iso(value) → datetime.fromtimestamp(value, timezone.utc).isoformat().
    3. The route @router.get("/health/details") — a plain sync def (no params: nothing attacker-controlled enters this handler — no query/body surface). Body assembled in D-29 key order: {"versions": _versions(), "uptime_seconds": <lazy-imported api.main.uptime_seconds()>, "last_check": {"health_job": _iso(max finished_at) or None, "market_state_mtime": _iso(os.stat(os.path.join(DATA_DIR, "market_state.json")).st_mtime) or None — stat inside try/except OSError → None}}. The lazy import of api.main.uptime_seconds happens INSIDE the handler (request-time sys.modules hit, main.py:105-108 precedent).
    4. api/main.py — (a) def uptime_seconds() immediately after the _START anchor (L32 region): return int(time.monotonic() - _START); (b) /health body (L49) becomes {"status": "ok", "uptime_seconds": uptime_seconds()} — behavior byte-unchanged; (c) from api.health import router as health_router beside the existing router imports (L20-23); (d) app.include_router(health_router) in the include region (L52-54) with a comment noting 机密级 router-level auth (private L53 style). Nothing else in main.py moves.
    5. tests/test_auth.py in the SAME task (mandatory pin — the suite goes red otherwise): expected_secret (L349) gains "/health/details"; module docstring L8-14 wording updated: the D-11 exemption list is per-path enumeration and /health/details is 机密级 (joins the audit set, not the exemption); SC2 paragraph notes the new route's classification.
    6. Drive red → green on the five behaviors; run the file suites; specific-path staging + commit: `feat(05-01): auth-gated GET /health/details (D-29..D-31) + uptime_seconds anchor` for api/, `test(05-01): health-details contract suite` for tests/test_health_details.py, `test(05-01): SC2 audit expected_secret + /health/details` for tests/test_auth.py — data/ and logs/ stay untouched.
  </action>
  <verify>
    <automated>python -m pytest tests/test_health_details.py tests/test_auth.py -q</automated>
    <fails_when>non-zero exit, or any failed/error above 0, or 0 tests collected — red means a gate drift (401/403 envelope bytes or the challenge header), an exact-key-set violation, an ISO computation mismatch, an uptime anchor split, or the SC2 audit catching an unclassified /health/details route</fails_when>
    <automated>git status --porcelain -- data/ logs/</automated>
    <fails_when>non-empty output (isolation pin — real data/ and logs/ must be zero-touched)</fails_when>
  </verify>
  <acceptance_criteria>
    - api/health.py exists with the router-level require_api_key gate, GET /health/details assembling the D-29 shape, and the _versions/_latest_succeeded_health_check/_iso helpers; api/main.py has uptime_seconds() (with /health calling it) and the two-line health_router import + include; tests/test_auth.py expected_secret contains "/health/details"
    - python -m pytest tests/test_health_details.py tests/test_auth.py -q exits 0; git status --porcelain -- data/ logs/ empty; specific-path staging used
  </acceptance_criteria>
  <done>The full /health/details slice works end-to-end: an authenticated GET returns the exact D-29 body with versions from the live environment, uptime from the /health anchor, and last_check from the real registry + market file; unauthenticated callers get the pinned 401/403; the SC2 audit proves the route's secret tier.</done>
</task>

<task type="auto" tdd="true">
  <name>tests/test_health_details.py full OPS-03 SC1 matrix: null legs, fail-closed no-token leg, versions fallback, public /health byte pins</name>
  <files>tests/test_health_details.py</files>
  <read_first>
    - C:/Users/Davis/Desktop/gogo/api/health.py (the module under test — null legs and _versions live here)
    - C:/Users/Davis/Desktop/gogo/api/jobs.py lines 88-99 + 227-245 (read_job/tolerance semantics the null legs pin)
    - C:/Users/Davis/Desktop/gogo/tests/test_health.py (whole file — the public /health byte pins that must survive beside the new route)
    - C:/Users/Davis/Desktop/gogo/tests/test_health_details.py (the file being extended)
  </read_first>
  <behavior>
    - Test 1 (health_job null legs): seeded variants each answer 200 with health_job == null — empty/missing jobs dir; only failed health-check jobs; only succeeded jobs of other kinds (e.g. pipeline); a corrupt .json in the registry; a dotfile-only registry.
    - Test 2 (market_state null leg): no market_state.json under DATA_DIR → 200 with market_state_mtime == null (never 5xx); a non-stat-able path (patch os.stat to raise OSError) → null too.
    - Test 3 (fail-closed no-token leg): with no token file and no GOGO_API_TOKEN env (clean-env fixture), a presented key → 403 {"detail": "invalid API key", "code": "invalid_api_key"} (read_token None → fail-closed, api/auth.py semantics).
    - Test 4 (versions fallback): monkeypatch importlib.metadata.version to raise PackageNotFoundError for uvicorn → versions["uvicorn"] == "unknown", python/fastapi keys still populated; versions self-consistency leg: each non-unknown value equals the in-test importlib.metadata.version computation.
    - Test 5 (public /health byte pins): with the new route registered, GET /health (no key) → 200, body key set exactly {"status", "uptime_seconds"}, uptime monotonic across two calls; content-type application/json.
  </behavior>
  <action>
    Extend tests/test_health_details.py into the full matrix (the Task 1 tracer slice plus these legs), then drive green:

    1. Add the null-leg helpers (seed-with-status/kind variants reusing the registry seed helper; corrupt-file and dotfile cases per test_jobs.py:297-308 shape) and the HTTP legs — every null leg asserts status 200 and the exact JSON null value, pinning the never-5xx read discipline.
    2. Add the fail-closed leg (empty token dir + presented key → 403 — the api/auth.py None-expected branch, which the file-token vs env ordering of the autouse fixture must not mask: monkeypatch.delenv("GOGO_API_TOKEN") first).
    3. Add the versions-fallback legs via monkeypatch on importlib.metadata.version and the self-consistency computation (compute expected in-test with importlib.metadata.version("uvicorn")/("fastapi") — never hardcode version strings; both machines pass regardless of installed versions).
    4. Assert no error body ever contains str(tmp_path) and no body carries the token byte (leak hygiene, cheap legs on the 401/403/200 bodies).
    5. Run tests/test_health_details.py until green; then the FULL suite — public state/health suites must stay green beside the new 机密级 route; hygiene; specific-path staging + commit: `test(05-01): health-details null/fail-closed/versions-fallback matrix`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_health_details.py -q</automated>
    <fails_when>non-zero exit, or any failed/error above 0, or 0 tests collected — red means a null-leg gap (5xx on missing data), a fail-closed regression, a hardcoded-version drift, or a path/token leak in an error body</fails_when>
    <automated>python -m pytest -q</automated>
    <fails_when>non-zero exit, or any failed/error above 0, or "no tests ran" (full-suite regression gate — the public tier and envelope suites stay green beside the new route)</fails_when>
    <automated>git status --porcelain -- data/ logs/</automated>
    <fails_when>non-empty output (isolation pin)</fails_when>
  </verify>
  <acceptance_criteria>
    - tests/test_health_details.py covers: all health_job null variants, market_state_mtime null variants, fail-closed 403 no-token, versions PackageNotFoundError fallback + self-consistency, no-path/no-token leak hygiene, public /health byte pins
    - python -m pytest -q exits 0; git status --porcelain -- data/ logs/ empty; specific-path staging used
  </acceptance_criteria>
  <done>The OPS-03 SC1 contract is fully pinned: every data-absence leg answers 200 with the honest null, no-token callers are fail-closed, versions never hardcode and never crash, and /health stays byte-exact beside the new route.</done>
</task>

<task type="auto">
  <name>Close-out: frozen-envelope zero-diff proof + full-suite regression + hygiene + 05-01-SUMMARY.md</name>
  <files>tests/test_health_details.py, tests/test_auth.py</files>
  <read_first>
    - C:/Users/Davis/Desktop/gogo/api/errors.py (the frozen table that must show zero diff)
    - C:/Users/Davis/Desktop/gogo/tests/test_auth.py lines 330-373 (the SC2 audit — final state to re-verify)
  </read_first>
  <action>
    Prove the plan's envelope purity and close the plan:

    1. `git diff -- api/errors.py` must be empty (frozen 04-01 table untouched — the route raised nothing new; its only error paths are require_api_key's existing raises).
    2. Grep the new code for new console text: any print in api/health.py would violate the ASCII/zero-console discipline of the route modules — none expected (module-level zero-side-effect contract).
    3. Re-run the affected suites and then the FULL suite (baseline 148 passed 1 skipped + this plan's ~10 additions — record the exact new count in the SUMMARY); git status --porcelain -- data/ logs/ must stay empty.
    4. Write .planning/phases/05-recovery-observability-ops-polish/05-01-SUMMARY.md — record: the route contract as built (path, gate, 200 body shape with an example body from the suite, 401/403 envelope bytes), the uptime_seconds refactor (diff-shape note: /health behavior unchanged), the null-leg semantics decisions ([ASSUMED] rows above — mark ACCEPT/DELTA for the end-of-phase review), the SC2 audit inventory including the new route, the versions self-consistency approach, and the full-suite outcome.
  </action>
  <verify>
    <automated>git diff -- api/errors.py</automated>
    <fails_when>non-empty output (frozen envelope table must show zero diff)</fails_when>
    <automated>python -m pytest -q</automated>
    <fails_when>non-zero exit, or any failed/error above 0, or "no tests ran"</fails_when>
    <automated>git status --porcelain -- data/ logs/</automated>
    <fails_when>non-empty output (isolation pin)</fails_when>
  </verify>
  <acceptance_criteria>
    - git diff -- api/errors.py empty; full suite exits 0; data/logs hygiene clean
    - 05-01-SUMMARY.md exists with the contract-as-built record, the [ASSUMED] rows marked for review, and the suite count
  </acceptance_criteria>
  <done>The plan is provably closed: zero frozen-envelope diff, full suite green, data/ and logs/ untouched, and the SUMMARY carries the exact contract for the end-of-phase review.</done>
</task>

</tasks>

## Artifacts this phase produces (symbols)

- `api/health.py` (new path) — `router` (APIRouter with `Depends(require_api_key)` — same dependency object as api/private.py), `health_details` (GET /health/details → 200 D-29 dict), `_versions()` (sys.version + importlib.metadata, PackageNotFoundError → "unknown"), `_latest_succeeded_health_check()` (registry scan: kind health-check + status succeeded, max finished_at), `_iso(value)`
- `api/main.py` (modified) — `uptime_seconds()` public function beside `_START`; `/health` body calls it (unchanged behavior); `health_router` import + include (two lines)
- `tests/test_health_details.py` (new path) — full SC1 suite: gate matrix, exact key sets, ISO equality, null legs, fail-closed no-token, versions fallback + self-consistency, leak hygiene, public /health pins; `_isolated` autouse fixture + `write_token` + registry/market_state seed helpers with frozen mtimes
- `tests/test_auth.py` (modified) — `expected_secret` set gains "/health/details"; module docstring D-11/SC2 wording updated (per-path exemption, new route is 机密级)

## Source Audit (multi-source coverage)

```
SOURCE    | ID      | Feature/Requirement                        | Plan  | Status
--------- | ------- | ------------------------------------------ | ----- | ------
GOAL      | SC1     | /health/details versions/uptime/last-check behind auth; no key -> 401 | 05-01 | COVERED (tasks 1-3)
REQ       | OPS-03  | 日志轮转 + 鉴权版 GET /health/details        | 05-01..05-04 | COVERED (details half here; rotation in 05-02; live proof 05-04)
CONTEXT   | D-29    | 返回字段 versions/uptime_seconds/last_check  | 05-01 | COVERED (tasks 1-2)
CONTEXT   | D-30    | sys.version + importlib.metadata 惰性读; /health 锚点复用; registry + os.stat 来源 | 05-01 | COVERED (tasks 1-2)
CONTEXT   | D-31    | 机密级 X-API-Key; 401/403 D-10 分工; 冻结信封零新增 | 05-01 | COVERED (tasks 1, 3)
CONTEXT   | discretion | 路由模块位置 (独立模块); last_check 查询实现; versions 兜底; 缺失语义 | 05-01 | COVERED (module; flagged [ASSUMED] rows)
RESEARCH  | PATTERNS | SC2 连带必改 same-task; 机密级 router 样板; 纯函数/隔离纪律 | 05-01 | COVERED (task 1)
```

Other source rows covered by sibling plans: console.log rotation + registry cap → 05-02; D-35 Mac checklist + D-36 15:30 task confirm + D-37 deployment note + classification-table mirrors → 05-03; real-machine live gate (SC1 live matrix + rotation proof + SC3 suite) → 05-04. Explicit exclusions (not gaps): v2 OPS-04/05/06, ACT-04/05, Mac 实跑回填 (用户执行), GUI items (CLI-only 2026-09-04 用户拍板), 进程守护.

## Assumption-delta decision

No assumption-delta payload was delivered with this plan-phase invocation. The [ASSUMED] rows above (market-state null semantics, uptime wiring shape, newest-job definition, ISO computation) are planner-裁定 micro-decisions with fallbacks recorded for the end-of-phase review — each is a one-line local change if rejected, no architectural dependency.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Consumer → /health/details | Ops-introspection metadata (versions/uptime/mtimes) crosses here; attacker-controlled only in the X-API-Key header — the handler itself has zero params |
| API → jobs registry | Registry jsons are read-only via read_job — crafted/corrupt files must never 5xx or crash the endpoint |
| API → data/market_state.json | os.stat only — no handle retained, no content read |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | GET /health/details unauthenticated | high | mitigate | Router-level require_api_key (same dependency object as private/actions): no key → 401 + WWW-Authenticate, wrong key → 403, no-token-configured → fail-closed 403; SC2 audit asserts dependency identity on the live route object — a tier drift fails the suite in the same commit that introduces it |
| T-05-02 | Information Disclosure | Registry/state file disclosure via forged last_check | low | accept | Body carries only the newest health-check finished_at and market_state mtime in ISO — metadata with no file content; a local writer with logs/ access already holds the files themselves; loopback-only default bind (SEC-03) caps exposure |
| T-05-03 | Denial of Service | Corrupt/oversized registry files or missing dirs | medium | mitigate | Scan mirrors api/jobs.py prune tolerance: OSError/ValueError per file → skip; listdir OSError → null; handler is a plain sync read — bounded work per request, no held handles, no 5xx paths (suite pins every null leg as 200) |
| T-05-04 | Information Disclosure | Version fingerprint aids targeting if exposed | low | accept | Data is loopback-only by default (SEC-03) and key-gated; version disclosure is standard for ops endpoints; the token requirement is the control — documented, no code change |
| T-05-SC | Tampering | package installs | high | mitigate | Zero package installs — package-legitimacy gate not triggered (stdlib importlib.metadata/datetime + installed fastapi stack) |

ASVS level 1, blocking threshold high: the high row maps to concrete tests (Task 1 behaviors 1/5, Task 2 legs, Task 3 SC2 re-verify). Package legitimacy: no install commands exist in this plan.
</threat_model>

<verification>
- Task 1: python -m pytest tests/test_health_details.py tests/test_auth.py -q green — gate matrix, exact key sets, ISO equality, uptime anchor, SC2 audit with /health/details; data/logs hygiene clean.
- Task 2: python -m pytest tests/test_health_details.py -q green — null legs, fail-closed, versions fallback, leak hygiene; python -m pytest -q full-suite green.
- Task 3: git diff -- api/errors.py empty; python -m pytest -q green; hygiene clean; 05-01-SUMMARY.md written with the contract-as-built record.
- Per-task sample rate: quick suite after every task commit; full suite at the plan gate.
</verification>

<success_criteria>
- Valid-key GET /health/details returns the exact D-29 body — versions from the live environment, uptime from the /health anchor, last_check from the real registry + market file; every absence leg answers 200 with null (SC1).
- No key → pinned 401 + challenge; wrong key → pinned 403; no-token-configured → fail-closed 403 — zero new error texts, zero frozen-envelope diff (D-31).
- SC2 audit proves /health/details carries require_api_key (same-task pin, PATTERNS constraint 3); /health stays byte-exact and public (HLT-01).
- api/main.py diff is the uptime_seconds helper + two-line include; full pytest suite green; data/ and logs/ untouched.
</success_criteria>

<output>
Create .planning/phases/05-recovery-observability-ops-polish/05-01-SUMMARY.md when done — record the route contract as built (path, gate, example 200 body, 401/403 bytes), the uptime_seconds refactor shape (/health behavior unchanged), the null-leg and versions-fallback decisions ([ASSUMED] rows marked ACCEPT/DELTA for the end-of-phase review), the SC2 audit route inventory, and the full-suite outcome.
</output>
