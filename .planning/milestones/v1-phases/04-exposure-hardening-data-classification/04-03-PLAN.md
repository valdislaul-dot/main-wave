---
phase: 04-exposure-hardening-data-classification
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - api/private.py
  - api/main.py
  - tests/test_private.py
  - tests/test_auth.py
autonomous: true
requirements:
  - STA-02
  - SEC-02
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "GET /v1/private/{name} with name in {portfolio, journal, candidates} is served ONLY through the token gate: no key → 401 {\"detail\": \"missing API key\", \"code\": \"missing_api_key\"} + WWW-Authenticate: ApiKey, wrong key → 403 {\"detail\": \"invalid API key\", \"code\": \"invalid_api_key\"} — the router carries APIRouter(dependencies=[Depends(require_api_key)]) and lives in its own /v1/private/* namespace structurally isolated from the public /v1/state/* (04-CONTEXT 敏感端点形态, STA-02 SC1, SEC-02 D-10/D-11)"
    - "With a valid key the responses replicate the STA-01 contract byte-for-byte: the raw file body (never re-serialized — D-01) with media_type application/json plus X-Data-Mtime and X-Data-Age-S headers from the file's real mtime; stale-cache fallback serves last-known-good bytes with the cached mtime + X-Data-Stale: true; a cold cache on missing/unreadable data answers 503 {\"detail\": \"private data temporarily unavailable\", \"code\": \"private_data_unavailable\"} — never a bare 500 (D-04 semantics, SC1)"
    - "candidates resolution follows the in-repo selection rule: no ?date= returns the newest candidates_*.json excluding the candidates_v3_* legacy prefix; ?date=YYYY-MM-DD or ?date=YYYYMMDD (whitelist formats) returns that exact dated file (YYYYMMDD normalized to dashed before filename composition); an invalid date answers 422 {\"detail\": \"date must be YYYY-MM-DD or YYYYMMDD\", \"code\": \"invalid_date_format\"}; a well-formed date with no matching file answers 503 private data temporarily unavailable (04-CONTEXT 敏感端点形态 候选语义, SC4)"
    - "An unknown name answers 404 {\"detail\": \"Not Found\", \"code\": \"unknown_private_name\"} before any path composition (whitelist-before-compose, D-03/D-04) and whitelist-out files that really exist stay unreachable — decoy guards pin that no path text or str(tmp_path) ever appears in error bodies"
    - "The public surface is untouched: /health, /health/ready, GET /v1/state/*, and /openapi.json answer without a key; the market/temperature endpoints keep their exact behavior (SEC-02 two-tier classification, SC1/SC2)"
    - "A route-by-route audit (test) proves every APIRoute carries exactly its tier's protection: require_api_key present on /v1/private/*, /v1/actions/*, /v1/jobs/*; absent on /health, /health/ready, /v1/state/*, /openapi.json — no sensitive route reachable without a token, no public route requiring one (SC2, 04-CONTEXT 分级审计落地形态 discretion)"
    - "The key never leaks through the new surface: private response bodies, error bodies, and the leak-audit's collected request/response set contain no token byte; the private read path makes no network call (import chain stays fastapi + scripts.daily.config, reads are pure local file reads) (SC3, D-05)"
    - "python -m pytest -q stays green with tests/test_private.py added and api/main.py gaining exactly the two-line private-router include; git status --porcelain -- data/ logs/ stays empty"
  artifacts:
    - path: "api/private.py"
      provides: "The token-gated read router for the 机密级 tier: PRIVATE_FILES whitelist map + the candidates dynamic-file rule, the read-layer twin get_private(name, date=None, retries=2, retry_delay=0.02, reader=read_state_file) duplicating state.py's retry/cache loop against LOG_DIR with its own _CACHE slots per (name[, date]), the GET /v1/private/{name} route with ?date= whitelist gate, raw Response + X-Data-Mtime/X-Data-Age-S/X-Data-Stale headers, 404/422/503 raises with the frozen error-table texts"
      contains: "router, PRIVATE_FILES, _CACHE, get_private, get_private_endpoint"
    - path: "api/main.py"
      provides: "Two-line include: from api.private import router as private_router + app.include_router(private_router) beside the state include — nothing else moves (the 04-01 handlers/openapi wiring and the boot sequence stay byte-untouched)"
      contains: "private_router include"
    - path: "tests/test_private.py"
      provides: "The STA-02/SEC-02 contract suite: gate matrix (401/403 bytes + challenge header), public exemption legs, raw byte-verbatim + exact freshness headers per name, candidates selection (latest / both date formats / legacy exclusion / unknown-date 503 / invalid-date 422), unknown-name 404 + decoy guard + no-path-in-bodies, injected-reader torn-window unit tests on get_private (stale body == cached-mtime version, cold 503)"
      contains: "TestClient, autouse LOG_DIR/DATA_DIR tmp fixture, _write_* helpers incl. candidates_v3_* decoy"
    - path: "tests/test_auth.py"
      provides: "The SC2 route-by-route classification audit test + the private-route 401-without-key exemption leg + the leak-audit extension collecting /v1/private responses"
      contains: "route classification table assertion"
  key_links:
    - from: "api/private.py"
      to: "api/state.py"
      via: "read_state_file imported and reused as-is (path-parameterized, no DATA_DIR coupling — api/state.py:38-49); get_private is the documented twin of get_state (api/state.py:56-80) retargeted at LOG_DIR — the signed D-01..D-05 core is NOT refactored to take a directory parameter (04-PATTERNS discretion decision)"
    - from: "api/private.py"
      to: "api/auth.py"
      via: "the same require_api_key dependency object gates this router (router-level, never middleware) — zero new auth code (04-CONTEXT reusable assets)"
    - from: "api/private.py"
      to: "scripts/daily/config.py"
      via: "LOG_DIR module attr imported once, joined at call time (the monkeypatch seam every suite relies on); the module import chain stays fastapi + scripts.daily.config only with no side effects (state.py:14-17 discipline)"
    - from: "candidates file selection"
      to: "in-repo listing rule"
      via: "f.startswith('candidates_') and not f.startswith('candidates_v') with date-part name sorting (mirrors scripts/daily/morning_check.py:11-18 / auction_pool.py:48-52 selection rule — the RULE is mirrored, file bytes still go through read_state_file; no script loader is imported — API 不改脚本逻辑 data discipline)"
    - from: "tests/test_private.py"
      to: "tests/test_state.py scaffolding"
      via: "module-level TestClient(app), autouse fixture monkeypatching api.private.LOG_DIR (+ api.state.DATA_DIR for the public-side legs) to tmp_path and clearing api.private._CACHE, decoy/byte/header assertion shapes (tests/test_state.py:52-63, 87-129)"
  prohibitions:
    - statement: "The raw passthrough contract (D-01) MUST hold on the private surface: file bytes are returned verbatim through Response(content=raw) — never json.loads/re-dump, never FileResponse (held handles block os.replace on Windows), media_type application/json without charset"
      category: safety
      requirement_id: STA-02
      status: unverified
    - statement: "The state.py signed read core (D-01..D-05) MUST NOT be refactored to accept a directory — get_private is a documented twin duplicate in api/private.py; api/state.py gets no code change from this plan"
      category: safety
      requirement_id: STA-02
      status: unverified
    - statement: "The name whitelist and the date whitelist MUST be checked before any path composition: unknown name → 404 unknown_private_name (no filesystem touch), invalid date → 422 invalid_date_format (D-03/D-04, SC4); candidates_* date part composes filenames only after whitelist-before-compose and never from unvalidated input"
      category: safety
      requirement_id: STA-02
      status: unverified
    - statement: "GET handlers in api/private.py MUST make no external network call and the import chain stays fastapi + scripts.daily.config (state.py discipline) — no script module loaders are imported, no script output is parsed (API 不改脚本逻辑)"
      category: safety
      requirement_id: SEC-02
      status: unverified
    - statement: "The envelope codes/details come from the frozen 04-01 table verbatim (unknown_private_name / private_data_unavailable / invalid_date_format rows exist there) — this plan does NOT edit api/errors.py"
      category: safety
      requirement_id: SEC-02
      status: unverified
    - statement: "The classification audit MUST assert dependency presence on the actual route objects (require_api_key in route.dependencies) and MUST NOT be satisfiable by documentation alone — and /openapi.json joins the public tier it documents"
      category: safety
      requirement_id: SEC-02
      status: unverified
---

# Plan 04-03 — Token-gated private read endpoints /v1/private/{name} (STA-02 tracer) + SC2 route-by-route classification audit

## User-story framing

**As a** consumer holding a valid X-API-Key (cloud panel, scheduled integrator, the user off-machine), **I want to** GET my 持仓/账本/候选 exactly like the public market state — raw file bytes with freshness headers — but ONLY with my key, on a namespace that can never be confused with the public one — **so that** the privacy red line (2026-08-31) holds even if the API ever leaves 127.0.0.1: the most sensitive data in the repo is served token-gated by construction, and a route-by-route audit can prove it.

<objective>
Deliver the phase's signature capability on top of the Wave-1 foundation: `api/private.py` — the token-gated read router for the 机密级 tier (whitelist PRIVATE_FILES + dynamic candidates rule, `get_private(name, date=None)` twin of the Phase 2 read layer against LOG_DIR, `?date=` whitelist param, raw byte passthrough + X-Data-* freshness headers, 404/422/503 raises speaking the frozen 04-01 envelope codes), the two-line `api/main.py` include, the full contract suite `tests/test_private.py`, and the SC2 route-by-route classification audit + exemption/leak extensions in `tests/test_auth.py`. 04-02's journal atomicity (Wave 1) is the signed precondition this plan's deployment stands on.

Purpose: STA-02's SC1/SC2/SC3 land here — reads with the exact STA-01 wire contract, structure-level proof that every endpoint carries exactly its tier's protection, and no token leak through the new surface. The plan opens with the phase tracer: one authenticated GET /v1/private/portfolio path wired through every layer (auth dependency → whitelist → shared read layer → raw response) before any expansion.

Output: `api/private.py`, `api/main.py` (two-line include), `tests/test_private.py`, `tests/test_auth.py` (audit additions), green suite.
Decisions: D-13, D-14, D-15, D-16
</objective>

## Flagged Assumptions (spec-less probe fallback — continuation)

- STA-02 (`unclassified` probe — no scenario clause to honor): the endpoint shape, semantics and error split are fully pinned by 04-CONTEXT 敏感端点形态 (path namespace, name set, candidates date semantics, D-10 auth split, raw body + freshness headers, D-06 precondition) — the *candidates latest-file rule* mirrors the in-repo selection rule (morning_check.py:11-18) since the user's data discipline forbids importing script loaders into the API.
- SEC-02 (`unclassified` probe — no scenario clause to honor): the classification table is user-signed (公开级 vs 机密级 rows, 2026-09-04); the audit's landing form (测试断言式 vs 文档表+测试双保险) was left to planner discretion — executed as a route-object assertion test in test_auth.py (single home per 04-PATTERNS) with the PROJECT.md table (04-06) as the documentation mirror.
- [ASSUMED] The candidate file for a date query is exactly candidates_{YYYY-MM-DD}.json under logs/ and the latest-selection sort is by the filename's date part (verified repo naming: logs/candidates_2026-09-03.json exists; candidates_v3_* legacy files present and must be excluded).
- [ASSUMED] read_state_file's same-handle fstat mtime behavior (api/state.py:38-49) transfers verbatim to LOG_DIR targets — no data/ vs logs/ filesystem difference affects it (same volume, same NTFS semantics).
- [ASSUMED] get_private's stale-cache slots keyed per (name, date) do not grow unbounded in practice (dates are trading days only) — the _CACHE dict is bounded by the number of distinct requested dates since boot; acceptable for a single-user service, revisited only if a consumer ever pages deep history (documented in the SUMMARY, not a scope item).

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-exposure-hardening-data-classification/04-CONTEXT.md
@.planning/phases/04-exposure-hardening-data-classification/04-PATTERNS.md
@.planning/phases/04-exposure-hardening-data-classification/04-01-SUMMARY.md
@.planning/phases/04-exposure-hardening-data-classification/04-02-SUMMARY.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@api/state.py
@api/auth.py
@api/main.py
@api/errors.py
@tests/test_state.py
@tests/test_auth.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>api/private.py + main.py registration + tests/test_private.py: one authenticated GET /v1/private/portfolio slice end-to-end (gate → whitelist → read layer → raw bytes + headers)</name>
  <files>api/private.py, api/main.py, tests/test_private.py</files>
  <read_first>
    - 04-PATTERNS.md api/private.py section (module skeleton, route shape, candidate rule, decoy guards, test cases) — C:/Users/Davis/Desktop/gogo/.planning/phases/04-exposure-hardening-data-classification/04-PATTERNS.md
    - C:/Users/Davis/Desktop/gogo/api/state.py (whole file: module skeleton L19-35, read_state_file L38-49, get_state L56-80, headers L92-100 — the reuse surface and the twin to duplicate)
    - C:/Users/Davis/Desktop/gogo/api/auth.py (require_api_key — the dependency object to attach), C:/Users/Davis/Desktop/gogo/api/main.py (the include insertion point beside L36-37)
    - C:/Users/Davis/Desktop/gogo/api/errors.py (the frozen table rows this module's raises speak: unknown private name → unknown_private_name, private data temporarily unavailable → private_data_unavailable, date text → invalid_date_format)
    - C:/Users/Davis/Desktop/gogo/tests/test_state.py lines 27-129 (scaffolding, byte/header/decoy pin shapes), C:/Users/Davis/Desktop/gogo/tests/test_actions.py lines 37-59 (write_token + registry helpers)
  </read_first>
  <behavior>
    - Test 1 (gate, portfolio path): with api.private.LOG_DIR pointed at tmp_path and a real-looking logs/portfolio.json written there (UTF-8 Chinese content, CRLF line endings), GET /v1/private/portfolio with no header → 401 {"detail": "missing API key", "code": "missing_api_key"} + WWW-Authenticate: ApiKey; with a wrong key → 403 {"detail": "invalid API key", "code": "invalid_api_key"} without the challenge header; with the valid key → 200.
    - Test 2 (raw + headers, portfolio path): the 200 body bytes equal the file bytes exactly (byte-verbatim incl. CRLF and the Chinese content — Pitfall 1), content-type starts with application/json, X-Data-Mtime equals the file's real mtime as a string, X-Data-Age-S is a non-negative integer string, and no X-Data-Stale header is present on the fresh path.
    - Test 3 (public untouched): GET /health → 200 with no key; GET /v1/state/{name} for a seeded whitelist state file → 200 with no key (public tier intact beside the new private router — SC1).
    - Test 4 (journal path rides the same slice): GET /v1/private/journal with the valid key → 200 raw bytes + headers for logs/trading_journal.json.
  </behavior>
  <action>
    Build the private read slice end-to-end. Write the four behaviors in tests/test_private.py FIRST (module docstring in Chinese naming STA-02/SEC-02/SC1-3, the D-01 raw-passthrough contract, and the data-isolation pin; scaffolding copied from tests/test_state.py: module-level TestClient(app), autouse fixture monkeypatching api.private.LOG_DIR (and api.state.DATA_DIR for Test 3) into tmp_path and clearing api.private._CACHE between tests, _write_* file helpers, write_token helper) — the tests fail red on ImportError (no api/private.py yet), then implement:

    1. api/private.py — module docstring in Chinese; import chain fastapi + scripts.daily.config only, zero import side effects, ASCII console text; call-time path resolution only. Skeleton per 04-PATTERNS: PRIVATE_FILES = {"portfolio": "portfolio.json", "journal": "trading_journal.json"} (candidates handled dynamically by the shared selection helper); _CACHE = {} keyed per (name, date); router = APIRouter(dependencies=[Depends(require_api_key)]); from api.state import read_state_file (reuse, never copy); from scripts.daily.config import LOG_DIR.
    2. get_private(name, date=None, retries=2, retry_delay=0.02, reader=read_state_file) — the get_state twin: target = os.path.join(LOG_DIR, filename) where filename comes from PRIVATE_FILES[name] or the candidates rule; identical retry/cache/stale semantics with slots keyed (name, date); docstring states it is the state.py twin targeting logs/ (the signed state.py core is NOT refactored).
    3. The route: @router.get("/v1/private/{name}") def get_private_endpoint(name: str, date: str = None) — whitelist gate first (name in PRIVATE_FILES or name == "candidates", else raise HTTPException(404, detail="unknown private name")); then if date is not None and not the whitelist regex fullmatch → HTTPException(422, detail="date must be YYYY-MM-DD or YYYYMMDD"); candidates filename resolution (latest-or-dated) happens BEFORE the read-layer call so the 404/422 gates precede every path composition (D-03); unavailability → HTTPException(503, detail="private data temporarily unavailable"); response = Response(content=raw, media_type="application/json", headers={X-Data-Mtime, X-Data-Age-S, + X-Data-Stale on the stale path}) — never FileResponse, never re-serialization.
    4. api/main.py — exactly two lines: the import beside the existing router imports and app.include_router(private_router) immediately after the state include (L36-37 region). Nothing else moves.
    5. Drive red → green on the four behaviors; then run the full file suite; hygiene + commit with specific-path staging: `feat(04-03): token-gated /v1/private/{name} read surface (portfolio/journal slice)` for api/ + `test(04-03): private read contract suite` for tests/test_private.py — data/ and logs/ must stay untouched.
  </action>
  <verify>
    <automated>python -m pytest tests/test_private.py -q</automated>
    <fails_when>non-zero exit, or any failed/error above 0, or 0 tests collected — red means a gate drift (401/403 bytes or the challenge header), a raw-passthrough violation (body re-serialized / charset added), a header computation drift (mtime/age strings), or the include landing wrong</fails_when>
    <automated>git status --porcelain -- data/ logs/</automated>
    <fails_when>non-empty output (isolation pin)</fails_when>
  </verify>
  <acceptance_criteria>
    - api/private.py exists with the whitelist, the auth-gated router, get_private (injectable-reader twin), the route with raw Response + freshness headers; api/main.py gained exactly the two-line include
    - tests/test_private.py has the four tracer behaviors green; python -m pytest tests/test_private.py -q exits 0; data/logs hygiene clean; specific-path staging used
  </acceptance_criteria>
  <done>The full private-read slice works end-to-end: an authenticated GET /v1/private/portfolio (and journal) returns byte-verbatim ledger content with freshness headers, unauthenticated callers get the pinned 401/403, and the public tier answers unchanged beside it.</done>
</task>

<task type="auto" tdd="true">
  <name>tests/test_private.py full STA-02 matrix: candidates selection (latest/dates/legacy exclusion), decoy + no-path guards, injected-reader torn-window unit tests</name>
  <files>tests/test_private.py</files>
  <read_first>
    - C:/Users/Davis/Desktop/gogo/api/private.py (the module under test — the candidates rule and get_private seam live here)
    - C:/Users/Davis/Desktop/gogo/scripts/daily/morning_check.py lines 11-18 (the in-repo candidate listing rule to mirror)
    - C:/Users/Davis/Desktop/gogo/tests/test_state.py lines 87-176 (decoy guard + injected-reader torn-window unit-test shapes — L124-129 no-path-in-bodies)
    - C:/Users/Davis/Desktop/gogo/tests/test_private.py (the file being extended)
  </read_first>
  <behavior>
    - Test 1 (candidates latest): with logs/ holding candidates_2026-09-01.json, candidates_2026-09-03.json and a legacy candidates_v3_2026-09-02.json decoy, GET /v1/private/candidates with the valid key → 200 with the bytes of candidates_2026-09-03.json (the newest non-legacy file); the legacy file is never selected.
    - Test 2 (candidates by date): ?date=2026-09-01 → the bytes of candidates_2026-09-01.json; ?date=20260901 (compact whitelist format) → the same file (normalized); both responses carry the file's real mtime/age headers.
    - Test 3 (candidates errors): ?date=2026/09/01, ?date=2026091, ?date=abc, ?date=2026-13-99 → 422 {"detail": "date must be YYYY-MM-DD or YYYYMMDD", "code": "invalid_date_format"} with zero filesystem reads beyond the gate; ?date=2026-09-05 (well-formed, no such file) → 503 {"detail": "private data temporarily unavailable", "code": "private_data_unavailable"}.
    - Test 4 (unknown name + decoy): whitelist-out files that really exist under tmp logs/ (e.g. logs/daily_reports/2026-09-03.md or logs/portfolio.json.bak) stay unreachable — GET /v1/private/daily_reports → 404 {"detail": "Not Found", "code": "unknown_private_name"}; the body never equals the decoy content and str(tmp_path) appears in no error body; GET /v1/private/portfolio.json and /v1/private/../portfolio (path traversal shapes) → 404 unknown_private_name (the {name} param never composes a path before the whitelist).
    - Test 5 (torn-window read semantics via injected reader): get_private unit tests with a fake reader (no HTTP): fresh ok → (fresh, raw, mtime) with headers materialized from the fake's mtime; decode failure then success on retry → fresh after retries; persistent decode failure with a warm cache → stale body byte-equal to the last-known-good with the CACHED mtime (headers and body always the same version); cold cache + persistent failure → StateUnavailable → the route answers 503 private data temporarily unavailable, never a bare 500.
  </behavior>
  <action>
    Extend tests/test_private.py into the full STA-02 matrix (the Task 1 tracer slice plus these additions), then drive green:

    1. Add the candidates file helpers (write dated candidates files incl. the candidates_v3_* decoy) and the Test 1-4 HTTP behaviors — candidates selection and the 422/503/404 edges pin the whitelist-before-compose and legacy-exclusion rules.
    2. Add the Test 5 unit tests exercising get_private directly with an injected fake reader (reader= parameter, retry_delay=0) — the same injectable-reader seam as tests/test_state.py's read-layer tests; assert the stale path serves the cached mtime version (body and headers never split versions) and the cold-cache path raises StateUnavailable which the route maps to the pinned 503.
    3. All pins speak the frozen 04-01 envelope shapes; no new codes are introduced (this plan does not edit api/errors.py).
    4. Run tests/test_private.py until green; then the full suite (public state suites must stay green beside the private router — SC1's market/temperature-open guarantee); hygiene + commit with specific-path staging: `test(04-03): full STA-02 private matrix — candidates selection, decoys, torn-window reads`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_private.py -q</automated>
    <fails_when>non-zero exit, or any failed/error above 0, or 0 tests collected — red means a candidates-selection regression (legacy file selected / wrong newest), a date-format gate gap, a decoy reachability bug, or a stale-cache version split (body vs mtime mismatch)</fails_when>
    <automated>python -m pytest -q</automated>
    <fails_when>non-zero exit, or any failed/error above 0, or "no tests ran" (full-suite regression gate — public tier + envelope suites stay green)</fails_when>
    <automated>git status --porcelain -- data/ logs/</automated>
    <fails_when>non-empty output (isolation pin)</fails_when>
  </verify>
  <acceptance_criteria>
    - tests/test_private.py covers: newest-non-legacy candidates selection, both whitelist date formats + normalization, the 422/503/404 error edges with envelope shapes, decoy unreachability + no-path-in-bodies, and the injected-reader torn-window/stale/cold-503 semantics
    - python -m pytest -q exits 0; git status --porcelain -- data/ logs/ empty; specific-path staging used
  </acceptance_criteria>
  <done>The STA-02 contract is fully pinned: candidates behave exactly per the selection rule, every gate answers before any path composition, decoys stay unreachable, and the read layer never splits body and header versions.</done>
</task>

<task type="auto">
  <name>tests/test_auth.py SC2 route-by-route classification audit + private exemption leg + leak-audit extension (private responses join the byte audit)</name>
  <files>tests/test_auth.py</files>
  <read_first>
    - 04-PATTERNS.md test_auth.py section (audit shape: iterate app.routes, assert Depends presence per tier) — C:/Users/Davis/Desktop/gogo/.planning/phases/04-exposure-hardening-data-classification/04-PATTERNS.md
    - C:/Users/Davis/Desktop/gogo/tests/test_auth.py (whole file — the exemptions block L169-183, the leak audit L209-254, the 401/403 matrix)
    - C:/Users/Davis/Desktop/gogo/api/main.py (route inventory: /health, /health/ready, /v1/state/*, /v1/actions/*, /v1/jobs/*, /v1/private/*, /openapi.json)
  </read_first>
  <action>
    Extend tests/test_auth.py with the SC2 audit and the two extensions (all test-only):

    1. SC2 route-by-route classification audit test: iterate app.routes; classify every APIRoute by its path pattern; assert require_api_key presence in route.dependencies equals the tier table — 机密级 (/v1/private/*, /v1/actions/*, /v1/jobs/*): present; 公开级 (/health, /health/ready, /v1/state/*, /openapi.json): absent. Depends objects expose their .dependency attribute — assert the actual callable is require_api_key, not just any dependency. The test fails if a future route lands in the wrong tier or a tier's list is empty (a classification table with no matching routes must fail — no vacuous pass).
    2. Exemption-leg extension: the existing public-exemption test (L169-183) gains the private-route leg — GET /v1/private/portfolio with no key → 401 (not 200, not 404) + WWW-Authenticate, proving the private namespace is gate-first.
    3. Leak-audit extension (SC3): the existing byte-absence audit (L209-254) already collects job logs + child env + response bodies; add the /v1/private/{portfolio, journal, candidates} responses (with the valid key against seeded tmp files) and their 401/403/404/422/503 error bodies into the collected body set, and keep asserting the token byte appears in none of them.
    4. Run tests/test_auth.py until green, then the full suite; hygiene + commit with specific-path staging: `test(04-03): SC2 route-by-route classification audit + private leak extension`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_auth.py -q</automated>
    <fails_when>non-zero exit, or any failed/error above 0, or 0 tests collected — red means a route landed in the wrong tier (a public route gained a dependency or a private route lost one), the exemption leg drifted, or the token byte appeared in a private response/error body</fails_when>
    <automated>python -m pytest -q</automated>
    <fails_when>non-zero exit, or any failed/error above 0, or "no tests ran" (full-suite regression gate)</fails_when>
    <automated>git status --porcelain -- data/ logs/</automated>
    <fails_when>non-empty output (isolation pin)</fails_when>
  </verify>
  <acceptance_criteria>
    - tests/test_auth.py contains the audit test (every route's dependency presence matches the two-tier table, non-vacuously), the private 401 exemption leg, and the leak audit extended over private responses + error bodies
    - python -m pytest -q exits 0; git status --porcelain -- data/ logs/ empty; specific-path staging used
  </acceptance_criteria>
  <done>SC2 is provable from the test suite alone: every endpoint carries exactly its tier's protection by route-object assertion, and the token-absence audit now covers the most sensitive responses in the API.</done>
</task>

</tasks>

## Artifacts this phase produces (symbols)

- `api/private.py` (new path) — `router` (`APIRouter(dependencies=[Depends(require_api_key)])`), `PRIVATE_FILES` (portfolio/journal) + the candidates dynamic rule, `_CACHE` per (name, date), `get_private(name, date=None, retries, retry_delay, reader)` (state.py twin), `get_private_endpoint(name, date)` (GET /v1/private/{name} → 200 raw + headers / 401/403 / 404 / 422 / 503)
- `api/main.py` (modified) — two-line private_router include beside the state include
- `tests/test_private.py` (new path) — full STA-02 suite (gate/raw/headers/candidates/decoy/errors/torn-window injected-reader units); autouse LOG_DIR fixture + _write_* helpers + write_token
- `tests/test_auth.py` (modified) — SC2 route-by-route audit, private exemption leg, leak-audit extension

## Source Audit (multi-source coverage)

```
SOURCE    | ID      | Feature/Requirement                        | Plan  | Status
--------- | ------- | ------------------------------------------ | ----- | ------
GOAL      | SC1     | 持仓/账本/候选 reads token-gated, raw bodies + freshness headers, 公开面无 key | 04-03 | COVERED (tasks 1-2)
GOAL      | SC2     | Route-by-route audit: tier-exact protection + boot check tested | 04-03 (+04-05 boot test) | COVERED (task 3 audit; boot half in 04-05)
GOAL      | SC3     | Errors path-free/traceback-free; no token in logs | 04-01 + 04-03 | COVERED (task 3 leak extension; envelope in 04-01)
REQ       | STA-02   | 敏感数据命名空间 /v1/private/*, ?date= 白名单取历史, D-10 鉴权分工, raw + 头, D-06 前置 | 04-03 | COVERED (tasks 1-3; precondition 04-02)
REQ       | SEC-02   | 分级鉴权: 公开级/机密级表 + 逐路由审计 | 04-03 | COVERED (task 3; docs table in 04-06)
CONTEXT   | discretion | 审计落地形态 (测试断言式) | 04-03 | COVERED (task 3, non-vacuous assertion)
RESEARCH  | read layer | read_state_file reuse, get_private twin, no state.py refactor | 04-03 | COVERED (tasks 1-2)
```

Other source rows covered by sibling plans: error envelope + WR-02/WR-03 → 04-01; journal atomicity → 04-02; date whitelist + script acceptance → 04-04; D-12 env-forced token (fail-closed boot test half of SC2) → 04-05; PROJECT.md/README classification mirrors + SC5 scans → 04-06; real-machine live gate → 04-07. Explicit exclusions (not gaps): candidates historical cache eviction policy (bounded single-user cache documented, not a work item), ACT-04/05, OPS-03, v2 items, GUI fixes.

## Assumption-delta decision

No assumption-delta payload was delivered with this plan-phase invocation — the delta scan does not fire. The private-read noun is new to this phase's surface but its contract is fully user-pinned in 04-CONTEXT (no new assumption case); the [ASSUMED] rows above (file naming, mtime transfer, cache bound) carry runtime fallbacks recorded in the SUMMARY.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Consumer → /v1/private/* | The most sensitive repo data (持仓/账本/候选) crosses here; token gate at router level; name/date params are attacker-controlled until whitelist-gated |
| API → logs/ files | Reads cross into the ledger/candidates files — read-only, byte-verbatim, whitelist-only paths |
| /v1/private responses | Raw sensitive bytes + freshness headers leave the service only to keyed callers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08 | Information Disclosure | GET /v1/private/{name} unauthenticated access | critical | mitigate | Router-level require_api_key dependency (same D-10 gate as actions): no key → 401 + WWW-Authenticate, wrong key → 403, fail-closed; public/private structural namespace split means a dependency misconfiguration cannot slide sensitive data into the public tree; SC2 audit test asserts dependency presence per tier on the live route objects |
| T-04-09 | Tampering | name/date → path composition | high | mitigate | Whitelist-before-compose (D-03): PRIVATE_FILES map + candidates rule gate any filename; unknown name → 404 before a filesystem touch; date must fullmatch the whitelist regex before any candidates_{date}.json composition; traversal/encoded shapes answer 404/422 with zero reads — decoy tests pin unreachability of real whitelist-out files |
| T-04-10 | Information Disclosure | Torn/stale ledger served as fresh | high | mitigate | 04-02 atomic writes (Wave-1 precondition) eliminate writer-side tearing; read-layer twin keeps retry + last-good stale fallback (stale body always served with the cached mtime version + X-Data-Stale: true — never a version split); cold cache → 503, never a bare 500 (D-04) |
| T-04-11 | Information Disclosure | Token leak via private responses | medium | mitigate | Leak audit extended over private response bodies + all their error bodies (byte-absence assertion, SC3); reads are pure local file reads — no network path exists on the route (import chain discipline) |
| T-04-12 | Elevation of Privilege | Tier drift on future routes | medium | mitigate | Non-vacuous SC2 audit test fails any future route whose dependency presence mismatches its tier, and fails if a tier has no routes — classification stays provable after every change |
| T-04-SC | Tampering | package installs | high | mitigate | Zero package installs — the package-legitimacy gate is not triggered (stdlib + installed fastapi stack; read_state_file/require_api_key reuse) |

ASVS level 1, blocking threshold high: critical/high rows map to concrete tests (Task 1 behaviors 1-4, Task 2 behaviors 1-5, Task 3 audit + leak extension). Package legitimacy: no install commands exist in this plan.
</threat_model>

<verification>
- Task 1: python -m pytest tests/test_private.py -q green — gate matrix, byte-verbatim + header pins for portfolio/journal, public tier untouched beside the include; data/logs hygiene clean.
- Task 2: python -m pytest tests/test_private.py -q green — full candidates matrix (newest/legacy-exclusion/both formats/422/503), decoy + no-path guards, injected-reader stale/cold semantics; python -m pytest -q full-suite green.
- Task 3: python -m pytest tests/test_auth.py -q green — route-by-route audit (non-vacuous), private 401 exemption leg, leak audit over private bodies; full suite green.
- Per-task sample rate: quick suite after every task commit; full suite at the plan gate (04-VALIDATION.md Sampling Rate).
</verification>

<success_criteria>
- Valid-key GETs of portfolio/journal/candidates return raw file bytes + X-Data-Mtime/X-Data-Age-S headers; no key → 401 + challenge; wrong key → 403; market/temperature endpoints stay keyless (SC1).
- The SC2 audit asserts tier-exact dependency presence on every live route, non-vacuously (SC2).
- The token byte is provably absent from private bodies and error bodies; no private read path can make a network call (SC3).
- api/main.py is a two-line diff on top of 04-01's wiring; full pytest suite green; data/ and logs/ untouched.
</success_criteria>

<output>
Create .planning/phases/04-exposure-hardening-data-classification/04-03-SUMMARY.md when done — record the exact route contract as built (route, status codes, envelope body bytes), the candidates selection rule implementation, the get_private twin's deviation list vs get_state (if any), the audit test's route inventory, and full-suite outcomes.
</output>
