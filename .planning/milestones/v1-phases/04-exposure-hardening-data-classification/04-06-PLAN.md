---
phase: 04-exposure-hardening-data-classification
plan: 06
type: execute
wave: 4
depends_on:
  - 04-03
  - 04-04
  - 04-05
files_modified:
  - .planning/PROJECT.md
  - README.md
autonomous: true
requirements:
  - SEC-02
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "PROJECT.md Constraints/Security records the two-tier data classification (公开级: /health, /health/ready, /v1/state/*, /openapi.json — no key needed | 机密级: /v1/private/*, POST /v1/actions/*, GET /v1/jobs/* — API key required) as the standing reference for every future endpoint decision, marked with the 定稿机制 stamp (用户确认 2026-09-02 + Phase 4 定稿 2026-09-04) and linked to the privacy red line (2026-08-31) — future phases and reviewers read the classification here, not from memory (SEC-02 documentation requirement)"
    - "README.md documents the known single-flight limits (SC5): one pipeline per time (409 already_running), the job state that survives, and the concurrent entry points that deliberately remain outside the lock — Mac 端 crontab 无锁, 手动 CLI, GUI 已弃用 (2026-09-04) — plus the boot posture (loopback default, GOGO_API_TOKEN env-forced for non-loopback per 04-05) and the classification reference; the documented-limits clause of SC5 ('README documents known limits') is satisfied"
    - "The endpoint list inside README's API section stays in sync with what the code serves: /health, /health/ready, /v1/state/*, /v1/private/*, /v1/actions/*, /v1/jobs/* — the list is verified against api/main.py + the router files during the task (the docs gate opens only on byte-truth)"
    - "SC5 scan commands are embedded in this plan's <verify> elements as executable checks (git history for data/api_token.txt, sync_cloud whitelist source, .gitignore coverage) — the executor runs them and records outcomes in the SUMMARY as SC5 evidence; a scan failure is a halt-and-report, not a docs-fix"
    - "Only two files are touched — .planning/PROJECT.md and README.md; no code, no tests, no runtime behavior changes; data/ and logs/ stay untouched; the suite stays green (docs-only diff)"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Constraints/Security 数据分类表: two-tier table (数据类别/端点/保护级别) + 定稿 markers (2026-09-02 用户确认, Phase 4 定稿 2026-09-04) + privacy red-line pointer (2026-08-31); the classification single source for all future endpoint/phase decisions"
      contains: "数据分类表, 定稿 marker, privacy red-line link"
    - path: "README.md"
      provides: "API known-limits section: single-flight scope (409 + running_job_id), remaining concurrent entry points (Mac crontab 无锁/手动 CLI/GUI 弃用), boot posture (loopback default + env-forced token), classification reference pointer, endpoint inventory"
      contains: "Known limits section, endpoint list, classification pointer"
  key_links:
    - from: "PROJECT.md classification table"
      to: "the routers that implement it (api/state.py, api/private.py, api/actions.py, api/jobs.py)"
      via: "endpoint-name strings — a future phase adding an endpoint MUST check the table first; the table lists the router + route strings as its 端点 column so the grep from table to code is direct"
    - from: "README.md known-limits section"
      to: "04-03 (private namespace) and 04-04 (date whitelist) outcomes"
      via: "the 409/422 behaviors and the /v1/private/* namespace are cited as implemented facts (SUMMARY evidence), not future promises"
    - from: "the documented limits"
      to: "SC5 acceptance at the 04-07 gate"
      via: "04-06 SUMMARY records the scan outputs; the 04-07 human-check reviews README content against the running service"
  prohibitions:
    - statement: "The classification table MUST NOT list any /v1/private or /v1/actions endpoint as public and MUST NOT mark any 公开级 row as key-protected (self-consistent with the code served by api/state.py + the routers); the 机密级 rows must name exactly the /v1/private/*, POST /v1/actions/* and GET /v1/jobs/* families"
      category: safety
      requirement_id: SEC-02
      status: unverified
    - statement: "The docs MUST NOT claim limits that the code does not implement (e.g. claiming all entry points are locked, claiming rate limits, claiming ETags) — the known-limits section documents exactly the residual entry points 04-CONTEXT names"
      category: safety
      requirement_id: SEC-02
      status: unverified
    - statement: "No Chinese/English encoding risk: README.md content is ASCII + UTF-8 with no BOM; terminal output during scans stays ASCII"
      category: safety
      requirement_id: SEC-02
      status: unverified
    - statement: "Docs-only diff: no code, tests, config, or runtime changes in this plan; the full pytest suite stays green; data/ and logs/ untouched"
      category: safety
      requirement_id: SEC-02
      status: unverified
---

# Plan 04-06 — Documentation: PROJECT.md data classification table + README known-limits (SC5)

## User-story framing

**As a** future developer or reviewer who needs to add or audit an API endpoint, **I want to** a standing, 定稿-marked classification table in PROJECT.md (which families are public, which are key-protected) and a README known-limits section that states honestly what is and is not protected by the single-flight lock, the boot posture, and the residual manual entry points — **so that** the SC5 documentation clauses are met with byte-truthful references, and no future phase has to reconstruct the classification from memory.

<objective>
Write the two documentation deliverables of SEC-02/SC5: (1) the two-tier data-classification table in `.planning/PROJECT.md` Constraints/Security, stamped with the 定稿 markers and the privacy red-line link; (2) the README known-limits section covering the single-flight scope, the residual concurrent entry points (Mac crontab 无锁/手动 CLI/GUI 弃用), the boot posture (loopback default + env-forced token), the endpoint inventory, and a pointer to the classification. Run the SC5 scans as executable verify checks and record the evidence for the 04-07 gate.

Purpose: the 04-CONTEXT docs policy asks that the classification become standing reference (README 保持克制, PROJECT.md 承担分类), and SC5 requires the README to document known limits. The earlier plans changed runtime behavior (envelope codes, private namespace, date whitelist, boot gate) — this plan records those facts where future planners and users read them, without touching code. The scans (git history, sync_cloud whitelist, .gitignore) are the SC5 acceptance evidence — a scan failure halts and reports; it is never papered over by a docs edit.

Output: `.planning/PROJECT.md` (classification clause added), `README.md` (known-limits section added), SUMMARY with scan evidence.
Decisions: D-23, D-24, D-25, D-20
</objective>

## Flagged Assumptions (spec-less probe fallback — continuation)

- [ASSUMED] PROJECT.md has a Constraints section (per the house PROJECT.md layout of this project; the task reads the file first and appends under the security/constraints clause if present, else creates the clause); README.md exists at repo root with an API section — the exact insertion points are resolved by reading both files first.
- [ASSUMED] The Mac 端 crontab state (无锁) and GUI deprecation (2026-09-04) are as recorded in 04-CONTEXT/WR-02 context; the README wording cites these as known residual entry points without auditing the Mac machine from here (out of scope for a docs task; the 04-07 gate records machine-truth).

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-exposure-hardening-data-classification/04-CONTEXT.md
@.planning/phases/04-exposure-hardening-data-classification/04-PATTERNS.md
@.planning/phases/04-exposure-hardening-data-classification/04-03-SUMMARY.md
@.planning/phases/04-exposure-hardening-data-classification/04-04-SUMMARY.md
@.planning/phases/04-exposure-hardening-data-classification/04-05-SUMMARY.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@README.md
@api/main.py
@scripts/daily/sync_cloud.py
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>PROJECT.md data classification table: two-tier + 定稿 markers + privacy red-line link</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - C:/Users/Davis/Desktop/gogo/.planning/PROJECT.md (whole file — find the Constraints/Security clause and the 定稿 marker conventions already used)
    - C:/Users/Davis/Desktop/gogo/.planning/phases/04-exposure-hardening-data-classification/04-CONTEXT.md (数据分类 decision: 公开级/机密级 rows)
    - C:/Users/Davis/Desktop/gogo/api/main.py + C:/Users/Davis/Desktop/gogo/api/state.py + C:/Users/Davis/Desktop/gogo/api/private.py + C:/Users/Davis/Desktop/gogo/api/actions.py + C:/Users/Davis/Desktop/gogo/api/jobs.py (grep the route decorators to byte-truth the endpoint strings the table lists)
  </read_first>
  <action>
    Add the standing classification table to the Constraints/Security clause of .planning/PROJECT.md:

    1. Verify byte-truth first: grep the route decorators across api/main.py, api/state.py, api/private.py, api/actions.py, api/jobs.py and list the actual prefixes — the table's endpoint column must match what the code serves (the docs gate opens on the grep output, not on memory).
    2. Table shape (two-tier, mirroring 04-CONTEXT):
       - 公开级 (no key): /health, /health/ready, /v1/state/*, /openapi.json — 数据无持仓/无策略暴露面
       - 机密级 (API key required): /v1/private/* (portfolio/journal/candidates), POST /v1/actions/*, GET /v1/jobs/* — 持仓/操作/任务状态
       Rows carry the protection level and the router source so a reader can grep table → code.
    3. Stamp: a 定稿 line noting 用户确认 2026-09-02 + Phase 4 定稿 2026-09-04 (the exact marker shape follows the house convention already present in PROJECT.md — do not invent a new one), plus a one-line pointer to the privacy red line (2026-08-31) and to README known-limits.
    4. Constraints clause placement per the file's existing structure (append under the security/constraints clause; if none exists, create the clause following the file's heading conventions).
    5. No code/README changes in this task. Gate BEFORE staging: run `git diff --stat -- .planning/PROJECT.md` and confirm it shows the edit (the uncommitted diff is the real-edit evidence — once committed, a diff against HEAD is empty by construction). Then stage ONLY .planning/PROJECT.md and commit: `docs(04-06): PROJECT.md 数据分类表 (定稿)`.
  </action>
  <verify>
    <automated>python -m pytest -q</automated>
    <fails_when>non-zero exit, or any failed/error above 0, or "no tests ran" (docs must not break the suite)</fails_when>
    <automated>git show --stat --format="" HEAD</automated>
    <fails_when>output empty or does not list .planning/PROJECT.md (the committed edit must be real — HEAD is this task's commit; the real-edit diff gate ran inside the action BEFORE staging)</fails_when>
    <automated>git show --name-only --format="" HEAD</automated>
    <fails_when>output is not exactly ".planning/PROJECT.md" (specific-path staging violated — the task's commit carries files beyond PROJECT.md)</fails_when>
    <automated>grep -c -E "v1/private|v1/actions|v1/jobs|/health" .planning/PROJECT.md</automated>
    <fails_when>output is 0 (endpoint families must appear in the table)</fails_when>
    <automated>git status --porcelain -- data/ logs/</automated>
    <fails_when>non-empty output (isolation pin)</fails_when>
  </verify>
  <acceptance_criteria>
    - PROJECT.md carries the two-tier table whose endpoint strings match the grep of the actual route decorators; 定稿 markers + privacy red-line pointer present; suite green; docs-only commit
  </acceptance_criteria>
  <done>The classification is standing reference: future endpoint decisions and reviews read the 公开级/机密级 split from PROJECT.md, stamped and linked to the privacy red line.</done>
</task>

<task type="auto">
  <name>README.md known-limits section: single-flight scope + residual entry points + boot posture + SC5 scans</name>
  <files>README.md</files>
  <read_first>
    - C:/Users/Davis/Desktop/gogo/README.md (whole file — find the API section and the established tone/language; keep 克制 per the docs policy)
    - C:/Users/Davis/Desktop/gogo/.planning/phases/04-exposure-hardening-data-classification/04-CONTEXT.md (WR-02 residual entry points; GUI 弃用 2026-09-04; docs policy)
    - C:/Users/Davis/Desktop/gogo/scripts/daily/sync_cloud.py (L25-45 whitelist — SC5 scan source)
    - C:/Users/Davis/Desktop/gogo/.gitignore (token/lock/log coverage)
    - C:/Users/Davis/Desktop/gogo/.planning/phases/04-exposure-hardening-data-classification/04-03-SUMMARY.md and 04-04-SUMMARY.md and 04-05-SUMMARY.md (the implemented facts the limits section cites: /v1/private/* namespace, 409/422 shapes, date whitelist, boot gate)
  </read_first>
  <action>
    Add the known-limits section to README.md (API section, following the existing structure and language):

    1. Single-flight scope: exactly one pipeline/job per time — a second entry returns 409 with the running_job_id (cite the envelope code already_running); what is covered (POST /v1/actions/pipeline via the API) and what is NOT (below).
    2. Residual concurrent entry points, honestly: Mac 端 crontab 调度 (无锁,跨机无法被本机锁约束), 手动 CLI (python scripts/...), GUI 已弃用 (2026-09-04 CLI-only) — the lock guards API entries only; the daily schedule must not overlap the API pipeline (the operational rule 04-CONTEXT records).
    3. Boot posture: loopback default (127.0.0.1); non-loopback binds require GOGO_API_TOKEN in the environment (auto-generated data/api_token.txt does not satisfy it — 04-05); token file location + .gitignore coverage.
    4. Endpoint inventory + classification pointer: the served prefixes (byte-checked against the routers in this task, same grep as Task 1) with a pointer to PROJECT.md's standing table.
    5. Tone: 克制, factual, no promises of rate limits/ETags/concurrency the code does not implement; Chinese where README is Chinese, ASCII console text.
    6. Run the SC5 scans (each is a real check the executor runs now and records):
       - git log --all --oneline -- data/api_token.txt (must be empty — token never committed)
       - grep source of scripts/daily/sync_cloud.py for api_token references (must be absent from the whitelist — whitelist = the data files only)
       - .gitignore coverage of data/api_token.txt + logs (must be present)
       A scan failure is halt-and-report: fix nothing by editing docs; record the failing scan in the SUMMARY and stop.
    7. Commit with specific-path staging: `docs(04-06): README known-limits + SC5 evidence`.
  </action>
  <verify>
    <automated>git log --all --oneline -- data/api_token.txt</automated>
    <fails_when>non-empty output — the token is in git history (SC5 halt condition)</fails_when>
    <automated>grep -n "api_token" scripts/daily/sync_cloud.py</automated>
    <fails_when>any output line — sync_cloud would ship the token to cloud (SC5 halt condition; whitelist is data files only)</fails_when>
    <automated>grep -n -E "api_token|logs" .gitignore</automated>
    <fails_when>no output lines — .gitignore does not cover the token and logs (SC5 halt condition)</fails_when>
    <automated>git status --porcelain -- data/ logs/</automated>
    <fails_when>non-empty output (isolation pin)</fails_when>
  </verify>
  <acceptance_criteria>
    - README known-limits section covers single-flight scope + residual entry points + boot posture + endpoint inventory; SC5 scans all pass and are recorded in the SUMMARY; docs-only commit
  </acceptance_criteria>
  <done>README documents the known limits truthfully and the SC5 scan evidence is captured — the machine gate at 04-07 needs only the live-service checks.</done>
</task>

</tasks>

## Artifacts this phase produces (symbols)

- `.planning/PROJECT.md` (modified) — 数据分类表 + 定稿 markers + privacy red-line pointer
- `README.md` (modified) — known-limits section (single-flight, residual entry points, boot posture, endpoint inventory)

## Source Audit (multi-source coverage)

```
SOURCE    | ID      | Feature/Requirement                        | Plan  | Status
--------- | ------- | ------------------------------------------ | ----- | ------
CONTEXT   | D-01..  | 分类表成为常驻参考 (README 克制/分类进 PROJECT) | 04-06 | COVERED (task 1)
CONTEXT   | WR-02   | 残余并发入口点如实记录                      | 04-06 | COVERED (task 2)
GOAL      | SC5     | README 记录已知限制 + token 不在 git 历史/sync whitelist | 04-06 | COVERED (task 2 scans)
CONTEXT   | 2026-09-04 | GUI 弃用, CLI-only                        | 04-06 | COVERED (task 2 residual list)
REQ       | SEC-02  | 暴露面硬化 — documentation leg             | 04-06 | COVERED (tasks 1-2)
REQ       | STA-02   | private reads (separate)                   | 04-03 | COVERED (sibling plan)
```

Other source rows covered by sibling plans: envelope → 04-01; journal → 04-02; private/audit → 04-03; date → 04-04; boot gate → 04-05; live gate → 04-07. Exclusions (not gaps): ACT-04/05, OPS-03, v2 items, GUI fixes (2026-09-04 CLI-only).

## Assumption-delta decision

No assumption-delta payload was delivered with this plan-phase invocation — the delta scan does not fire. The [ASSUMED] rows above (PROJECT.md/README insertion points; Mac crontab state as recorded) are resolved by reading the files first and by the 04-07 gate recording machine-truth — weakening or silently skipping the SC5 scans is not an option: a scan that fails halts the plan and is reported.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → operator behavior | The README/PROJECT.md claims shape what an operator believes is protected — a false claim (e.g. "all entries locked") is itself a security defect |
| Classification table → future code | Future phases read the table to decide protection levels — a wrong row propagates into a wrongly-exposed endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-20 | Tampering | PROJECT.md classification table | medium | mitigate | Table rows are byte-verified against the route decorators (grep gate in Task 1); 定稿 markers pin the standing status so silent table edits in later phases are reviewable diffs |
| T-04-21 | Spoofing | README known-limits claims | medium | mitigate | The section documents exactly the residual entry points 04-CONTEXT names (Mac crontab 无锁/手动 CLI/GUI 弃用) and never claims locks/limits the code lacks; prohibition row 2 is a literal gate |
| T-04-22 | Information Disclosure | Token leakage via docs/scans | high | mitigate | SC5 scans are executable verifies: git history empty for data/api_token.txt, sync_cloud whitelist free of api_token, .gitignore covers token + logs; a scan failure halts and reports (never doc-papered) |
| T-04-SC | Tampering | package installs | high | mitigate | Zero package installs — the package-legitimacy gate is not triggered (docs-only plan) |

ASVS level 1, blocking threshold high: the disclosure row is the SC5 halt; the other rows are content-truth gates enforced by the greps in <verify>. Package legitimacy: no install commands exist in this plan.
</threat_model>

<verification>
- Task 1: grep of route decorators precedes the table write (byte-truth); suite green; PROJECT.md-only commit.
- Task 2: three SC5 scans execute and pass (git history, sync_cloud grep, .gitignore grep) — any failure halts; README.md-only commit with the scan evidence.
- Hygiene after every task: git status --porcelain -- data/ logs/ empty; specific-path staging.
</verification>

<success_criteria>
- PROJECT.md carries the stamped two-tier classification matching the served routes; README documents the single-flight scope, residual entry points, and boot posture without overclaiming.
- SC5 scans pass with evidence recorded in the SUMMARY; the full suite stays green; only the two docs files are modified.
</success_criteria>

<output>
Create .planning/phases/04-exposure-hardening-data-classification/04-06-SUMMARY.md when done — record the classification table rows as written, the README section content, the raw SC5 scan outputs (git log, sync_cloud grep, .gitignore grep), and any byte-truth corrections found while grepping the routers.
</output>
