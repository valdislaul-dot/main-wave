---
phase: 04-exposure-hardening-data-classification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/errors.py
  - api/main.py
  - tests/test_errors.py
  - tests/test_state.py
  - tests/test_auth.py
  - tests/test_actions.py
autonomous: true
requirements:
  - STA-02
  - SEC-02
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every 4xx/5xx response across the api/ surface carries the unified body shape {\"detail\": <unchanged>, \"code\": <stable machine-readable code>} — 401 missing_api_key + WWW-Authenticate: ApiKey, 403 invalid_api_key, 404 family, 409 already_running / already_running_other_entry (object detail unchanged, code as top-level sibling), 503 family, framework 405 method_not_allowed, framework 422 validation_error and unhandled-500 internal_error — implemented by app-level exception handlers registered in api/main.py near app construction, never by editing raise sites (04-CONTEXT 暴露面硬化清单: 机器可读错误码; P2 UI-review Top-3 item 1; SC3)"
    - "404 copy is single-sourced API-wide: every 404 — handler-raised or framework route-miss — returns detail exactly \"Not Found\" with the semantic difference carried by code (unknown_state_name / unknown_action_kind / job_not_found / unknown_private_name / not_found) (04-CONTEXT: 404 文案单一定稿; P2 UI-review item 2)"
    - "The code table lives in api/errors.py and is complete and frozen at this plan: every raise-site text the phase will produce (including the private-endpoint texts unknown private name and private data temporarily unavailable, and the date-parameter text date must be YYYY-MM-DD or YYYYMMDD) already has a row, so later plans never edit the table; unknown detail texts fall back to http_{status_code}, and no handler ever echoes paths, tokens, or exception internals into a body (SC3, D-04)"
    - "Tracebacks stay server-side: an unhandled exception prints to console (stderr, no token/path) and the response is 500 {\"detail\": \"internal server error\", \"code\": \"internal_error\"} — existing pins asserting \"Traceback\" not in r.text keep passing on every error path"
    - "/openapi.json is served (docs_url stays None): the schema reflects every registered route incl. /v1/state/*, /v1/actions/*, /v1/jobs/*, /health, /health/ready; GET /openapi.json returns 200 application/json without a key and is a documented public-classification route (04-CONTEXT: openapi schema 同步更新; P2 UI-review item 3)"
    - "The two P2-review test-fidelity defects in tests/test_state.py are fixed: the WR-02 hammer test no longer passes vacuously (writer exceptions captured and asserted empty, a per-iteration progress counter asserted >= 1, GETs overlapping a real torn window) and the WR-03 dot-segment pin asserts the server truth — a raw-ASGI-scope request with the literal un-normalized path /v1/state/market_state/../auction_state returns 404, with a comment documenting that the TestClient 200 variant is an httpx pre-transport collapse artifact (02-REVIEW.md WR-02/WR-03, 04-CONTEXT P2 遗留纳入)"
    - "python -m pytest -q stays green at 97+ passed with the envelope contract suite added, git status --porcelain -- data/ logs/ stays empty, and the pin sweep updates error-body assertions without weakening the zero-side-effect, no-spawn-on-rejection, or traceback-absence assertions they accompany"
  artifacts:
    - path: "api/errors.py"
      provides: "The frozen text-keyed code table (CODE_BY_DETAIL + codes for the dict-shaped 409s and framework classes) and the three app-level exception-handler callables (HTTPException → unified body with code; RequestValidationError → validation_error; Exception → internal_error) — pure stdlib/fastapi imports, no import side effects, ASCII-only console text"
      contains: "CODE_BY_DETAIL, error handlers, JSONResponse builder"
    - path: "api/main.py"
      provides: "add_exception_handler registrations beside the app constructor (L27 wiring region) and openapi_url=\"/openapi.json\" on the FastAPI() call — docs_url/redoc_url stay None; include_router lines, /health and the whole main() boot sequence byte-untouched (Phase 1/2 diff-auditable payload)"
      contains: "add_exception_handler x3, openapi_url"
    - path: "tests/test_errors.py"
      provides: "The envelope contract suite: per-status body-shape pins (detail + code keys in that order), unified-404 byte pin across handler-raised and framework route-miss classes, 405/422/500 handlers, fallback-code pin for an unknown-text 404, traceback-stays-server-side pin, /openapi.json 200 + schema route smoke"
      contains: "TestClient, autouse tmp isolation fixture, route-miss + unhandled-500 exercises"
    - path: "tests/test_state.py"
      provides: "Updated error-body pins (L102/127/204/214/225/238) to the envelope shape; WR-02 hammer rewritten with exception capture + progress counter + overlap barrier; WR-03 dot-segment pin replaced by the raw-ASGI 404 assertion"
    - path: "tests/test_auth.py"
      provides: "Updated error-body pins (L113/118/127/143/162/187/192/223) to the envelope shape"
    - path: "tests/test_actions.py"
      provides: "Updated error-body pins (L187-192/202/411-414/432/436/444/459 incl. the 409 whole-body equalities) to the envelope shape"
  key_links:
    - from: "api/errors.py handlers"
      to: "api/main.py"
      via: "app.add_exception_handler(StarletteHTTPException/RequestValidationError/Exception, ...) registered at module level beside the FastAPI() constructor so the module-level TestClient(app) suites exercise the real envelope (03-02 main.py wiring convention)"
    - from: "handler code mapping"
      to: "raise sites"
      via: "text-keyed table lookup on exc.detail for string details (404 texts are read before the copy is unified to Not Found so the per-site code can be derived), shape-keyed for the 409 object detail (running_job_id present → already_running, absent → already_running_other_entry) — raise sites in api/state.py/api/auth.py/api/actions.py stay byte-unchanged"
    - from: "pin sweep"
      to: "tests/test_state.py + tests/test_auth.py + tests/test_actions.py"
      via: "each whole-body equality pin becomes the full envelope shape {\"detail\": ..., \"code\": ...} (404 details unified to Not Found; non-404 details byte-identical); 409 pins add the code sibling while preserving the object detail exactly"
    - from: "/openapi.json"
      to: "route docstrings"
      via: "route docstrings are the schema source once openapi_url is enabled — the docstrings already exist on state/actions routes; the executor only verifies the schema renders (later plans 04-03/04-04 add private/date routes with docstrings documenting codes + date formats)"
  prohibitions:
    - statement: "Raise sites MUST NOT be edited for the envelope: api/state.py, api/auth.py, api/actions.py keep their exact detail texts and raise shapes — the mapping is text-keyed/shape-keyed in api/errors.py and applied by the registered handlers only"
      category: safety
      requirement_id: SEC-02
      status: unverified
    - statement: "Non-404 error bodies MUST keep their pinned detail bytes byte-identical (401 missing API key + WWW-Authenticate: ApiKey, 403 invalid API key, 409 object details, 503 texts) — only the new code sibling is added; the unified-404 copy change is scoped to the 404 class exactly"
      category: safety
      requirement_id: SEC-02
      status: unverified
    - statement: "No error body or console line MAY ever contain a file path, the token value, or exception internals (SC3, D-04) — handlers map by table lookup and the unhandled-500 handler prints the traceback to stderr server-side only; the code table is complete and frozen in this plan so later plans never append rows"
      category: safety
      requirement_id: SEC-02
      status: unverified
    - statement: "The pin sweep MUST NOT weaken the assertions it updates: zero-side-effect checks (registry dir stays empty), no-spawn-on-rejection, decoy guards, \"Traceback\" not in r.text and str(tmp_path) not in error-body checks all keep their force after the shape update"
      category: safety
      requirement_id: SEC-02
      status: unverified
    - statement: "api/main.py changes are confined to the app-constructor wiring region (L27 area): the include_router lines, /health, the SEC-03 boot block and main() ordering stay byte-untouched — later plans (04-03 include, 04-05 boot) edit main.py in later waves"
      category: safety
      requirement_id: SEC-02
      status: unverified
    - statement: "WR-02/WR-03 are test-fidelity fixes in tests/test_state.py only — no production code in api/ or scripts/ changes for them, and the raw-ASGI WR-03 pin asserts 404 (the real uvicorn/Starlette routing answer), never resurrecting the httpx-normalized 200 assertion"
      category: safety
      requirement_id: SEC-02
      status: unverified
---

# Plan 04-01 — Unified machine-readable error envelope + /openapi.json + P2 test-fidelity fixes (WR-02/WR-03)

## User-story framing

**As a** consumer who automates against gogo's API (cloud panel, scheduled integrator, the user off-machine), **I want to** branch on stable machine-readable error `code` values instead of parsing prose, see one 404 vocabulary whether the miss was a route or a resource, and get the service's own schema over HTTP — **so that** my error handling never breaks on a copy change and the wire contract is self-describing, while server tracebacks stay out of every response body.

<objective>
Deliver the response-surface foundation of Phase 4: `api/errors.py` (the frozen text-keyed/shape-keyed error-code table + three app-level exception handlers returning `{"detail": <unchanged>, "code": <stable>}` on every 4xx/5xx, 404 copy unified API-wide to `Not Found` with per-site codes), the `api/main.py` wiring (handler registration beside the app constructor + `openapi_url="/openapi.json"`, docs_url stays None), the new envelope contract suite `tests/test_errors.py`, the mandatory error-body pin sweep across `tests/test_state.py` / `tests/test_auth.py` / `tests/test_actions.py` (which 04-CONTEXT 04-PATTERNS line 99 lists as mandatory), and the two P2-review test-fidelity fixes in `tests/test_state.py` (WR-02 vacuous-pass hammer + WR-03 httpx-normalized dot-segment pin) — all in Wave 1 so every later plan pins the final body shapes once.

Purpose: the P2 UI-review Top-3 items were user-signed into 04-CONTEXT 暴露面硬化清单 (机器可读错误码 + 双 404 文案归一 + openapi 同步更新, with WR-02/WR-03 纳入本阶段). The envelope changes the shape of every error the phase touches, so it must land first and freeze the code table; 04-03 (private endpoints) and 04-04 (date params) then pin final shapes against a stable surface instead of double-sweeping pins.

Output: `api/errors.py`, `api/main.py` (wiring only), `tests/test_errors.py`, pin-swept `tests/test_state.py`/`tests/test_auth.py`/`tests/test_actions.py` incl. the WR-02/WR-03 fixes, green full suite.
Decisions: D-18, D-19, D-20
</objective>

## Flagged Assumptions (spec-less probe fallback — continuation)

- STA-02 / SEC-02 (`unclassified` probes — no scenario clause to honor in REQUIREMENTS.md): the phase's success criteria SC3 (path-free/traceback-free error bodies) is delivered as a uniform envelope because every status class in the api/ raises or falls through to shared framework machinery — the envelope is the only place a uniform shape can be enforced without touching raise sites. The 04-CONTEXT decisions list pins the body-shape policy (统一 `{"detail": ...}` 风格 D-04 基调 + 4xx/5xx 带机器可读错误码字段 + 404 文案单一定稿); the code-value names and the single 404 copy (`Not Found`) are planner discretion, executed here and pinned in this plan's suite so later plans consume them as contract.
- [ASSUMED] FastAPI/Starlette dispatch routes both handler-raised `fastapi.HTTPException` and framework-generated HTTPExceptions (route-miss 404, method 405) through a handler registered for `starlette.exceptions.HTTPException` — if the runtime proves otherwise (a framework 404 bypassing the handler), the executor falls back to registering the handler for both exception classes and records it; the contract under test (every 404 body unified) does not change.
- [ASSUMED] A registered `Exception` handler makes TestClient return the 500 JSON body instead of raising `raise_server_exceptions` — the contract tests assert bodies, never rely on raised exceptions.
- [ASSUMED] `/openapi.json` renders cleanly from the existing route docstrings/type hints (fastapi 0.115.14 machine-verified); if schema generation raises on a route, the executor fixes the docstring/annotation on that route only, never disables openapi again.

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-exposure-hardening-data-classification/04-CONTEXT.md
@.planning/phases/04-exposure-hardening-data-classification/04-PATTERNS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-read-only-state-endpoints-defensive-read-layer/02-UI-REVIEW.md
@.planning/phases/02-read-only-state-endpoints-defensive-read-layer/02-REVIEW.md
@.planning/phases/03-trigger-runner-job-registry-locks-auth-enforcement/03-02-SUMMARY.md
@api/main.py
@api/state.py
@api/auth.py
@api/actions.py
@tests/test_state.py
@tests/test_auth.py
@tests/test_actions.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>api/errors.py code table + three exception handlers + api/main.py wiring (openapi_url enabled) driven by the tests/test_errors.py envelope contract suite</name>
  <reversibility rating="costly">Every error response of the live API changes shape (adds code; 404 copy unifies) — user-signed in 04-CONTEXT (机器可读错误码 + 404 文案单一定稿 + openapi 同步更新) and reversible in-suite by pin rollback, but it is the phase's widest wire-contract change, so it lands first and is pinned before any later plan builds on it.</reversibility>
  <files>api/errors.py, api/main.py, tests/test_errors.py</files>
  <read_first>
    - 04-PATTERNS.md api/errors.py section (raise-site inventory with texts, code examples, dual-404 design, pin-site line list) — C:/Users/Davis/Desktop/项目/gogo/.planning/phases/04-exposure-hardening-data-classification/04-PATTERNS.md
    - 02-UI-REVIEW.md Top-3 items — C:/Users/Davis/Desktop/项目/gogo/.planning/phases/02-read-only-state-endpoints-defensive-read-layer/02-UI-REVIEW.md
    - C:/Users/Davis/Desktop/项目/gogo/api/main.py (whole file: L27 FastAPI() constructor with docs_url=None/redoc_url=None/openapi_url=None, include_router lines L36-37, boot main() L46-81 — the exact wiring region)
    - C:/Users/Davis/Desktop/项目/gogo/api/state.py lines 83-100 (raise-site + Response/headers shape), C:/Users/Davis/Desktop/项目/gogo/api/auth.py lines 33-50 (401/403 raise sites + challenge header)
    - C:/Users/Davis/Desktop/项目/gogo/tests/test_state.py lines 27-47 (module-level TestClient + autouse fixture convention), C:/Users/Davis/Desktop/项目/gogo/tests/test_actions.py lines 37-59 (write_token/registry helpers)
  </read_first>
  <behavior>
    - Test 1 (401/403 envelope): POST /v1/actions/pipeline with no key → 401 body == {"detail": "missing API key", "code": "missing_api_key"} and WWW-Authenticate: ApiKey; wrong key → 403 {"detail": "invalid API key", "code": "invalid_api_key"} and no challenge header.
    - Test 2 (404 unification): GET /v1/state/unknown_name (handler-raised) → 404 body == {"detail": "Not Found", "code": "unknown_state_name"}; POST /v1/actions/nonsense → {"detail": "Not Found", "code": "unknown_action_kind"}; GET /v1/jobs/<32-hex missing> → {"detail": "Not Found", "code": "job_not_found"}; GET /v1/nonexistent_route (framework route-miss) → {"detail": "Not Found", "code": "not_found"} — the 404 detail bytes are identical across all four; only code differs.
    - Test 3 (405/422/500 handlers): POST /health (method not allowed) → 405 with code method_not_allowed and detail "Method Not Allowed"; GET /v1/state/ with a declared-invalid type trigger... use POST /v1/actions/pipeline with an unknown body-free surface or a route declaring a query param — the framework RequestValidationError path (e.g. an int-typed query on a route that has one) → 422 {"detail": [<starlette list>], "code": "validation_error"}; an unhandled exception path (a test-only route registered in the suite app via app.add_api_route that raises RuntimeError) → 500 {"detail": "internal server error", "code": "internal_error"} and "Traceback" not in the body.
    - Test 4 (fallback + shape order): a suite-registered route raising HTTPException(404, "some unmapped text") → 404 {"detail": "Not Found", "code": "http_404"} (text-keyed miss falls back to the status-class code); the JSON response body keys appear in order "detail" then "code".
    - Test 5 (traceback server-side): the unhandled-exception route from Test 3 prints a traceback containing the exception name to console (capsys on the TestClient call path) while the body stays path-free and token-free.
    - Test 6 (openapi): GET /openapi.json → 200, application/json, body has "openapi" and "paths" keys whose paths include /health, /health/ready, /v1/state/{name}, /v1/actions/{kind}, /v1/jobs/{job_id}.
  </behavior>
  <action>
    Write tests/test_errors.py first (the six behaviors above as the envelope contract suite, module docstring in Chinese naming 04-CONTEXT 暴露面硬化清单 + SC3 + P2 UI-review items) and watch it fail red (today there is no code field and openapi is disabled), then implement api/errors.py and wire api/main.py until green.

    1. api/errors.py — module docstring in Chinese; ASCII-only console text; imports: os (not needed unless logging helpers), sys, traceback for the unhandled handler, fastapi (HTTPException, Request), fastapi.responses.JSONResponse, starlette.exceptions.HTTPException as StarletteHTTPException, starlette.requests.Request as StarletteRequest if needed for typing. Zero import side effects. Public surface:
       - `CODE_BY_DETAIL` — the frozen, complete mapping table (text-keyed for string details): "missing API key" → missing_api_key; "invalid API key" → invalid_api_key; 404-origin texts are mapped BEFORE the copy unification: "unknown state name" → unknown_state_name, "unknown action kind" → unknown_action_kind, "job not found" → job_not_found, "unknown private name" → unknown_private_name (raised by 04-03, row present from today per the freeze rule), "Not Found" → not_found (framework route-miss; also the unified copy text — mapping must never match the framework row when the original text is one of the handler texts); "Method Not Allowed" → method_not_allowed (framework route-method miss — the framework 405 detail passes through unchanged because only the 404 copy is unified; Test 3 pins this row, so it must exist in the table rather than falling back to http_405); "state temporarily unavailable" → state_temporarily_unavailable; "state file unavailable" → state_file_unavailable; "private data temporarily unavailable" → private_data_unavailable (04-03 raise text, row frozen today); "job temporarily unavailable" → job_temporarily_unavailable; "date must be YYYY-MM-DD or YYYYMMDD" → invalid_date_format (04-03/04-04 raise text, row frozen today); "date not supported for this action kind" → date_not_supported (04-04 raise text, row frozen today).
       - The 409 rows are shape-keyed, not text-keyed (kind names vary): dict detail containing "running_job_id" → already_running; dict detail without it → already_running_other_entry. Dict details pass through byte-unchanged as {"detail": <dict>, "code": ...}.
       - `_code_for(status, detail)` — returns CODE_BY_DETAIL.get(detail) for str details, the shape-keyed 409 code for dict details with "message", else f"http_{status}" (status-class fallback). Never raises, never echoes detail internals beyond the standard body.
       - `async def http_exception_handler(request, exc)` — handles StarletteHTTPException (which covers fastapi.HTTPException raise sites AND framework 404/405): original_detail = exc.detail; code = _code_for(exc.status_code, original_detail); if exc.status_code == 404 and isinstance(original_detail, str): body_detail = "Not Found" (unified copy) else body_detail = original_detail; content = {"detail": body_detail, "code": code} (key order exactly this); return JSONResponse(status_code=exc.status_code, content=content, headers=exc.headers).
       - `async def validation_error_handler(request, exc)` — RequestValidationError: return JSONResponse(422, {"detail": exc.errors() or the standard list, "code": "validation_error"}) — the detail list passes through as starlette renders it.
       - `async def unhandled_exception_handler(request, exc)` — Exception: print ASCII-only marker line + traceback to stderr server-side (traceback.print_exc, never the request headers/path/token), return JSONResponse(500, {"detail": "internal server error", "code": "internal_error"}).
    2. api/main.py wiring — exactly: (a) import the three handlers from api.errors beside the existing imports; (b) on the FastAPI() call change openapi_url=None to openapi_url="/openapi.json" (docs_url=None and redoc_url=None stay); (c) three app.add_exception_handler lines directly after the FastAPI() constructor, before include_router lines: StarletteHTTPException → http_exception_handler, RequestValidationError → validation_error_handler, Exception → unhandled_exception_handler. Nothing else in the file moves — the include_router lines, /health and the whole main() boot sequence stay byte-untouched (later plans own them).
    3. Order of work: tests red → implementation green → run the file suite → run the full suite (the pre-existing whole-body pins will fail here BY DESIGN — the sweep is Task 2's job; if Task 1's green gate is the file suite plus the untouched suites minus the known pin deltas, proceed to Task 2 immediately rather than half-sweeping pins in Task 1). Commit with specific-path staging: `feat(04-01): machine-readable error envelope + openapi schema` for api/ + `test(04-01): envelope contract suite` for tests/test_errors.py — never a blanket add; data/ and logs/ must stay untouched.
  </action>
  <verify>
    <automated>python -m pytest tests/test_errors.py -q</automated>
    <fails_when>non-zero exit, or any failed/error above 0, or 0 tests collected — red means a body-shape drift (check the exact {"detail": ..., "code": ...} bytes incl. the unified 404 copy), a 404/405/422/500 handler mis-dispatch, or openapi still disabled</fails_when>
    <automated>git status --porcelain -- data/ logs/</automated>
    <fails_when>non-empty output — the isolation pin failed</fails_when>
  </verify>
  <acceptance_criteria>
    - api/errors.py exists with the frozen CODE_BY_DETAIL table (all rows listed above incl. the private/date texts), the shape-keyed 409 split, the status-class fallback, and the three handler callables; zero import side effects; ASCII-only console text
    - api/main.py has exactly the import + openapi_url change + three add_exception_handler lines in the L27 wiring region; everything else byte-untouched
    - tests/test_errors.py's six behaviors green: envelope shapes for 401/403/404 (unified copy, four per-site codes)/405/422/500, fallback http_404, detail-then-code key order, traceback server-side only, /openapi.json 200 with all phase routes listed
    - python -m pytest tests/test_errors.py -q exits 0; git status --porcelain -- data/ logs/ empty; specific-path staging
  </acceptance_criteria>
  <done>The unified envelope contract is implemented and pinned: every error class in the api/ surface renders {"detail": ..., "code": ...} with one 404 vocabulary, tracebacks never reach a body, and /openapi.json serves the schema — with the pre-existing suites' only failures being the known pin-shape deltas Task 2 sweeps.</done>
</task>

<task type="auto">
  <name>Error-body pin sweep across tests/test_state.py, tests/test_auth.py, tests/test_actions.py to the envelope shape (incl. the 409 whole-body pins)</name>
  <files>tests/test_state.py, tests/test_auth.py, tests/test_actions.py</files>
  <read_first>
    - 04-PATTERNS.md api/errors.py section pin-update list — C:/Users/Davis/Desktop/项目/gogo/.planning/phases/04-exposure-hardening-data-classification/04-PATTERNS.md
    - C:/Users/Davis/Desktop/项目/gogo/tests/test_state.py (whole file: error pins at L102/127/204/214/225/238 with their surrounding assertions — keep the accompanying zero-side-effect/decoy/traceback-absence force intact)
    - C:/Users/Davis/Desktop/项目/gogo/tests/test_auth.py (whole file: pins at L113/118/127/143/162/187/192/223)
    - C:/Users/Davis/Desktop/项目/gogo/tests/test_actions.py (whole file: pins at L187-192/202/411-414/432/436/444/459)
    - C:/Users/Davis/Desktop/项目/gogo/api/errors.py (the frozen table from Task 1 — the sweep maps 1:1 onto it)
  </read_first>
  <action>
    Sweep every error-body assertion in the three suites onto the Task 1 envelope shape, preserving each surrounding assertion's force:

    1. Mapping rules: 401/403/503 pins → append the code key from the frozen table, detail bytes untouched — e.g. {"detail": "job temporarily unavailable", "code": "job_temporarily_unavailable"}. 404 pins → detail becomes the unified copy "Not Found" and the code comes from the ORIGINAL text: "unknown state name" → unknown_state_name, "unknown action kind" → unknown_action_kind, "job not found" → job_not_found; framework route-miss 404s (tests/test_actions.py:440-450 class) → keep status-only + no-Traceback assertions and add one body pin {"detail": "Not Found", "code": "not_found"} where a body is asserted. 409 whole-body pins (tests/test_actions.py:322-324 map-hit and 411-414 another-entry-point) → the dict detail stays byte-identical and the code sibling is added: {"detail": {"message": "pipeline already running", "running_job_id": id1}, "code": "already_running"} and {"detail": {"message": "pipeline already running (another entry point)"}, "code": "already_running_other_entry"} — keep the "running_job_id" not in body["detail"] assertion at L414.
    2. Site list (exact): tests/test_state.py pins at L102 (404 unknown state name), L127 (503 unavailable path), L204, L214, L225, L238; tests/test_auth.py pins at L113 (401 missing), L118 (401 missing on jobs route if present), L127 (403 invalid), L143, L162, L187, L192 (401/403 matrix), L223; tests/test_actions.py pins at L187-188 (401), L192 (403), L202 (404 unknown action kind), L322-324 (409 map-hit whole body), L411-414 (409 other-entry whole body), L432 (404 unknown action kind), L436 (404 job not found), L444 (404 malformed job ids loop), L459 (503 job temporarily unavailable).
    3. Discipline: this sweep only changes error-body assertions — the accompanying assertions (registry dir stays empty at L203/451, WWW-Authenticate header presence/absence, no-challenge-header on 403, "Traceback" not in r.text, decoy guards, str(tmp_path) never in bodies) are untouched and must keep passing. Where a pin compares against r.json() whole-body equality, prefer the full-shape equality (dict equality is order-insensitive; if a response.text byte pin exists anywhere, update it to the detail-then-code order from Task 1).
    4. Also extend one framework-404 body pin per suite (or one centrally in test_errors.py — do not duplicate) so the route-miss class is covered in the suites that previously pinned only handler 404s; do not add new endpoint coverage here (that is 04-03/04-04's job).
    5. Gate: run the three swept suites, then the full suite — python -m pytest -q must be fully green (97 passed + the new envelope tests, 0 failed) — then hygiene + commit with specific-path staging: `test(04-01): sweep error-body pins to the code-field envelope`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_state.py tests/test_auth.py tests/test_actions.py tests/test_errors.py -q</automated>
    <fails_when>non-zero exit, or any failed/error above 0 — a red pin means the sweep mapped a wrong code (check the frozen table), left a stale whole-body equality, or accidentally weakened an accompanying zero-side-effect/no-Traceback assertion</fails_when>
    <automated>python -m pytest -q</automated>
    <fails_when>non-zero exit, or any failed/error above 0, or "no tests ran" — the full-suite regression gate: Phase 1/2/3 suites all green on the new envelope</fails_when>
    <automated>git status --porcelain -- data/ logs/</automated>
    <fails_when>non-empty output (isolation pin)</fails_when>
  </verify>
  <acceptance_criteria>
    - Every pin at the listed sites asserts the full envelope shape with the frozen code value; 404 detail bytes unified to "Not Found"; 409 object details byte-identical with the code sibling; no accompanying assertion weakened
    - python -m pytest -q exits 0 with the envelope suite included; git status --porcelain -- data/ logs/ empty; specific-path staging used
  </acceptance_criteria>
  <done>The whole pre-existing error-pin surface speaks the envelope: every suite asserts detail + code, the 404 copy is unified, the 409 shapes carry their codes, and the full repo suite is green again.</done>
</task>

<task type="auto">
  <name>tests/test_state.py P2 fidelity fixes: WR-02 writer-hammer rewritten (exception capture + progress counter + real torn window) and WR-03 dot-segment pin moved to a raw-ASGI 404 assertion</name>
  <files>tests/test_state.py</files>
  <read_first>
    - 02-REVIEW.md WR-02 and WR-03 defect specs — C:/Users/Davis/Desktop/项目/gogo/.planning/phases/02-read-only-state-endpoints-defensive-read-layer/02-REVIEW.md
    - C:/Users/Davis/Desktop/项目/gogo/tests/test_state.py lines 100-135 (WR-03 pin + comment region) and 260-331 (hammer + source-scan region) — read both regions before editing
    - 04-PATTERNS.md tests/test_state.py section (prescribed fix shapes) — C:/Users/Davis/Desktop/项目/gogo/.planning/phases/04-exposure-hardening-data-classification/04-PATTERNS.md
  </read_first>
  <action>
    Fix the two test-fidelity defects in tests/test_state.py (production code untouched — both are test bugs):

    1. WR-02 hammer (writer_loop region ~L290-315): the loop swallows any non-OSError exception, so a dead writer thread makes the test pass vacuously as a static-file smoke test and the x-data-stale branch is never exercised. Rewrite: (a) writer exceptions are captured into a shared thread-safe list (append in an except clause around the loop body) and the test asserts the list is empty after both threads join — a writer crash now fails the test; (b) add a per-iteration progress counter the writer increments each successful rewrite and assert it is >= 1 after the run — proving the writer actually wrote; (c) force the GETs to overlap a real torn window (a threading.Barrier or an initial writer sleep so the first rewrite is mid-flight when the reader loop starts) and keep the hammer's duration bounded so the suite stays fast. The 0×5xx assertion and the stale-branch pin (body equals last-good bytes with the cached real mtime, X-Data-Stale: true) keep their force.
    2. WR-03 dot-segment pin (~L106-115): class (c) currently asserts GET /v1/state/market_state/../auction_state returns 200 — an httpx/TestClient URL-normalization artifact; real uvicorn (curl --path-as-is) gets a framework 404 and the handler never runs, so the 200 pin misdocuments the wire contract. Replace it with the server truth: build a raw-ASGI scope dict whose "path" is the literal "/v1/state/market_state/../auction_state" (same method/host/scheme/query_string keys TestClient would produce; include "type": "http", "headers": [] or the minimal header list), invoke app(scope, receive, send) under asyncio with stub receive/send callables, capture the start-message status, and assert 404. Add the repo-comment (mirroring tests/test_actions.py:440-450's note) that the TestClient 200 variant exists only because httpx collapses ".." pre-transport and that the raw-ASGI assertion is the wire truth. Class (b) encoded shapes (..%2F..) keep their status-only 404 pins unchanged.
    3. Gate: run tests/test_state.py, then the full suite, then hygiene + commit with specific-path staging: `test(04-01): fix WR-02 hammer vacuous pass + WR-03 dot-segment pin to raw-ASGI 404`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_state.py -q</automated>
    <fails_when>non-zero exit, or any failed/error above 0 — red means the hammer still passes vacuously (writer exception capture/progress counter missing), the raw-ASGI scope was built wrong (not a genuine 404), or an envelope pin regressed</fails_when>
    <automated>python -m pytest -q</automated>
    <fails_when>non-zero exit, or any failed/error above 0, or "no tests ran" (full-suite regression gate)</fails_when>
    <automated>git status --porcelain -- data/ logs/</automated>
    <fails_when>non-empty output (isolation pin)</fails_when>
  </verify>
  <acceptance_criteria>
    - The WR-02 hammer fails on a writer crash (exception list asserted empty), proves real writes (progress >= 1), and overlaps readers with a torn window — no longer a vacuous pass
    - The WR-03 dot-segment class (c) pin asserts 404 via a raw-ASGI scope with the literal un-normalized path, with the httpx-collapse comment; class (b) pins unchanged
    - python -m pytest -q exits 0; git status --porcelain -- data/ logs/ empty; specific-path staging used
  </acceptance_criteria>
  <done>The two P2-review fidelity defects are closed: the hammer can no longer pass on a dead writer, and the dot-segment contract pin asserts what real uvicorn answers (404), not what httpx normalizes away.</done>
</task>

</tasks>

## Artifacts this phase produces (symbols)

- `api/errors.py` (new path) — `CODE_BY_DETAIL` (frozen text-keyed table incl. future private/date rows), shape-keyed 409 split (`already_running` / `already_running_other_entry`), `_code_for(status, detail)` with `http_{status}` fallback, `http_exception_handler` (404 copy unification), `validation_error_handler`, `unhandled_exception_handler`
- `api/main.py` (modified, L27 wiring region only) — `from api.errors import ...`, `openapi_url="/openapi.json"`, three `app.add_exception_handler(...)` lines
- `tests/test_errors.py` (new path) — envelope contract suite (six behaviors), module-level TestClient + autouse tmp isolation + suite-registered probe routes for the 405/fallback/unhandled classes
- `tests/test_state.py` (modified) — envelope pins; WR-02 hammer with exception capture/progress/overlap; WR-03 raw-ASGI 404 pin
- `tests/test_auth.py` (modified) — envelope pins
- `tests/test_actions.py` (modified) — envelope pins incl. the two 409 whole-body shapes

## Source Audit (multi-source coverage)

```
SOURCE    | ID      | Feature/Requirement                        | Plan  | Status
--------- | ------- | ------------------------------------------ | ----- | ------
GOAL      | SC3     | Error responses: no paths, no tracebacks (server logs only) | 04-01 | COVERED (tasks 1-2; handlers + no-Traceback pins)
CONTEXT   | 硬化清单 | 机器可读错误码字段 (P2 UI-review Top-3 #1)  | 04-01 | COVERED (tasks 1-2)
CONTEXT   | 硬化清单 | 404 文案单一定稿 (P2 UI-review Top-3 #2)     | 04-01 | COVERED (tasks 1-2)
CONTEXT   | 硬化清单 | openapi schema 同步更新 (P2 UI-review #3)    | 04-01 | COVERED (task 1)
CONTEXT   | 硬化清单 | P2 遗留 WR-02/WR-03 修正 (02-REVIEW specs)   | 04-01 | COVERED (task 3)
CONTEXT   | 硬化清单 | 日志硬化审计规则 (job 子进程日志无 key)      | 04-03 | COVERED (sibling plan 04-03 Task 3)
REQ       | STA-02   | 读端点 raw 透传 + 新鲜度头 (new surface)     | 04-03 | COVERED (sibling plan)
REQ       | SEC-02   | 分级鉴权表 + 逐路由审计                      | 04-03 | COVERED (sibling plan)
CONTEXT   | 机密级   | /v1/private/* 端点 (敏感端点形态)            | 04-03 | COVERED (sibling plan)
CONTEXT   | D-04..D-06 | 写侧原子化 (STA-02 上线前置)              | 04-02 | COVERED (sibling plan)
CONTEXT   | WR-01/D-12 | env 强制 token + 控制台警告               | 04-05 | COVERED (sibling plan)
CONTEXT   | D-09 扩展 | date 白名单参数 (SC4)                       | 04-04 | COVERED (sibling plan)
CONTEXT   | 分级文档  | PROJECT.md 分级表 + README 已知限制 (SC5)    | 04-06 | COVERED (sibling plan)
CONTEXT   | 真机 gate | 实况验证                                    | 04-07 | COVERED (sibling plan)
```

Explicit exclusions (not gaps): ACT-04/ACT-05 job cancel/orphan adoption (REQUIREMENTS v2), OPS-03 log rotation + /health/details (Phase 5), ETag/限流/CORS + pipeline digest (v2), 15:30 task 存废 (Phase 5), GUI fixes (GUI deprecated 2026-09-04, zero work items), PUT/PATCH status mutation (定稿机制 excluded).

## Assumption-delta decision

No assumption-delta payload was delivered with this plan-phase invocation — the delta scan does not fire; flagged assumptions above ([ASSUMED] handler dispatch, TestClient 500 behavior, openapi render) are the plan's own and each carries a runtime-fallback instruction for the executor.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Consumer → API | Untrusted HTTP crosses here; every 4xx/5xx body rendered by the registered handlers — bodies must never echo paths/tokens/exception internals |
| Server console | The unhandled-exception handler prints tracebacks to stderr — the only place internals may surface; console text stays ASCII and token-free |
| /openapi.json | New public route serving the schema — route shapes only, no data, no tokens; must join the public classification tier (SC2 audit in 04-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | error envelope handlers | high | mitigate | Text-keyed/shape-keyed mapping only — bodies carry the frozen detail copy + code, never exc internals, paths, or the token; unhandled exceptions go to stderr via traceback.print_exc with an ASCII marker, body is the fixed internal_error shape (SC3, D-04); every suite pins "Traceback" not in r.text and str(tmp_path) never in bodies |
| T-04-02 | Tampering | openapi schema exposure | low | mitigate | openapi_url serves route shapes only (no data/token); the schema route joins the public tier of the 04-03 classification audit; docs_url stays None so no interactive UI is served |
| T-04-03 | Information Disclosure | unified-404 copy regression | low | mitigate | The unification is copy-level only — per-site semantics move into code values pinned in tests/test_errors.py (unknown_state_name/unknown_action_kind/job_not_found/not_found) so a consumer branching on code never parses prose; codes are frozen in api/errors.py and swept across all three suites |
| T-04-04 | Denial of Service | handler mis-dispatch (unmapped text) | low | mitigate | `http_{status}` fallback keeps every 4xx/5xx well-formed even for an unmapped detail — a raise-site text added later can never produce a malformed body, and the frozen-table rule keeps CODE_BY_DETAIL authoritative |
| T-04-SC | Tampering | package installs | high | mitigate | Zero package installs in this plan — the package-legitimacy gate is not triggered (stdlib + installed fastapi 0.115.14 stack only) |

ASVS level 1, blocking threshold high: the high rows map to concrete tests in this plan (Task 1 behaviors 1-6, Task 2 pin sweep, Task 3 fidelity fixes). The fail-closed boot check (WR-01/D-12) test coverage belongs to 04-05; the SC2 route-by-route audit belongs to 04-03.
</threat_model>

<verification>
- Task 1: python -m pytest tests/test_errors.py -q green — envelope shapes, unified 404, 405/422/500 handlers, fallback code, traceback server-side, /openapi.json 200 with phase routes.
- Task 2: python -m pytest tests/test_state.py tests/test_auth.py tests/test_actions.py tests/test_errors.py -q green, then python -m pytest -q full-suite green (97 passed + envelope tests, 0 failed) — the sweep left no stale whole-body pin.
- Task 3: python -m pytest tests/test_state.py -q green (hammer + raw-ASGI pins), full suite green.
- Hygiene after every task: git status --porcelain -- data/ logs/ empty; specific-path staging for every commit.
- Per-task sample rate: quick suite after every task commit; full suite at the plan gate (04-VALIDATION.md Sampling Rate).
</verification>

<success_criteria>
- Every 4xx/5xx response in the api/ surface renders {"detail": <unchanged or unified-404 copy>, "code": <frozen code>} — verified by the envelope suite and the swept pins in all three pre-existing suites (SC3).
- 404s speak one copy ("Not Found") API-wide with per-site machine codes; non-404 details byte-identical to their pinned texts (04-CONTEXT 硬化清单).
- Unhandled exceptions produce the fixed 500 body with tracebacks only on the server console; /openapi.json serves the route schema without a key.
- WR-02/WR-03 fidelity defects closed with production code untouched; full pytest suite green; data/ and logs/ untouched.
</success_criteria>

<output>
Create .planning/phases/04-exposure-hardening-data-classification/04-01-SUMMARY.md when done — record the final code table (every detail text → code), the handler registration lines with pre-edit line numbers from api/main.py, the exact unified-404 body bytes, any runtime deviation from the [ASSUMED] dispatch behavior, the swept pin-site list, and full-suite outcomes.
</output>
