---
phase: 04-exposure-hardening-data-classification
plan: 07
type: execute
wave: 5
depends_on:
  - 04-05
  - 04-06
files_modified:
  - .planning/phases/04-exposure-hardening-data-classification/04-07-SUMMARY.md
autonomous: true
requirements:
  - STA-02
  - SEC-02
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The real machine serves the hardened API: a scheduled-service restart (Start-ScheduledTask gogo-api) boots the 04-01/03/05 main.py, and live HTTP probes return the envelope bodies and classification behavior — private endpoints 200 with the real data/api_token.txt and byte-verbatim raw bodies + X-Data-Mtime/X-Data-Age-S headers (STA-02 SC1 live), 401 without a key, 403 with a wrong key, public endpoints open without a key, live date-format 422s (SC4 live)"
    - "The live matrix exercises only read/health endpoints and the 401/403/422 negative paths — no real pipeline or morning-check is ever triggered live (a real capture would write data/ and is not a verification artifact; only the verify-only health-check precedent of D-02 applies)"
    - "SC5 evidence is complete on the real machine: the 04-06 scan set re-runs here (git history empty for data/api_token.txt, sync_cloud whitelist free of api_token, .gitignore coverage) plus the live checks — the service is left running with the loopback posture and the token file intact for the user's next trading session"
    - "The machine-truth check on the flagged assumptions runs: the launch path really binds 127.0.0.1 (run_api.bat + Task Scheduler), and no file-token non-loopback consumer exists — if the live restart contradicts this (e.g. the scheduled task binds 0.0.0.0 and now refuses to boot without GOGO_API_TOKEN), the gate HALTS and reports to the user instead of weakening the gate"
    - "The final full pytest suite runs on the real machine and stays green with no failures above 0"
    - "The phase closes with the human review gate: the user reviews the five success criteria (SC1-SC5) against the SUMMARY evidence and confirms acceptance or files a delta — the plan's final task carries the <human-check> element, harvested at end-of-phase (human_verify_mode=end-of-phase per .planning/config.json), not a mid-plan halt"
  artifacts:
    - path: ".planning/phases/04-exposure-hardening-data-classification/04-07-SUMMARY.md"
      provides: "The live-gate evidence log: restart command + outcome, live matrix table (each probe: URL/method/auth → status + body bytes + headers), SC5 scan re-runs, assumption-truth results, full-suite output, service state at close — the single artifact the human gate reviews against SC1-SC5"
      contains: "live matrix, SC5 evidence, assumption truths, suite output"
  key_links:
    - from: "the live matrix probes"
      to: "the running service on the real machine"
      via: "http://127.0.0.1:8000 URLs with the real data/api_token.txt (read from the file into the X-API-Key header — api/auth.py:39 reads X-API-Key only; never echoed into output beyond that header)"
    - from: "the human gate"
      to: "the SUMMARY evidence"
      via: "the user reads 04-07-SUMMARY.md and accepts SC1-SC5 or files a delta — the exact 03-04 gate shape (this is the 04 phase twin of the Phase 3 live gate: <human-check> inside the final task's <verify>)"
  prohibitions:
    - statement: "No real pipeline or morning-check run is ever triggered live — a real capture writes data/ and is not a verification artifact; the only live job execution allowed is the verify-only health-check precedent of D-02 (and none at all when it would collide with the market-hours rule of the 15:30 task window)"
      category: safety
      requirement_id: STA-02
      status: unverified
    - statement: "The token value never appears in captured output or the SUMMARY — requests send it only in the X-API-Key header (api/auth.py:39 reads X-API-Key; an Authorization: Bearer probe is a 401 by contract); the response/error text pins never include it"
      category: safety
      requirement_id: SEC-02
      status: unverified
    - statement: "If the restart contradicts the flagged assumptions (non-loopback bind refusing, launcher drift), the gate HALTS and reports to the user — weakening the D-12 env-only gate or adding a GOGO_API_TOKEN to a launcher without user sign-off is not an option"
      category: safety
      requirement_id: SEC-02
      status: unverified
    - statement: "Hygiene holds through the gate: git status --porcelain -- data/ logs/ empty after every task; specific-path staging; the service is left RUNNING (never killed at the end of the gate)"
      category: safety
      requirement_id: SEC-02
      status: unverified
---

# Plan 04-07 — Live gate on the real machine (SC1-SC5) + human review checkpoint

## User-story framing

**As a** trader who will run this API tomorrow morning, **I want to** the hardened service verified on the machine that actually serves it — real token, real files, real restart — with every probe logged and my five success criteria reviewed against that evidence — **so that** the phase closes on machine-truth, not on a synthetic suite.

<objective>
Run the Phase 04 live gate on the real machine: restart the scheduled service, probe the live matrix (private/public/envelope/date behaviors), re-run the SC5 scans on the real repo, verify the flagged assumptions hold on this machine, run the full suite, and close with the human review gate over the SUMMARY evidence (SC1-SC5). This plan modifies no code — its only artifact is the SUMMARY that the human gate reviews.

Purpose: the phase checklist's last leg — every earlier plan proved behavior in the synthetic suite; only a live probe of the actual service proves the wiring (scheduled-task boot of the edited api/main.py, real token file, real data files, real headers) and closes the D-08/loopback assumption chain. The human gate mirrors 03-04 exactly (its final task carries `<human-check>` inside `<verify>`, harvested at end-of-phase per `.planning/config.json` `human_verify_mode: end-of-phase`): acceptance is a user decision at phase close, never a mid-plan halt.

Output: `04-07-SUMMARY.md` (the evidence log), a running service, user acceptance or a filed delta.
Decisions: D-21
</objective>

## Flagged Assumptions (spec-less probe fallback — continuation — verified HERE, not inherited)

- The 04-05 [ASSUMED] rows reach their truth test in this plan: (a) the launch path (run_api.bat + Task Scheduler) binds the loopback default — Task 1 observes the restarted service's actual bind and boot outcome; (b) no file-token + non-loopback consumer exists — if the restart fails because the scheduled task binds non-loopback and the env token is absent, the gate halts and reports (the D-12 gate itself is not weakened).
- The 04-CONTEXT market-hours rule for real pipeline runs (never during the 15:30 task window overlap) applies — this plan never triggers a real run anyway (prohibition row 1), so the rule is satisfied vacuously.
- [ASSUMED] Task Scheduler task name "gogo-api" exists as recorded in the run_api.bat/04-PATTERNS context — Task 1 lists the scheduled tasks first (schtasks /query) and adapts the restart command to the found name, halting if no task exists at all.

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-exposure-hardening-data-classification/04-CONTEXT.md
@.planning/phases/04-exposure-hardening-data-classification/04-PATTERNS.md
@.planning/phases/04-exposure-hardening-data-classification/04-01-SUMMARY.md
@.planning/phases/04-exposure-hardening-data-classification/04-02-SUMMARY.md
@.planning/phases/04-exposure-hardening-data-classification/04-03-SUMMARY.md
@.planning/phases/04-exposure-hardening-data-classification/04-04-SUMMARY.md
@.planning/phases/04-exposure-hardening-data-classification/04-05-SUMMARY.md
@.planning/phases/04-exposure-hardening-data-classification/04-06-SUMMARY.md
@.planning/phases/04-exposure-hardening-data-classification/03-04-SUMMARY.md
@.planning/ROADMAP.md
@.planning/STATE.md
@run_api.bat
@api/main.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Restart the resident service onto the phase code + live matrix probes (SC1/SC4 live) — needs a non-sandboxed session for Start-ScheduledTask</name>
  <files>.planning/phases/04-exposure-hardening-data-classification/04-07-SUMMARY.md</files>
  <read_first>
    - 03-04-SUMMARY.md restart block (Start-ScheduledTask gogo-api primary, run_api.bat fallback, port-8000 owner-pid stop) — C:/Users/Davis/Desktop/gogo/.planning/phases/03-trigger-runner-job-registry-locks-auth-enforcement/03-04-SUMMARY.md
    - C:/Users/Davis/Desktop/gogo/run_api.bat (repo-root launch entry), C:/Users/Davis/Desktop/gogo/api/main.py (the code generation under test)
    - 04-PATTERNS.md main.py WR-01/boot section (the branch the restart exercises) — C:/Users/Davis/Desktop/gogo/.planning/phases/04-exposure-hardening-data-classification/04-PATTERNS.md
  </read_first>
  <action>
    BEFORE probing: this task's commands (Start-ScheduledTask / schtasks / curl to the live port) need a non-sandboxed Bash session on the real machine — if the sandbox blocks them, STOP and ask the user to approve a non-sandboxed session or run the restart manually; never simulate the restart.

    1. Restart: schtasks /query /tn gogo-api first (name-adapt per the flagged assumption); if a listener already holds port 8000 with OLD code, stop it via the 03-04 owner-pid method; then Start-ScheduledTask /tn gogo-api; poll http://127.0.0.1:8000/health until 200 (bounded ~60s); record the boot outcome. If the boot FAILS because the launcher binds non-loopback without GOGO_API_TOKEN → HALT and report to the user (the D-12 gate is working as designed; the launcher needs a signed delta, not a gate weakening). If the bind is loopback as assumed, proceed — this also confirms assumption-truth (a).
    2. Live matrix — read-only probes ONLY (never trigger a real pipeline/morning-check; prohibition row 1). For each row, record status + body bytes + the response headers into the SUMMARY table:
       - GET /health and /health/ready with no key → 200 (public)
       - GET /v1/state/* (the state family the running service serves) with no key → 200 (public per the classification table)
       - GET /openapi.json → 200 (schema present — envelope-era openapi_url live)
       - GET /v1/private/portfolio with the real token (read data/api_token.txt and send it as the X-API-Key header — api/auth.py:39 reads X-API-Key only, so an Authorization: Bearer probe would be a 401 by contract; never print the token) → 200 + body byte-identical to data/portfolio.json + X-Data-Mtime/X-Data-Age-S present (SC1 live)
       - GET /v1/private/journal with the real token (X-API-Key header) → 200 + byte-identical raw body (SC1 live)
       - GET /v1/private/candidates (latest by default per 04-03) with the real token (X-API-Key header) → 200 + raw-body identity against the newest candidates_*.json the code selects
       - GET /v1/private/portfolio with NO key → 401 with the envelope body {code, detail} (D-10 live); with a WRONG key → 403 envelope body
       - GET /v1/private/unknown-name with the real token (X-API-Key header) → 404 envelope with the per-site code (unknown_private_name) and the unified "Not Found" copy (D-10/404 unification live)
       - GET /v1/private/candidates?date=2026-99-99 with the real token (X-API-Key header) → 422 envelope (invalid_date_format — SC4 live); if the date param is not on the running candidates route (04-03 divergence), record what the service actually does
       - POST /v1/actions/pipeline WITHOUT triggering a run: a dry 401/403 probe only (no auth → 401 envelope). NEVER a successful POST that starts a real job — the 409 shape is already pinned in the suite (04-01); the live gate does not start jobs.
    3. Record every row into 04-07-SUMMARY.md as it runs (the file is this plan's only artifact; build it progressively with Write/Edit, never heredoc). Token value appears nowhere in the file.
  </action>
  <verify>
    <automated>python -c "import urllib.request;t=open('data/api_token.txt').read().strip();f=urllib.request.Request('http://127.0.0.1:8000/v1/private/portfolio',headers={'X-API-Key':t});print(urllib.request.urlopen(f).status)"</automated>
    <fails_when>exit non-zero or status != 200 (the private read path is not live) — this is an automated live probe of the already-restarted service, not a real capture; the token is read from the file into the X-API-Key header (api/auth.py reads X-API-Key only, per D-10) and never printed</fails_when>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8000/health</automated>
    <fails_when>output != 200 (service not healthy after restart)</fails_when>
    <automated>git status --porcelain -- data/ logs/</automated>
    <fails_when>non-empty output (isolation pin — no live probe may write data/)</fails_when>
  </verify>
  <acceptance_criteria>
    - Service restarted and healthy on loopback; the live matrix table is fully recorded in the SUMMARY (status + body identity + headers per row); private 200/401/403/404, public 200, date 422 behaviors observed live; no real job ever started; data/ logs/ clean
  </acceptance_criteria>
  <done>The restarted service answers the live probes as recorded in the SUMMARY matrix — the phase code boots from the real scheduled task and serves the hardened contract on the loopback posture.</done>
</task>

<task type="auto">
  <name>SC5 re-run on the real repo + assumption-truth record + full suite + service left running</name>
  <files>.planning/phases/04-exposure-hardening-data-classification/04-07-SUMMARY.md</files>
  <read_first>
    - C:/Users/Davis/Desktop/gogo/scripts/daily/sync_cloud.py (L25-45 whitelist — SC5 scan source), C:/Users/Davis/Desktop/gogo/.gitignore (token/lock/log coverage)
    - 04-06-SUMMARY.md scan-output rows (the baseline the real-machine re-run must match) — C:/Users/Davis/Desktop/gogo/.planning/phases/04-exposure-hardening-data-classification/04-06-SUMMARY.md
  </read_first>
  <action>
    Continue the SUMMARY evidence log (Task 1 wrote the live matrix; this task appends the remaining evidence):

    1. SC5 re-run on the real machine (the 04-06 scan set, executed here as machine-truth):
       - git log --all --oneline -- data/api_token.txt → must be empty
       - grep -n "api_token" scripts/daily/sync_cloud.py → must have no hits (whitelist = data files only)
       - grep -n -E "api_token|logs" .gitignore → must show coverage
       Record raw outputs into the SUMMARY.
    2. Assumption-truth rows: (a) launch path bound loopback — observed from Task 1's boot outcome; (b) no file-token non-loopback consumer — the service boots only if true; (c) scheduled-task name resolution. Record each as TRUE or HALT-with-reason.
    3. Full suite on the real machine: python -m pytest -q → record pass counts; the run may take the baseline ~13s plus the added files — it is a local verification run, not a real pipeline.
    4. Leave the service RUNNING (the user's next session needs it; do not stop the scheduled task). Verify health one last time and record the final state.
    5. Hygiene: git status --porcelain -- data/ logs/ empty; stage only the SUMMARY file. Commit: `docs(04-07): live gate SUMMARY (SC1-SC5 evidence)`.
  </action>
  <verify>
    <automated>git log --all --oneline -- data/api_token.txt</automated>
    <fails_when>non-empty output (SC5 halt)</fails_when>
    <automated>grep -n "api_token" scripts/daily/sync_cloud.py</automated>
    <fails_when>any output line (SC5 halt)</fails_when>
    <automated>python -m pytest -q</automated>
    <fails_when>non-zero exit, or any failed/error above 0, or "no tests ran"</fails_when>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8000/health</automated>
    <fails_when>output != 200 (the service must be left running and healthy)</fails_when>
    <automated>git status --porcelain -- data/ logs/</automated>
    <fails_when>non-empty output (isolation pin)</fails_when>
  </verify>
  <acceptance_criteria>
    - SC5 scans pass with raw outputs recorded; assumption-truth rows recorded; full suite green on the real machine; service running and healthy; SUMMARY committed
  </acceptance_criteria>
  <done>The SUMMARY holds the complete SC1-SC5 evidence set; the machine state is the hardened service, running, with the repo clean.</done>
</task>

<task type="auto">
  <name>Close-out + SUMMARY completion + user SC1-SC5 review walkthrough (end-of-phase human gate, mirroring 03-04)</name>
  <files>.planning/phases/04-exposure-hardening-data-classification/04-07-SUMMARY.md</files>
  <read_first>
    - 03-04-SUMMARY.md close-out block (final machine state + user walkthrough record shape) — C:/Users/Davis/Desktop/gogo/.planning/phases/03-trigger-runner-job-registry-locks-auth-enforcement/03-04-SUMMARY.md
    - C:/Users/Davis/Desktop/gogo/.planning/phases/04-exposure-hardening-data-classification/04-CONTEXT.md (success criteria SC1-SC5 wording)
  </read_first>
  <action>
    Close the phase gate:

    1. Final hygiene review: python -m pytest -q already green (Task 2); git status --porcelain shows ONLY the intended phase 04 tracked changes (api/errors.py, api/private.py, api/main.py, scripts/daily/date_args.py, scripts/daily/run_pipeline.py, scripts/daily/morning_check.py, scripts/daily/trading_journal.py, README.md, .planning/PROJECT.md, tests/*, .planning/phases/04-* files) — anything else gets investigated before close.
    2. Complete 04-07-SUMMARY.md: the restart record + live matrix (Task 1), SC5 raw outputs + assumption-truth rows + suite count + final machine state (Task 2), and the mapping of each success criterion to its evidence rows (SC1 → matrix private rows; SC2 → classification audit suite count + boot observation; SC3 → envelope probe bodies; SC4 → date 422 row; SC5 → scan outputs + README limits reference).
    3. Close with the user: present the five success criteria with the live evidence for each and ask for the review verdict — ACCEPT closes the phase; a DELTA files the gap (what failed, expected vs observed) for the orchestrator's gap-closure routing. Alongside the verdict, present the 04-04 fail-loud session-date semantics for explicit sign-off (04-04 documented it as planner discretion; it is unsigned until this walkthrough): run_pipeline.py / morning_check.py refuse any format-valid --date that is not the local session date with an ASCII message + exit 2 before any capture/write/network — so the API date param is deliberately inert for non-today dates by design until the user defines richer historical semantics. Record the verdict + date + the date-gate sign-off at the bottom of the SUMMARY file.
  </action>
  <verify>
    <automated>python -m pytest -q</automated>
    <fails_when>non-zero exit, or any failed/error above 0, or "no tests ran" (phase-gate full suite)</fails_when>
    <automated>git status --porcelain</automated>
    <fails_when>output lists tracked files beyond the intended phase-04 set (unexpected modifications must be investigated, not closed over)</fails_when>
    <human-check>The user reviews the five success criteria against the live evidence in 04-07-SUMMARY.md (each SC maps to SUMMARY rows per step 2) and answers ACCEPT or DELTA with the reason; the verdict is recorded in the SUMMARY. The walkthrough ALSO asks the user to sign the 04-04 fail-loud session-date semantics (documented planner discretion in 04-04 Flagged Assumptions, previously unsigned): a format-valid --date that is not the local session date makes run_pipeline.py / morning_check.py print an ASCII refusal and exit 2 before any capture/write/network, so the API date param is deliberately inert for non-today dates until the user defines richer historical semantics — the explicit sign-off is recorded beside the verdict. This is the end-of-phase human gate (human_verify_mode=end-of-phase) — the executor records the outcome and does not auto-accept.</human-check>
  </verify>
  <acceptance_criteria>
    - python -m pytest -q exits 0; git status --porcelain lists only the intended phase-04 tracked set; data/ logs/ untouched
    - 04-07-SUMMARY.md is complete: restart record, live matrix, SC5 raw outputs, assumption-truth rows, suite count, final machine state, SC→evidence mapping
    - The user verdict (ACCEPT or DELTA with reason) is recorded at the bottom of the SUMMARY, together with the explicit sign-off on the 04-04 fail-loud session-date semantics (format-valid non-today --date → ASCII refusal + exit 2; the API date param is inert for non-today dates by design); the phase closes on that verdict
  </acceptance_criteria>
  <done>The Phase 04 gate closes on a user verdict over the SUMMARY evidence — acceptance is the user's signature, not an automated assumption; a DELTA verdict routes the orchestrator's gap-closure pass.</done>
</task>

</tasks>

## Artifacts this phase produces (symbols)

- `.planning/phases/04-exposure-hardening-data-classification/04-07-SUMMARY.md` — live matrix, SC5 evidence, assumption truths, suite output, human verdict
- No production code files are created or modified by this plan — it is the phase's real-machine evidence gate (identical in kind to 03-04)

## Source Audit (multi-source coverage)

```
SOURCE    | ID      | Feature/Requirement                        | Plan  | Status
--------- | ------- | ------------------------------------------ | ----- | ------
GOAL      | SC1     | live token-gated reads + raw bodies + headers | 04-07 | COVERED (task 1 matrix)
GOAL      | SC2     | fail-closed boot check — live boot observation | 04-07 | COVERED (task 1 restart + task 2 assumption rows)
GOAL      | SC3     | error responses path-free/traceback-free live | 04-07 | COVERED (task 1 envelope probes)
GOAL      | SC4     | date whitelist live 422                        | 04-07 | COVERED (task 1 matrix)
GOAL      | SC5     | real-machine scans + README limits              | 04-07 | COVERED (task 2 scans; 04-06 sibling for content)
REQ       | STA-02   | private reads live                             | 04-07 | COVERED (task 1)
REQ       | SEC-02   | exposure hardening live                        | 04-07 | COVERED (tasks 1-2)
CONTEXT   | 03-04    | human gate precedent (phase twin)              | 04-07 | COVERED (task 3)
```

All other source rows covered by sibling plans 04-01..04-06. Exclusions (not gaps): real pipeline/morning-check executions (prohibited live — suite-pinned only), 15:30-window runs (market-hours rule), ACT-04/05, OPS-03, GUI items.

## Assumption-delta decision

No assumption-delta payload was delivered with this plan-phase invocation — the delta scan does not fire. The flagged assumptions of 04-05 (launch path loopback; no file-token non-loopback consumer) and this plan (task name gogo-api) reach their machine-truth test inside Task 1/2 — the recorded outcomes are TRUE rows or a HALT with a user-facing report; the D-12 gate itself is never weakened.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator session → live service | The gate runs real HTTP probes and a service restart on the machine that serves the trading API — commands here are the highest-privilege actions of the phase |
| Live service → data files | Read-only probes must never write data/ — the isolation pin guards the real portfolio/journal/candidates archives |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-23 | Tampering | Restart of the live service | medium | mitigate | Restart is the ONLY state-changing action, executed via the real scheduled task with the run_api.bat fallback (03-04 method); the D-12 boot failure mode is pre-planned as HALT-and-report, never a silent workaround |
| T-04-24 | Spoofing | Wrong-key/absent-key probes against live data | low | mitigate | Negative probes read only envelope bodies (401/403); no private body is served without the real token; the token travels only in the X-API-Key header (api/auth.py reads X-API-Key only, D-10) and never appears in captured output or the SUMMARY |
| T-04-25 | Information Disclosure | Token in SUMMARY/live outputs | high | mitigate | Prohibition row 2 is a literal gate: requests read data/api_token.txt into the header only; every captured byte is checked for the token value before the SUMMARY commit; automated probes print status codes, not headers |
| T-04-26 | Elevation of Privilege | Real job trigger via live probe | critical | mitigate | Prohibition row 1: no real pipeline/morning-check run is ever triggered live; the matrix's only POST is the no-auth 401 probe; the 409/already_running shape stays suite-pinned (04-01) — the live gate never starts a job |
| T-04-SC | Tampering | package installs | high | mitigate | Zero package installs — the package-legitimacy gate is not triggered (probes use stdlib/curl) |

ASVS level 1, blocking threshold high: the critical row (real job trigger) is a hard prohibition with no live exception; the disclosure row is enforced by the token-absence checks on captured output. Package legitimacy: no install commands exist in this plan.
</threat_model>

<verification>
- Task 1: service healthy after restart; live matrix recorded with status + body identity + headers per row; no job triggered.
- Task 2: SC5 scans pass on the real repo; full suite green; service left running and healthy; SUMMARY committed.
- Task 3: git status matches the intended phase-04 tracked set; SUMMARY complete with the SC→evidence mapping; user verdict recorded.
- Full phase: all five success criteria carry live-machine evidence; human gate outcome recorded at the bottom of the SUMMARY.
</verification>

<success_criteria>
- The restarted service answers the live matrix exactly as the suite predicted (200/byte-identity/401/403/404/422 shapes live).
- SC5 scans and the full suite pass on the real machine; the service is left running; the SUMMARY is complete with the SC→evidence mapping.
- The user reviews the SUMMARY against SC1-SC5 and records ACCEPT or a DELTA — the phase closes only on that verdict.
</success_criteria>

<output>
The SUMMARY file IS the output of this plan (04-07-SUMMARY.md). After the human verdict is recorded, the phase closes: the orchestrator marks Phase 4 done in ROADMAP.md (ACCEPT) or routes a gap-closure pass (DELTA).
</output>
