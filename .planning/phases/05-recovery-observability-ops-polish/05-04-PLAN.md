---
phase: 05-recovery-observability-ops-polish
plan: 04
type: execute
wave: 4
depends_on:
  - 05-01
  - 05-02
  - 05-03
files_modified:
  - api/log_housekeep.py
  - api/main.py
autonomous: true
requirements:
  - OPS-03
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The rotation dance is proven on the REAL machine under the real launch path: with no running/pending job in the registry (precondition scan) the gogo-api scheduled task is stopped, logs/api/console.log is inflated past 5MB, the task is started, boot completes (GET /health 200), console.log.1 exists holding the pre-rotation bytes at the inflated size, and the fresh console.log carries uvicorn's post-boot INFO banner lines — NOT console.log.1 — proving uvicorn's stderr landed on the re-pointed stream (uvicorn.run boots with access_log=False at main.py:108, so the banner is the post-repoint fresh-file write evidence — there are no per-request access lines to observe) (D-32/M-A; 03-04 adapt-and-record precedent if the real cmd semantics differ)"
    - "GET /health/details live matrix passes on the real service: no key → 401 with the missing_api_key envelope + WWW-Authenticate header; wrong key → 403 invalid_api_key, no challenge; valid key (read from data/api_token.txt) → 200 with the exact D-29 shape — versions populated from the live environment, uptime_seconds an int >= 0, health_job a parseable ISO timestamp (the registry holds >= 1 succeeded health-check job — 4 present at planner time), market_state_mtime a parseable ISO — while GET /health stays 200 public with its exact body (OPS-03 SC1 live)"
    - "The full pytest suite passes on the real machine at the phase gate: 157+ passed with 1 env-conditional skip (exact count recorded in the SUMMARY; baseline 148 from Phases 1-4 plus the 05-01/05-02 additions), the conftest network-blocking fixture proving no test touches the network by construction (SC3), data/ and logs/ hygiene clean after the gate"
    - "The end-of-phase human review is delivered: a checklist the operator runs against the live outputs (live /health/details curl bodies, rotation evidence, D-36 record, the flagged [ASSUMED] verdicts — each ACCEPT or DELTA, and the Mac checklist handoff) with the review outcome recorded in the 05-04 SUMMARY (03-04/04-07 end-of-phase gate twin; human_verify_mode = end-of-phase)"
    - "No suite test ever touches the real console.log or the real registry — the live manipulation happens only in this real-machine gate, on the actual service, with evidence captured and the inflated console.log.1 removed afterward"
  artifacts:
    - path: "api/log_housekeep.py"
      provides: "Contingency-only: if the live gate exposes a real-semantics mismatch (e.g. cmd handle sharing differs from the machine-verified model), the adapt-and-record fix lands here (03-04 precedent) with the delta recorded in the SUMMARY"
      contains: "rotate_console_log/repoint_std_streams fixes if adapted"
    - path: "api/main.py"
      provides: "Contingency-only: same adapt-and-record path for the boot dance ordering if the live evidence requires it"
      contains: "housekeeping block fixes if adapted"
    - path: ".planning/phases/05-recovery-observability-ops-polish/05-04-SUMMARY.md"
      provides: "The phase-gate record: live matrix outputs verbatim (curl bodies + status codes), rotation evidence (file sizes before/after, .1 existence, append target proof), D-36 cross-reference, final suite count, flagged-assumption verdicts (ACCEPT/DELTA) and the human-review checklist + outcome — the Phase 5 closing document (03-04/04-07 twin)"
      contains: "live evidence, suite counts, human-review checklist"
  key_links:
    - from: "real service restart"
      to: "rotation proof"
      via: "the cmd-scheduled launch path (Task Scheduler → run_api.bat >> logs/api/console.log 2>&1) is the exact environment the M-A dance exists for — only a stop/start of the real task exercises the inherited-handle semantics; the service must be confirmed stopped before inflating and restarted via the task (not a manual python launch) for the evidence to mean anything"
    - from: "post-boot uvicorn stderr banner"
      to: "stream re-point proof"
      via: "uvicorn.run runs with access_log=False (main.py:108) so no per-request line ever appears — uvicorn's boot INFO banner ('Uvicorn running on http://127.0.0.1:8000' / 'Application startup complete.' on uvicorn.error → fd 2, emitted after the housekeeping block) is the first post-second-repoint stderr write: the fresh console.log contains the banner while console.log.1's size stays exactly the inflated pre-rotation size (static) — that contrast is the byte-level proof the second repoint landed the streams on the fresh file"
    - from: "05-04-SUMMARY.md"
      to: "05-01/05-02/05-03 SUMMARYs + 05-PATTERNS flagged rows"
      via: "the human-review checklist itemizes every flagged [ASSUMED]/prohibition row across the four plans for ACCEPT/DELTA verdicts — the phase closes only when each carries a recorded verdict"
  prohibitions:
    - statement: "The live gate MUST NOT run while a pipeline/morning-check/backtest/health-check job is running or pending (registry precondition scan first — restarting mid-job would sweep a live run to interrupted) and MUST NOT run in the 9:15-9:35 auction window on a trading day (the operator's decision output depends on the service; if execution lands there, wait or reschedule the restart portion)"
      category: safety
      requirement_id: OPS-03
      status: unverified
    - statement: "The service restart happens through the REAL scheduled task (Stop-ScheduledTask/Start-ScheduledTask on gogo-api), never through a parallel second instance — a second boot would sweep the primary's registry under the single-writer assumption"
      category: safety
      requirement_id: OPS-03
      status: unverified
    - statement: "No suite test inflates/truncates the real console.log — live manipulation happens only in this gate; the inflated console.log.1 is removed after evidence capture so the machine is left with a small fresh console.log and no junk history"
      category: safety
      requirement_id: OPS-03
      status: unverified
    - statement: "If the live evidence contradicts the dance model, fix + record in place (03-04 adapt-and-record), then re-run the affected unit suites — never paper over the live result in the SUMMARY"
      category: safety
      requirement_id: OPS-03
      status: unverified
    - statement: "The token never appears in any captured output or record (D-05/T-04-18): curl responses and SUMMARY excerpts contain body shapes and status codes, never the X-API-Key value; key reads happen into shell variables, not transcripts"
      category: safety
      requirement_id: OPS-03
      status: unverified
---

# Plan 05-04 — Real-machine phase gate: live rotation proof + /health/details live matrix + final SC3 suite + end-of-phase human review (03-04/04-07 twin)

## User-story framing

**As an** operator who trusts only live evidence, **I want to** watch the real service rotate a >5MB console log on a real task restart, curl /health/details through all three auth outcomes against the running process, and see the final full suite pass on this machine — **so that** the bounded-footprint claim (SC2) and the auth-gated details endpoint (SC1) are proven on the actual launch path, not just in tmp_path tests.

<objective>
The phase's real-machine gate (03-04/04-07 twin): restart the gogo-api scheduled task with a pre-inflated console.log and capture byte-level rotation evidence (console.log.1 holds the old bytes, post-boot request bytes land in the fresh console.log); run the /health/details live matrix (401 no-key, 403 wrong-key, 200 valid-key with the exact D-29 shape) against the running service; run the final full suite (157+ passed, 1 env-conditional skip — exact count recorded); then deliver the end-of-phase human-review checklist covering the four plans' flagged [ASSUMED] rows (ACCEPT/DELTA verdicts), the D-36 record, and the Mac checklist handoff, recorded in 05-04-SUMMARY.md — the Phase 5 closing document.

Purpose: OPS-03 SC1/SC2/SC3 all close on live evidence on the real machine, with the adapt-and-record precedent available if the real launch semantics differ from the machine-verified model.

Output: live evidence + 05-04-SUMMARY.md (the phase gate record incl. human-review checklist); contingency code fix only if adaptation is required.
Decisions: D-32 (live proof), D-36 (cross-ref), [ASSUMED] verdict collection across plans 05-01..05-04
</objective>

## Flagged Assumptions (spec-less probe fallback — surfaced explicitly)

- [ASSUMED] The service is currently running under the gogo-api scheduled task (Phase 4 gate left it RUNNING) and the registry holds only terminal pairs (5 pairs at planner time — 4 succeeded health-check, none running/pending). The gate's precondition scan re-verifies both before any stop.
- [ASSUMED] The real cmd `>>` handle semantics match the machine-verified model (inherited handle without FILE_SHARE_DELETE). If the live rotation evidence contradicts it (rename still fails, or bytes land in .1), the adapt-and-record branch fixes the dance in code and re-runs the unit suites — the SUMMARY records the delta either way.
- [ASSUMED] The final suite lands at 157+ passed / 1 env-conditional skip (CONTEXT specifics); the exact count is recorded, not assumed. If it lands lower, the SUMMARY records the delta and the gate flags it for the human review instead of silently accepting.
- [ASSUMED] Executing outside the 9:15-9:35 auction window on trading days; if the window is unavoidable, the restart portion waits and only the read-only matrix + suite portions run (both are safe while the service runs).

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-recovery-observability-ops-polish/05-CONTEXT.md
@.planning/phases/05-recovery-observability-ops-polish/05-PATTERNS.md
@.planning/phases/05-recovery-observability-ops-polish/05-01-SUMMARY.md
@.planning/phases/05-recovery-observability-ops-polish/05-02-SUMMARY.md
@.planning/phases/05-recovery-observability-ops-polish/05-03-SUMMARY.md
@api/log_housekeep.py
@api/main.py
@run_api.bat
</context>

<tasks>

<task type="auto">
  <name>Live rotation proof on the real scheduled task: precondition scan → stop → inflate >5MB → start → byte-level evidence (.1 holds old bytes, fresh console.log receives post-boot writes)</name>
  <files>api/log_housekeep.py, api/main.py</files>
  <read_first>
    - C:/Users/Davis/Desktop/gogo/api/jobs.py (registry read helpers — the precondition scan needs read-only status checks; no writes)
    - C:/Users/Davis/Desktop/gogo/run_api.bat (the real launch path being exercised: python -m api.main >> logs\api\console.log 2>&1)
  </read_first>
  <action>
    Run the live rotation proof end-to-end on the real machine:

    1. Preconditions (read-only, each verified before proceeding): (a) the gogo-api task is registered — Get-ScheduledTask gogo-api; (b) registry scan shows ZERO running/pending jobs (any non-terminal job → abort and report, never restart mid-run); (c) not in the 9:15-9:35 auction window on a trading day (if so, defer the restart portion).
    2. Stop the service: Stop-ScheduledTask -TaskName gogo-api; confirm the process exited (task state / port 8000 probe refused). Record console.log size before inflation.
    3. Inflate past the 5MB threshold: append ~6MB of bytes to logs/api/console.log via a python one-liner (open append; no shell redirect) and record the new size.
    4. Start the service: Start-ScheduledTask -TaskName gogo-api; poll GET /health until 200 (boot completed, timeout ~60s).
    5. Evidence capture (each recorded verbatim in the SUMMARY): (a) console.log.1 exists and its size equals the inflated pre-rotation size (the old bytes moved, one generation); (b) console.log exists fresh with a small size; (c) the stream re-point proof: record console.log and console.log.1 sizes, then grep the fresh console.log for uvicorn's post-boot INFO banner ('Uvicorn running on http://127.0.0.1:8000' or 'Application startup complete.') — uvicorn runs with access_log=False (main.py:108), so no per-request access line is ever written; the banner uvicorn.error emits right after startup IS the first post-second-repoint stderr write, and its presence in the fresh console.log (with console.log.1's size unchanged at the inflated pre-rotation value — STATIC) proves the streams landed on the fresh file, not the renamed inode.
    6. Cleanup: delete the inflated console.log.1 (junk bytes; the real history worth keeping is the fresh console.log under 5MB) and record the final disk state.
    7. Adapt-and-record branch (only if evidence contradicts the model — e.g. rename still fails with WinError 32 or bytes land in .1): fix the dance in api/log_housekeep.py + api/main.py, re-run python -m pytest tests/test_log_housekeep.py tests/test_boot.py -q, re-run the proof from step 2, and record the delta prominently in the SUMMARY (03-04 precedent). Commit any adaptation scoped: `fix(05-04): adapt boot rotation dance to real cmd semantics`.
  </action>
  <verify>
    <automated>python -c "import os, sys; from api import jobs; base = jobs.jobs_dir(); names = os.listdir(base) if os.path.isdir(base) else []; stems = [n[:-5] for n in names if n.endswith('.json') and not n.startswith('.')]; act = [s for s in stems if (lambda r: r is not None and r.get('status') in ('pending', 'running'))(jobs.read_job(s, base))]; print('non-terminal:', act); sys.exit(1 if act else 0)"</automated>
    <fails_when>exit non-zero (precondition unmet — the printed list shows a running/pending job, so the gate aborts: restart would sweep a live run to interrupted; a corrupt registry file makes read_job raise ValueError, which also stops the scan with the traceback as the report — both are executable stop outcomes, never a silent pass)</fails_when>
    <automated>python -m pytest tests/test_log_housekeep.py tests/test_boot.py -q</automated>
    <fails_when>non-zero exit, or any failed/error above 0 (the rotation unit suites must still be green after any adaptation; if no adaptation happened this run is a no-op re-confirmation)</fails_when>
  </verify>
  <acceptance_criteria>
    - Preconditions verified (task registered, zero running/pending jobs, outside the auction window) and recorded
    - Evidence captured and recorded: console.log.1 exists with the pre-rotation bytes at the inflated size; the fresh console.log carries uvicorn's post-boot INFO banner while .1 stays static
    - Inflated .1 removed; the service is left RUNNING under the scheduled task with a small fresh console.log; unit suites green (adapted code re-verified if the adapt branch ran)
  </acceptance_criteria>
  <done>The M-A dance is proven on the real launch path: rotation happens at boot, one generation is kept, and post-boot uvicorn banner output lands in the fresh file — or the delta is fixed, recorded, and re-proven.</done>
</task>

<task type="auto">
  <name>/health/details live matrix against the running service: 401 no-key / 403 wrong-key / 200 valid-key exact D-29 shape + /health public pins</name>
  <files>api/main.py</files>
  <read_first>
    - C:/Users/Davis/Desktop/gogo/.planning/phases/05-recovery-observability-ops-polish/05-01-SUMMARY.md (the contract as built — expected envelope bytes and body shape to verify against)
  </read_first>
  <action>
    Exercise the live service's three auth outcomes and the exact body shape:

    1. No key: curl -s -i http://127.0.0.1:8000/health/details → record status (expect 401) and the body envelope {"detail": "missing API key", "code": "missing_api_key"} + the WWW-Authenticate: ApiKey header.
    2. Wrong key: curl with an invalid X-API-Key value → record status (expect 403) and {"detail": "invalid API key", "code": "invalid_api_key"} with NO challenge header.
    3. Valid key: read the token into a shell variable from data/api_token.txt (never echo it into the transcript — prohibitions) → curl with it → record status (expect 200), then assert the body with a python one-liner (pipe the curl body): top-level key set exactly {versions, uptime_seconds, last_check}; versions keys {python, uvicorn, fastapi} with non-empty values; uptime_seconds int >= 0; health_job is a parseable ISO timestamp (registry holds >= 1 succeeded health-check job — 4 at planner time) or, if genuinely absent, record the null with the registry state as evidence; market_state_mtime is a parseable ISO (data/market_state.json exists).
    4. Public pins: GET /health → 200 with body key set exactly {status, uptime_seconds}; GET /health/ready → 200 (or the state-consistent response per Phase 2 semantics — record whatever the live service returns).
    5. Record every status code + body verbatim (token value redacted by construction) in the SUMMARY as the SC1 live evidence.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8000/health/details</automated>
    <fails_when>output != "401" (no-key leg on the live service must be 401 — if the service is down or the route is open, the gate stops here and reports)</fails_when>
    <automated>python -c "import sys,json; body=json.load(sys.stdin); assert set(body)=={'versions','uptime_seconds','last_check'}, body; assert set(body['versions'])=={'python','uvicorn','fastapi'}; assert set(body['last_check'])=={'health_job','market_state_mtime'}; print('SHAPE-OK')"</automated>
    <fails_when>stdin body fails any assertion or prints anything but SHAPE-OK (200-body shape check — the curl from step 3 pipes into this)</fails_when>
  </verify>
  <acceptance_criteria>
    - The 401 and 403 legs returned the pinned envelopes with correct challenge-header presence/absence
    - The 200 leg passed the exact-shape assertion; health_job/market_state_mtime parseable (or null-with-evidence recorded)
    - /health + /health/ready public legs recorded; all outputs recorded verbatim (no token value in any record)
  </acceptance_criteria>
  <done>The live service answers /health/details with the pinned 401/403/200 contract and the exact D-29 shape — SC1 is proven on the running process, with the evidence recorded for the human review.</done>
</task>

<task type="auto">
  <name>Final SC3 suite + hygiene + end-of-phase human-review checklist with [ASSUMED] verdicts → 05-04-SUMMARY.md (Phase 5 gate record)</name>
  <files>api/main.py</files>
  <read_first>
    - C:/Users/Davis/Desktop/gogo/.planning/phases/05-recovery-observability-ops-polish/05-PATTERNS.md (flagged rows inventory cross-check for the verdict list)
  </read_first>
  <action>
    Close the phase gate:

    1. Final full suite: python -m pytest -q → record the exact pass/skip counts (expected 157+ passed, 1 env-conditional skip — the single skip is the test_health.py env-conditional one); assert data/ logs/ hygiene clean (git status --porcelain -- data/ logs/ empty).
    2. SC3 statement: the conftest autouse network-blocking fixture is part of the suite by construction (any test touching the network fails) — record the fixture's presence + the green run as the SC3 evidence for both machines.
    3. Write 05-04-SUMMARY.md — the Phase 5 closing record: (a) live evidence verbatim from Tasks 1-2 (rotation sizes, matrix status codes + bodies, shape assertion output); (b) final suite counts; (c) the human-review checklist — one line per flagged row across the four plans: 05-01's [ASSUMED] rows (market-state null semantics, uptime wiring, newest-job definition, ISO computation), 05-02's (two-repoint ordering, uniform dance, 25+3 seed, prune redundancy), 05-03's (task absent, README presence, count wording) — each with a verdict line for the operator to mark ACCEPT or DELTA with a fallback note from the plan's Flagged Assumptions section; (d) the D-36 record cross-reference (05-03-SUMMARY.md); (e) the Mac checklist handoff pointer (README segment, user executes + back-fills per D-35).
    4. End-of-phase human review (human_verify_mode = end-of-phase — no checkpoint task): present the SUMMARY + the live outputs to the operator with the checklist; the operator's review outcome (verdicts marked) is the phase's closing act — record it in the SUMMARY when received.
    5. Commit the gate record: `docs(05): phase gate evidence + human-review checklist (OPS-03 close)` for the SUMMARY (plus any adaptation commits from Task 1, already scoped).
  </action>
  <verify>
    <automated>python -m pytest -q</automated>
    <fails_when>non-zero exit, or any failed/error above 0, or "no tests ran" — the final suite must pass on the real machine; if it lands below the 157+ floor the SUMMARY records the delta and the human review is explicitly flagged</fails_when>
    <automated>git status --porcelain -- data/ logs/</automated>
    <fails_when>non-empty output (hygiene after the live gate — the service's own console.log is gitignored; tracked data/logs files must show zero change)</fails_when>
    <human-check>The operator reviews the Phase 5 outputs: the live /health/details matrix transcript, the rotation evidence, the D-36 record, and the flagged [ASSUMED] verdict list in 05-04-SUMMARY.md — marking each ACCEPT or DELTA. The review outcome is recorded in the SUMMARY. (03-04/04-07 end-of-phase gate twin)</human-check>
  </verify>
  <acceptance_criteria>
    - Full suite green with exact counts recorded (157+ / 1 skip expected); hygiene clean
    - 05-04-SUMMARY.md holds the live evidence, the human-review checklist with verdict lines, the D-36 cross-reference, and the Mac handoff pointer
    - The operator's review verdicts are recorded when received; the phase gate commits land scoped
  </acceptance_criteria>
  <done>Phase 5 closes with live SC1/SC2 evidence, a green final suite on the real machine, and an explicit human-review checklist where every flagged decision gets an operator verdict.</done>
</task>

</tasks>

## Artifacts this phase produces (symbols)

- Live evidence record (in `05-04-SUMMARY.md`) — rotation sizes + stream-append proof, /health/details 401/403/200 transcripts, /health public pins, final suite counts
- `.planning/phases/05-recovery-observability-ops-polish/05-04-SUMMARY.md` (new path, committed) — the Phase 5 closing gate record incl. the human-review checklist with [ASSUMED] verdict lines and the Mac handoff pointer
- Contingency: `api/log_housekeep.py` + `api/main.py` adaptations only if the live evidence contradicts the machine-verified dance model (03-04 adapt-and-record)

## Source Audit (multi-source coverage)

```
SOURCE    | ID      | Feature/Requirement                     | Plan  | Status
--------- | ------- | --------------------------------------- | ----- | ------
GOAL      | SC1     | 有效 key 得 versions/uptime/last-check, 无 key 401 — LIVE | 05-04 | COVERED (task 2 matrix)
GOAL      | SC2     | 常驻周级运行磁盘有界 — LIVE rotation proof | 05-04 | COVERED (task 1)
GOAL      | SC3     | Win/Mac 双机套件通过 + 网络封锁证明 — Win final | 05-04 | COVERED (task 3; Mac 经 05-03 清单用户执行)
REQ       | OPS-03  | 全套件收口 + end-of-phase human review   | 05-04 | COVERED (tasks 1-3)
CONTEXT   | D-32/D-33 | live 实机验证 (boot 轮转真实路径)          | 05-04 | COVERED (task 1)
CONTEXT   | D-36    | 审计记录交叉引用                          | 05-04 | COVERED (task 3 record)
```

Other source rows covered by sibling plans: /health/details contract + SC2 audit → 05-01; rotation + registry cap implementation → 05-02; D-35/D-36/D-37 docs + classification row → 05-03. Exclusions (not gaps): v2 OPS-04/05/06, ACT-04/05, 进程守护, GUI items (CLI-only), Mac 实跑 (跨机 rollout — checklist delivered in 05-03, run by user, back-filled per D-35).

## Assumption-delta decision

No assumption-delta payload was delivered with this plan-phase invocation. This gate's [ASSUMED] rows (service running state, cmd handle semantics match, 157+ floor, auction-window avoidance) are all precondition-scanned or both-branch handled inside the tasks; the verdict collection for the earlier plans' [ASSUMED] rows is the human-review checklist in Task 3 — the phase's final decision gate.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent ↔ real service process | Live manipulation (stop/start/inflate) of the production service — the only plan that touches the real launch path |
| Agent ↔ real registry | Read-only precondition scans; a restart sweeps only non-terminal jobs, hence the zero-inflight precondition |
| Live outputs ↔ SUMMARY record | Evidence recorded verbatim must not leak the token or misstate sizes/codes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-12 | Denial of Service | Restart during a live job sweeps it interrupted / breaks the auction window | high | mitigate | Zero-inflight registry precondition (abort + report if unmet) and the 9:15-9:35 avoidance rule; restart via the real task only, never a parallel instance (single-writer registry assumption) |
| T-05-13 | Tampering | Inflate step corrupting the real console.log | medium | mitigate | Append-only inflation via python open in append mode (no truncate, no shell redirect); the inflated .1 is deleted after evidence; fresh console.log left small; hygiene check at the gate |
| T-05-14 | Information Disclosure | Token leaking into transcripts/SUMMARY | high | mitigate | Token read into a shell variable, never echoed (D-05/T-04-18); transcripts record status codes + bodies with the key value structurally absent; SUMMARY prohibition row |
| T-05-15 | Spoofing | Evidence misstated in the SUMMARY (sizes/codes/adaptations) | medium | mitigate | Verbatim capture into the SUMMARY from command outputs in the same task; adapt-and-record branch re-runs suites and re-proves before recording |
| T-05-SC | Tampering | package installs | high | mitigate | Zero package installs — package-legitimacy gate not triggered |

ASVS level 1, blocking threshold high: the high rows map to concrete task steps (Task 1 precondition scan, Task 2 redacted capture). Package legitimacy: no install commands exist in this plan.
</threat_model>

<verification>
- Task 1: precondition scan passes (zero running/pending — executable one-liner, no placeholder); rotation evidence recorded (sizes, .1 existence, banner-in-fresh-file proof); unit suites green; inflated .1 removed; service left RUNNING.
- Task 2: 401/403/200 matrix + exact-shape python assertion pass on the live service; /health + /health/ready public pins recorded; no token in any output.
- Task 3: python -m pytest -q green with exact counts (157+ / 1 skip); hygiene clean; 05-04-SUMMARY.md written with live evidence + human-review checklist; human-check presented to the operator with verdicts recorded on return.
</verification>

<success_criteria>
- Live rotation proof captured on the real task launch path: .1 holds the inflated pre-rotation bytes, uvicorn's post-boot banner writes land in the fresh console.log (SC2 live).
- /health/details live matrix: pinned 401/403 envelopes + exact D-29 200 shape on the running service (SC1 live).
- Final full suite green with exact counts recorded (157+ passed, 1 env-conditional skip); data/ and logs/ clean; token never appears in any record (SC3 Win-side + network-blocking statement).
- 05-04-SUMMARY.md closes Phase 5 with the human-review checklist; operator verdicts recorded when returned.
</success_criteria>

<output>
Create .planning/phases/05-recovery-observability-ops-polish/05-04-SUMMARY.md when done — the Phase 5 gate record: rotation evidence verbatim (sizes before/after, .1 proof, append-target proof), /health/details matrix transcripts (token redacted), /health public pins, final suite counts, the human-review checklist with one verdict line per flagged [ASSUMED] row from all four plans, the D-36 cross-reference, and the Mac checklist handoff pointer.
</output>
